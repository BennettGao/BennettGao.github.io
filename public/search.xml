<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux基础环境配置</title>
    <url>/2023/09/13/Linux%E5%9F%BA%E6%9C%AC%E9%85%8D%E7%BD%AE%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<h6 id="1-切换为root用户"><a href="#1-切换为root用户" class="headerlink" title="1.切换为root用户"></a>1.切换为root用户</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[hadoop@localhost ~]$ su -root</span><br></pre></td></tr></table></figure>

<h6 id="2-修改主机名"><a href="#2-修改主机名" class="headerlink" title="2.修改主机名"></a>2.修改主机名</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@localhost ~]hostnamectl set-hostname master</span><br><span class="line">[root@localhost ~]bash</span><br><span class="line">[root@master ~]</span><br></pre></td></tr></table></figure>

<h6 id="3-静态网络配置"><a href="#3-静态网络配置" class="headerlink" title="3.静态网络配置"></a>3.静态网络配置</h6><p>1.查看ip地址的起始和结束地址</p>
<p><img src="https://img-blog.csdnimg.cn/73f221c91d834bb1ae62deb85e229bd9.png?x-oss-process=image/watermark,type_ZHJvaWRzYW5zZmFsbGJhY2s,shadow_50,text_Q1NETiBA5bCP5rGk5ZCM5a2mVFlU,size_12,color_FFFFFF,t_70,g_se,x_16#pic_left" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/c974890da4d84d80bbcb9a7f8f674495.png?x-oss-process=image/watermark,type_ZHJvaWRzYW5zZmFsbGJhY2s,shadow_50,text_Q1NETiBA5bCP5rGk5ZCM5a2mVFlU,size_11,color_FFFFFF,t_70,g_se,x_16#pic_left" alt="在这里插入图片描述"></p>
<p>2.修改网络配置文件</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]# vim /etc/sysconfig/network-scripts/ifcfg-ens33 </span><br></pre></td></tr></table></figure>

<p>ip地址参照4步骤自行选择（必须在起始和结束的范围内）<br>子网掩码默认设置为255.255.255.0<br>网关的值为将ip地址中最后一段的值改为2<br>DNS使用谷歌提供的免费dns1：8.8.8.8</p>
<p><img src="D:\Desktop\Hadoop截图\Inked屏幕截图%202022-08-11%20113459.jpg"></p>
<p>3.重启网络服务，查看是否配置成功</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">systemctl restart network</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ping www.baidu.com</span><br></pre></td></tr></table></figure>

<p><img src="https://img-blog.csdnimg.cn/cc05b71850684ab6a03ea6c609f6fa2a.png?x-oss-process=image/watermark,type_ZHJvaWRzYW5zZmFsbGJhY2s,shadow_50,text_Q1NETiBA5bCP5rGk5ZCM5a2mVFlU,size_16,color_FFFFFF,t_70,g_se,x_16#pic_left"></p>
<h6 id="4-主机映射配置hosts文件"><a href="#4-主机映射配置hosts文件" class="headerlink" title="4.主机映射配置hosts文件"></a>4.主机映射配置hosts文件</h6><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vim /etc/hosts</span><br></pre></td></tr></table></figure>

<p><img src="https://img-blog.csdnimg.cn/153747e13bf945268275556851466207.png#pic_center" alt="在这里插入图片描述"></p>
<p>改为自己主机IP地址</p>
<h6 id="5-关闭防火墙（三台都要关闭）"><a href="#5-关闭防火墙（三台都要关闭）" class="headerlink" title="5.关闭防火墙（三台都要关闭）"></a>5.关闭防火墙（三台都要关闭）</h6><p>1.关闭防火墙</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]# systemctl stop firewalld.service</span><br></pre></td></tr></table></figure>

<p>2.关闭防火墙自启</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]# systemctl disable firewalld.service</span><br></pre></td></tr></table></figure>

<p>3.查看防火墙状态</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]# systemctl status firewalld.service</span><br></pre></td></tr></table></figure>

<h6 id="6-禁用SELinux"><a href="#6-禁用SELinux" class="headerlink" title="6.禁用SELinux"></a>6.禁用SELinux</h6><p>1.临时关闭</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]#setenforce 0</span><br></pre></td></tr></table></figure>

<p>2.永久关闭</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]#vim /etc/selinux/config</span><br></pre></td></tr></table></figure>

<p>修改&#x2F;etc&#x2F;selinux目录下的config文件<br>将SELINUX&#x3D;enforcing改为SELINUX&#x3D;disabled</p>
<p><img src="https://img-blog.csdnimg.cn/20210318175524364.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl81MDA5MzM0Mw==,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述的"> <img src="https://img-blog.csdnimg.cn/20210318175637486.png" alt="在这里插入图片描述"></p>
<p> 然后重启服务器即可。</p>
<h6 id="7-ssh免密（实现远程登录和免密等率）"><a href="#7-ssh免密（实现远程登录和免密等率）" class="headerlink" title="7.ssh免密（实现远程登录和免密等率）"></a>7.ssh免密（实现远程登录和免密等率）</h6><p>1.生成公钥(三个主机同时进行)</p>
<p>切换为hadoop用户</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[root@master ~]#su - hadoop</span><br><span class="line">[hadoop@master ~]#ssh-keygen -t rsa -P &quot;&quot;</span><br></pre></td></tr></table></figure>

<p>2.把每一台的公钥拷贝到同一台虚拟机：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[hadoop@master ~]#ssh-copy-id -i ~/.ssh/id_rsa.pub masterIP地址   (root用户.ssh在root目录下)</span><br></pre></td></tr></table></figure>

<p><img src="https://img-blog.csdnimg.cn/20210815173926689.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzUwNjMxNzgz,size_16,color_FFFFFF,t_70"></p>
<p>3.将有所有的公钥文件分别拷贝到另外两个节点（拷贝2个文件）</p>
<p>在master 上执行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[hadoop@master ~]#scp -r ~/.ssh/authorized_keys hadoop@slave1:~/.ssh/</span><br><span class="line">[hadoop@master ~]#scp -r ~/.ssh/known_hosts hadoop@slave1:~/.ssh/</span><br><span class="line">[hadoop@master ~]#scp -r ~/.ssh/authorized_keys hadoop@slave2:~/.ssh/</span><br><span class="line">[hadoop@master ~]#scp -r ~/.ssh/known_hosts hadoop@slave2:~/.ssh/   </span><br></pre></td></tr></table></figure>

<p>4、测试免密是否成功</p>
<p>ssh 主机ip，不输密码就能登上就是成功 </p>
<p><img src="https://img-blog.csdnimg.cn/20210815175424491.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L20wXzUwNjMxNzgz,size_16,color_FFFFFF,t_70"></p>
]]></content>
      <categories>
        <category>大数据</category>
      </categories>
  </entry>
  <entry>
    <title>黑马Python学习笔记</title>
    <url>/2023/08/15/%E9%BB%91%E9%A9%ACPython%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<meta name="referrer" content="no-referrer" />

<ol>
<li><p>Pthon语言基础入门 </p>
</li>
<li><p>&amp;nbsp;Pthon语言高阶加强 </p>
</li>
<li><p>大数据分析PySpark</p>
</li>
<li><p>你好Python程序</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;Hello World!!&quot;</span>)</span><br><span class="line"></span><br><span class="line">&gt;&gt;Hello World!!</span><br></pre></td></tr></table></figure>

<p>！输入的双引号和括号为英文字符</p>
<h2 id="（一）-字面量"><a href="#（一）-字面量" class="headerlink" title="（一） 字面量"></a>（一） 字面量</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. 定义：在代码中，被写下来的固定的值，称之为字面量。</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2. 常用的6种值的类型</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 3. 字符串</p>
<p>！Python中，字符串需要用双引号包围；</p>
<p>！被双引号包围的都是字符串</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">666</span></span><br><span class="line"><span class="number">13.14</span></span><br><span class="line"><span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line"></span><br><span class="line">print( <span class="number">666</span> )</span><br><span class="line">print( <span class="number">13.14</span> )</span><br><span class="line">print( <span class="string">&quot;黑马程序员&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt;<span class="number">666</span></span><br><span class="line">&gt;&gt;<span class="number">13.14</span></span><br><span class="line">&gt;&gt;<span class="string">&quot;黑马程序员&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="（二）-注释"><a href="#（二）-注释" class="headerlink" title="（二） 注释"></a>（二） 注释</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. 单行注释</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 我是单行注释</span><br><span class="line">print( <span class="string">&quot;黑马程序员&quot;</span> )</span><br></pre></td></tr></table></figure>

<p>！规范：#号和注释内容一般建议以一个空格隔开</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2. 多行注释</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">         我是多行注释    </span></span><br><span class="line"><span class="string">         666</span></span><br><span class="line"><span class="string">         13.14</span></span><br><span class="line"><span class="string">         &quot;黑马程序员&quot;</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"></span><br><span class="line">print( <span class="number">666</span> )</span><br><span class="line">print( <span class="number">13.14</span> )</span><br><span class="line">print( <span class="string">&quot;黑马程序员&quot;</span> )</span><br></pre></td></tr></table></figure>

<h2 id="（三）-变量"><a href="#（三）-变量" class="headerlink" title="（三） 变量"></a>（三） 变量</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. 定义：程序运行时，能储存计算结果或表示值的抽象概念。</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 简单的说，变量就是在程序运行时，记录数据用的</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2.定义格式：</p>
<p>变量名称 &#x3D; 变量的值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 定义变量</span><br><span class="line">money = <span class="number">5000</span></span><br><span class="line"></span><br><span class="line"># print输出变量</span><br><span class="line">print( <span class="string">&quot; 钱包还有：&quot;</span> , money )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 钱包还有： <span class="number">5000</span></span><br><span class="line"></span><br><span class="line"># 买了东西，花费<span class="number">10</span>元</span><br><span class="line">money = money - <span class="number">10</span></span><br><span class="line">print( <span class="string">&quot;买了冰淇淋花费10元，还剩余：&quot;</span> , money , <span class="string">&quot;元&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt;买了冰淇淋花费<span class="number">10</span>元，还剩余： <span class="number">4990</span> 元</span><br></pre></td></tr></table></figure>

<p>！变量值可变</p>
<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/95b19ca5662844c3ab314002927b9563.png" alt="img"></p>
<h2 id="（四）-数据类型"><a href="#（四）-数据类型" class="headerlink" title="（四） 数据类型"></a>（四） 数据类型</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. type()语句【具有返回值】</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1）语法：</p>
<p>type(被查看类型的数据)</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2）使用方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">a = type( <span class="number">666</span> )</span><br><span class="line">print( a )</span><br><span class="line"></span><br><span class="line">print( type( <span class="number">13.14</span> ) )</span><br><span class="line"></span><br><span class="line">name = <span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line">name_type = type( name )</span><br><span class="line">print( name_type )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;int&#x27;</span>&gt;</span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;float&#x27;</span>&gt;</span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2. 变量无类型，查看变量的类型实际上查看的是变量存储数据的类型</p>
<p>！字符串变量：不是字符串是变量，而是变量存储了字符串</p>
<h2 id="（五）-数据类型转换"><a href="#（五）-数据类型转换" class="headerlink" title="（五） 数据类型转换"></a>（五） 数据类型转换</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1.&amp;nbsp; 常见的转换语句</p>
<p>！此三种语句具有返回值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 将数字类型  -&gt;  字符串类型</span><br><span class="line">num_str = str( <span class="number">11</span> )</span><br><span class="line">print( type( num_str ) , num_str )</span><br><span class="line"></span><br><span class="line">float_str = str( <span class="number">13.14</span> )</span><br><span class="line">print( type( float_str ) , float_str )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt; <span class="number">11</span></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt; <span class="number">13.14</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 将字符串类型   -&gt;  数字类型</span><br><span class="line">num = <span class="type">int</span>( <span class="string">&quot;11&quot;</span> )</span><br><span class="line">print( type( num ) , num )</span><br><span class="line"></span><br><span class="line">num2 = <span class="type">float</span>( <span class="string">&quot;13.14&quot;</span> )</span><br><span class="line">print( type( <span class="type">float</span> ) , num2 )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;int&#x27;</span>&gt; <span class="number">11</span></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;float&#x27;</span>&gt; <span class="number">13.14</span></span><br></pre></td></tr></table></figure>

<p>！想要将字符串转换成数字，必须要求字符串内的内容都是数字</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">num3 = <span class="type">int</span> ( <span class="string">&quot;黑马程序员&quot;</span> )</span><br><span class="line">print( type( num3 ) , num3 )</span><br><span class="line"></span><br><span class="line">&gt;&gt; ValueError: invalid literal <span class="keyword">for</span> <span class="title function_">int</span><span class="params">()</span> with base <span class="number">10</span>: <span class="string">&#x27;黑马程序员&#x27;</span></span><br></pre></td></tr></table></figure>

<p>！浮点数转整数会丢失精度</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 整数 -&gt; 浮点数</span><br><span class="line">float_num = <span class="type">float</span>( <span class="number">11</span> )</span><br><span class="line">print( type( float_num ) , float_num )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;float&#x27;</span>&gt; <span class="number">11.0</span></span><br><span class="line"></span><br><span class="line"># 浮点数 -&gt; 整数</span><br><span class="line">int_num = <span class="type">int</span>( <span class="number">13.14</span> )</span><br><span class="line">print( type( int_num ) , int_num )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;int&#x27;</span>&gt; <span class="number">13</span></span><br></pre></td></tr></table></figure>

<h2 id="（六）标识符"><a href="#（六）标识符" class="headerlink" title="（六）标识符"></a>（六）标识符</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. 定义：用户在编程时所用的一系列名字，用于给变量、类、方法等命名。&amp;nbsp;</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2. 命名规则：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; 内容限定（英文，中文，数字，下划线_）</p>
<p>！数字不可开头</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; &amp;nbsp; &amp;nbsp; 大小写敏感</p>
<p>&amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp;&amp;nbsp; 不可使用关键字 &amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 3. 命名规范：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 见名知义</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 下划线命名法</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 英文字母全小写</p>
<h2 id="（七）-远算符"><a href="#（七）-远算符" class="headerlink" title="（七） 远算符"></a>（七） 远算符</h2><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1. 算术运算符：+、-、*、&#x2F;、&#x2F;&#x2F;、%、**（指数）</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 2. 赋值运算符：&#x3D;</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 3. 复合赋值运算符：+&#x3D;、-&#x3D;、*&#x3D;、&#x2F;&#x3D;、&#x2F;&#x2F;&#x3D;、%&#x3D;、**&#x3D;</p>
<h2 id="（八）-字符串扩展"><a href="#（八）-字符串扩展" class="headerlink" title="（八） 字符串扩展"></a>（八） 字符串扩展</h2><h3 id="1-3种定义方式"><a href="#1-3种定义方式" class="headerlink" title="1. 3种定义方式"></a>1. 3种定义方式</h3><p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 1）定义形式：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ① 单引号定义法：</p>
<p>name &#x3D; ‘黑马程序员’</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ②双引号定义法：</p>
<p>name &#x3D; “黑马程序员”</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ③三引号定义法：</p>
<p>name &#x3D; “””黑马程序员”””</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; *支持换行操作，使用变量接收它，则为字符串；不使用变量接收，则为多行注释</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 单引号定义法，使用单引号进行包围</span><br><span class="line">name = <span class="string">&#x27;黑马程序员&#x27;</span></span><br><span class="line">print( type(name) )</span><br><span class="line"></span><br><span class="line"># 双引号定义法，写法和多行注释是一样的</span><br><span class="line">name = <span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line">print( type(name) )</span><br><span class="line"></span><br><span class="line"># 三引号定义法，写法和多行注释是一样的</span><br><span class="line">name = <span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">我是</span></span><br><span class="line"><span class="string">黑马</span></span><br><span class="line"><span class="string">程序员</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line">print( type(name) )</span><br><span class="line"></span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br><span class="line">&gt;&gt; &lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 2）字符串的引号嵌套</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ①单引号法，可以内含双引号；</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ②双引号定义法，可以内含单引号；</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ②可以使用转义字符（\）来将引号解除效用，变成普通字符串。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 字符串内，包含双引号</span><br><span class="line">name = <span class="string">&#x27;&quot;黑马程序员&quot;&#x27;</span></span><br><span class="line">print( name )</span><br><span class="line"></span><br><span class="line"># 字符串内，包含单引号</span><br><span class="line">name = <span class="string">&quot;&#x27;黑马程序员&#x27;&quot;</span></span><br><span class="line">print( name )</span><br><span class="line"></span><br><span class="line"># 使用转义字符（\）解除引号的效用</span><br><span class="line">name = <span class="string">&quot;\&quot;黑马程序员\&quot;&quot;</span></span><br><span class="line">print( name )</span><br><span class="line">name = <span class="string">&#x27;\&#x27;黑马程序员\&#x27;&#x27;</span></span><br><span class="line">print( name )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; <span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line">&gt;&gt; <span class="string">&#x27;黑马程序员&#x27;</span></span><br><span class="line">&gt;&gt; <span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line">&gt;&gt; <span class="string">&#x27;黑马程序员&#x27;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-字符串拼接"><a href="#2-字符串拼接" class="headerlink" title="2. 字符串拼接"></a>2. 字符串拼接</h3><p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 1）通过+号进行拼接</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&quot;黑马程序员&quot;</span></span><br><span class="line">print( <span class="string">&quot;我的名字是：&quot;</span> + name + <span class="string">&quot;我可以教大家IT技能&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 我的名字是：黑马程序员我可以教大家IT技能</span><br></pre></td></tr></table></figure>

<p>！无法和非字符串类型进行拼接</p>
<h3 id="3-字符串格式化"><a href="#3-字符串格式化" class="headerlink" title="3. 字符串格式化"></a>3. 字符串格式化</h3><p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 1）“%占位符”%变量</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 2）常用占位符</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 字符串：%s</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 整数：&amp;nbsp;&amp;nbsp; %d</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 浮点数：%f</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 3）数字精度控制</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 辅助符号“m.n”来控制数据的宽度和精度</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; m ：控制宽度，要求是数字（很少使用），设置的宽度小于数字本身，不生效</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; .n：控制小数点精度，要求是数字，会进行小数的四舍五入</p>
<blockquote>
<p>%5d：表示将整数的宽度控制在5位，如数字11，被设置为5d，就会变成：[空格][空格][空格]11，用三个空格补足宽度</p>
</blockquote>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">num1 = <span class="number">11</span></span><br><span class="line">num2 = <span class="number">11.345</span></span><br><span class="line">print( <span class="string">&quot;数字11宽度限制5，结果：%5d&quot;</span> % num1 )</span><br><span class="line">print( <span class="string">&quot;数字11宽度限制1，结果：%1d&quot;</span> % num1 )</span><br><span class="line">print( <span class="string">&quot;数字11.345宽度限制7，小数精度2，结果：%7.2f&quot;</span> % num2 )</span><br><span class="line">print( <span class="string">&quot;数字11.345不限制宽度，小数精度2，结果：%.2f&quot;</span> % num2 )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 数字<span class="number">11</span>宽度限制<span class="number">5</span>，结果：   <span class="number">11</span>                             # 宽度<span class="number">5</span>，补了<span class="number">3</span>个空格</span><br><span class="line">&gt;&gt; 数字<span class="number">11</span>宽度限制<span class="number">1</span>，结果：<span class="number">11</span>                                # 宽度小于数字本身，无影响</span><br><span class="line">&gt;&gt; 数字<span class="number">11.345</span>宽度限制<span class="number">7</span>，小数精度<span class="number">2</span>，结果：  <span class="number">11.35</span> </span><br><span class="line">   # 宽度<span class="number">7</span>，补了<span class="number">2</span>个空格，小数精度<span class="number">2</span>，四舍五入后为<span class="number">.35</span></span><br><span class="line">&gt;&gt; 数字<span class="number">11.345</span>不限制宽度，小数精度<span class="number">2</span>，结果：<span class="number">11.35</span>              </span><br><span class="line">   # 不限制宽度，小数点后四舍五入后为<span class="number">.35</span></span><br></pre></td></tr></table></figure>

<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 4）快速写法</p>
<p>语法：</p>
<p>f”内容{变量}”</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&quot;传智播客&quot;</span></span><br><span class="line">set_up_year = <span class="number">2006</span></span><br><span class="line">stock_price = <span class="number">19.99</span></span><br><span class="line">print( f<span class="string">&quot;我是&#123;name&#125;，我成立于&#123;set_up_year&#125;。我今天的股票价格是：&#123;stock_price&#125;&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 我是传智播客，我成立于<span class="number">2006</span>。我今天的股票价格是：<span class="number">19.99</span></span><br><span class="line"># 不做精度控制，原样输出</span><br></pre></td></tr></table></figure>

<p>！以上字符串格式化均可用于表达式格式化</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print( <span class="string">&quot;1 * 1的结果是：%d&quot;</span> % ( <span class="number">1</span> * <span class="number">1</span> ) )</span><br><span class="line">print( f<span class="string">&quot;1 * 1的结果是：&#123;1 * 1&#125;&quot;</span>)</span><br><span class="line">print( <span class="string">&quot;字符串在Python中的类型是：%s&quot;</span> % type(<span class="string">&#x27;字符串&#x27;</span>) )</span><br><span class="line"></span><br><span class="line">&gt;&gt; <span class="number">1</span> * <span class="number">1</span>的结果是：<span class="number">1</span></span><br><span class="line">&gt;&gt; <span class="number">1</span> * <span class="number">1</span>的结果是：<span class="number">1</span></span><br><span class="line">&gt;&gt; 字符串在Python中的类型是：&lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/81292ae9fabb4a0db9d522cd12e9506d.png" alt="img"></p>
<h2 id="（九）数据输入"><a href="#（九）数据输入" class="headerlink" title="（九）数据输入"></a>（九）数据输入</h2><ol>
<li>语法</li>
</ol>
<p>input();</p>
<ol start="2">
<li>使用：</li>
</ol>
<p>使用input()语句可以从键盘获取输入</p>
<p>使用一个变量接收（存储）input语句获取的键盘输入数据即可</p>
<p>数据输出：print</p>
<p>数据输入：input</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print( <span class="string">&quot;请告诉我你是谁？&quot;</span> )</span><br><span class="line">name = input()</span><br><span class="line">print( <span class="string">&quot;GET！！你是：%s&quot;</span>  % name )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; 请告诉我你是谁？</span><br><span class="line">&gt;&gt; XUANseyo</span><br><span class="line">&gt;&gt; GET！！你是：XUANseyo</span><br></pre></td></tr></table></figure>

<p>! input()语句可以在要求使用者输入内容之前，输出提示内容的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">name = input( <span class="string">&quot;你是谁？&quot;</span> )</span><br><span class="line">print( <span class="string">&quot;GET！！你是：%s&quot;</span> % name )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; 你是谁？XUANseyo</span><br><span class="line">&gt;&gt; GET！！你是：XUANseyo</span><br></pre></td></tr></table></figure>

<p>！无论input()输入什么，其都会自动转为字符串类型，我们要自行进行数据类型转换</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">num = input( <span class="string">&quot;你的银行卡密码是：&quot;</span> )</span><br><span class="line">print( <span class="string">&quot;你的银行卡密码的类型是：&quot;</span> , type( num ) )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 你的银行卡密码是：<span class="number">123456</span></span><br><span class="line">&gt;&gt; 你的银行卡密码的类型是： &lt;class <span class="string">&#x27;str&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<p>$练习：&amp;nbsp;</p>
<p><img src="https://img-blog.csdnimg.cn/3fbf7acdaa8a48fbb4a4464767b05a70.png" alt="img"></p>
<h2 id="（一）-布尔类型和比较运算符"><a href="#（一）-布尔类型和比较运算符" class="headerlink" title="（一） 布尔类型和比较运算符"></a>（一） 布尔类型和比较运算符</h2><ol>
<li>布尔类型：判断结果</li>
</ol>
<p>&amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; True：表示真（是、肯定）</p>
<p>&amp;nbsp;&amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp;&amp;nbsp; &amp;nbsp; False：表示假（否、否定）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 定义变量存储布尔类型的数据</span><br><span class="line">bool_1 = <span class="type">True</span></span><br><span class="line"><span class="variable">bool_2</span> <span class="operator">=</span> False</span><br><span class="line"><span class="title function_">print</span><span class="params">( f<span class="string">&quot;bool_1变量的内容是：&#123; bool_1 &#125;，类型为：&#123; type( bool_1 ) &#125;&quot;</span> )</span></span><br><span class="line">print( f<span class="string">&quot;bool_2变量的内容是：&#123; bool_2 &#125;，类型为：&#123; type( bool_2 ) &#125;&quot;</span> )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; bool_1变量的内容是：True，类型为：&lt;class <span class="string">&#x27;bool&#x27;</span>&gt;</span><br><span class="line">&gt;&gt; bool_2变量的内容是：False，类型为：&lt;class <span class="string">&#x27;bool&#x27;</span>&gt;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>比较运算和比较运算符：</li>
</ol>
<p>&#x3D;&#x3D;、!&#x3D;、&gt;、&lt;、&gt;&#x3D;、&lt;&#x3D;</p>
<h2 id="（二）-if语句的基本格式"><a href="#（二）-if语句的基本格式" class="headerlink" title="（二） if语句的基本格式"></a>（二） if语句的基本格式</h2><ol>
<li>if语句基本格式：</li>
</ol>
<p>if 要判断的条件：</p>
<p>&amp;nbsp;&amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp; &amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 条件成立时，要做的事情</p>
<p>！首行缩进确定代码块归属</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">age = <span class="number">30</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> age &gt;= <span class="number">18</span>:</span><br><span class="line">    print( <span class="string">&quot;我已经成年了。&quot;</span> )</span><br><span class="line">    print( f<span class="string">&quot;已经&#123; age &#125;岁了啊！&quot;</span> )</span><br><span class="line">print( <span class="string">&quot;时间过得真快啊！&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 我已经成年了。</span><br><span class="line">&gt;&gt; 已经<span class="number">30</span>岁了啊！</span><br><span class="line">&gt;&gt; 时间过得真快啊！</span><br></pre></td></tr></table></figure>

<h1 id="判断语句的结果必须为布尔类型的True或False"><a href="#判断语句的结果必须为布尔类型的True或False" class="headerlink" title="判断语句的结果必须为布尔类型的True或False"></a>判断语句的结果必须为布尔类型的True或False</h1><p>True会执行if内的代码语句</p>
<p>False则不会执行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">age = <span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> age &gt;= <span class="number">18</span>:</span><br><span class="line">    print( <span class="string">&quot;我已经成年了。&quot;</span> )</span><br><span class="line">    print( f<span class="string">&quot;已经&#123; age &#125;岁了啊！&quot;</span> )</span><br><span class="line">print( <span class="string">&quot;时间过得真快啊！&quot;</span> )</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; 时间过得真快啊！</span><br></pre></td></tr></table></figure>

<p>&amp;nbsp;$练习：&amp;nbsp;</p>
<p><img src="https://img-blog.csdnimg.cn/62d886c01d9344cb9a846446140f8f5e.png" alt="img"></p>
<h2 id="（三）-if-else-语句"><a href="#（三）-if-else-语句" class="headerlink" title="（三） if else 语句"></a>（三） if else 语句</h2><ol>
<li>基本格式：</li>
</ol>
<p>if 条件 ：</p>
<p>满足条件时要完成的事1</p>
<p>满足条件时要完成的事2</p>
<p>……</p>
<p>else：</p>
<p>不满足条件时要完成的事1</p>
<p>不满足条件时要完成的事2</p>
<p>……</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print( <span class="string">&quot;欢迎来到黑马儿童游乐场，儿童免费，成人收费！&quot;</span> )</span><br><span class="line">age =  input( <span class="string">&quot;请输入您的年龄：&quot;</span> )</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> <span class="title function_">int</span><span class="params">(age)</span> &gt;= <span class="number">18</span> :</span><br><span class="line">    print( <span class="string">&quot;您已成年，游玩需补票10元&quot;</span> )</span><br><span class="line"><span class="keyword">else</span> :</span><br><span class="line">    print( <span class="string">&quot;您未成年，无需补票&quot;</span> )</span><br><span class="line"></span><br><span class="line">print( <span class="string">&quot;祝您游玩愉快！！！&quot;</span> )</span><br><span class="line"></span><br><span class="line">&gt;&gt; 欢迎来到黑马儿童游乐场，儿童免费，成人收费！</span><br><span class="line">&gt;&gt; 请输入您的年龄：<span class="number">17</span></span><br><span class="line">&gt;&gt; 您未成年，无需补票</span><br><span class="line">&gt;&gt; 祝您游玩愉快！！！</span><br></pre></td></tr></table></figure>

<p>&amp;nbsp;&amp;nbsp;$练习：&amp;nbsp;<img src="https://img-blog.csdnimg.cn/ea67107c6fcd45f8b687b20b1aaa172c.png" alt="img"></p>
<h2 id="（四）-if-elif-else-语句"><a href="#（四）-if-elif-else-语句" class="headerlink" title="（四） if elif else 语句"></a>（四） if elif else 语句</h2><ol>
<li>基本格式：</li>
</ol>
<p>if 条件1 :</p>
<p>条件1满足时要完成的事</p>
<p>条件1满足时要完成的事</p>
<p>……</p>
<p>elif 条件2::</p>
<p>条件2满足时要完成的事</p>
<p>条件2满足时要完成的事</p>
<p>……</p>
<p>elif 条件n:</p>
<p>条件n满足时要完成的事</p>
<p>条件n满足时要完成的事</p>
<p>……</p>
<p>else:</p>
<p>所有条件不满足时要完成的事</p>
<p>所有条件不满足时要完成的事</p>
<p>……</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;欢迎来到黑马动物园！！&quot;</span>)</span><br><span class="line"></span><br><span class="line">height = <span class="type">int</span>(input(<span class="string">&quot;请输入你的身高：&quot;</span>))</span><br><span class="line">vip_level = <span class="type">int</span>(input(<span class="string">&quot;请输入您的vip级别（1~5）：&quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> height &lt; <span class="number">120</span> :</span><br><span class="line">    print(<span class="string">&quot;您的身高小于120cm，可以免费游玩&quot;</span>)</span><br><span class="line"></span><br><span class="line">elif vip_level &gt; <span class="number">3</span> :</span><br><span class="line">    print(<span class="string">&quot;您的vip级别大于3，可以免费游玩&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">else</span> :</span><br><span class="line">    print(<span class="string">&quot;不好意思，所有条件均不符合，需要购票10元&quot;</span>)</span><br><span class="line"></span><br><span class="line">print(<span class="string">&quot;祝您游玩愉快&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; 欢迎来到黑马动物园！！</span><br><span class="line">&gt;&gt; 请输入你的身高：<span class="number">150</span></span><br><span class="line">&gt;&gt; 请输入您的vip级别（<span class="number">1</span>~<span class="number">5</span>）：<span class="number">5</span></span><br><span class="line">&gt;&gt; 您的vip级别大于<span class="number">3</span>，可以免费游玩</span><br><span class="line">&gt;&gt; 祝您游玩愉快</span><br></pre></td></tr></table></figure>

<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/99ae1621687545c2b63a5792d317706e.png" alt="img"></p>
<h2 id="（五）-判断语句的嵌套"><a href="#（五）-判断语句的嵌套" class="headerlink" title="（五） 判断语句的嵌套"></a>（五） 判断语句的嵌套</h2><ol>
<li>基本格式</li>
</ol>
<p>if 条件1 ：</p>
<p>满足条件1时要完成的事1</p>
<p>满足条件1时要完成的事2</p>
<p>……</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;if条件2：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 满足条件2做的事情1</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 满足条件2做的事情2</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;……</p>
<ol start="2">
<li>嵌套的关键点：空格缩进</li>
</ol>
<p>&amp;nbsp;&amp;nbsp; 通过空格缩进，来决定语句之间的：层次关系</p>
<h2 id="（六）-实战案例"><a href="#（六）-实战案例" class="headerlink" title="（六） 实战案例"></a>（六） 实战案例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="type">random</span></span><br><span class="line"><span class="variable">num</span> <span class="operator">=</span> random.randint(<span class="number">1</span>,<span class="number">10</span>)</span><br><span class="line"></span><br><span class="line">m = <span class="type">int</span>(input(<span class="string">&quot;第一次才得数字：&quot;</span>))</span><br><span class="line"><span class="keyword">if</span> num == m :</span><br><span class="line">    print (<span class="string">&quot;猜对啦！&quot;</span>)</span><br><span class="line"><span class="keyword">else</span> :</span><br><span class="line">    <span class="keyword">if</span> num &gt; m:</span><br><span class="line">        print(<span class="string">&quot;猜小了！&quot;</span>)</span><br><span class="line">    <span class="keyword">else</span> :</span><br><span class="line">        print(<span class="string">&quot;猜大了！&quot;</span>)</span><br><span class="line"></span><br><span class="line">    m2 = <span class="type">int</span>(input(<span class="string">&quot;第二次猜测的数字：&quot;</span>))</span><br><span class="line">    <span class="keyword">if</span> m2 == num:</span><br><span class="line">        print(<span class="string">&quot;第二次猜对了！&quot;</span>)</span><br><span class="line">    elif num &gt; m2:</span><br><span class="line">        print(<span class="string">&quot;第二次猜小了！&quot;</span>)</span><br><span class="line">    elif num &lt; m2:</span><br><span class="line">        print(<span class="string">&quot;第二次猜大了&quot;</span>)</span><br><span class="line">    guess_num = <span class="type">int</span>(input(<span class="string">&quot;第三次输入你要猜测的数字：&quot;</span>))</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> guess_num == num:</span><br><span class="line">        print(<span class="string">&quot;第三次猜中了&quot;</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(<span class="string">&quot;三次机会用完了，没有猜中。&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="（一）-while循环的基本语法"><a href="#（一）-while循环的基本语法" class="headerlink" title="（一） while循环的基本语法"></a>（一） while循环的基本语法</h2><p>1.&amp;nbsp; 基本格式</p>
<p>while 条件：</p>
<p>条件满足时，做的事情1</p>
<p>条件满足时，做的事情2</p>
<p>…（省略）…</p>
<ul>
<li>只要条件满足，会无限循环执行 </li>
<li>条件需要布尔类型结果</li>
</ul>
<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/abca4452420d4be4945468c868e20228.png" alt="img"></p>
<h2 id="（二）-while循环的基础案例"><a href="#（二）-while循环的基础案例" class="headerlink" title="（二） while循环的基础案例"></a>（二） while循环的基础案例</h2><p>&amp;nbsp;<img src="https://img-blog.csdnimg.cn/109a728803fc4a1d9609bed9eadeb99d.png" alt="img"></p>
<h2 id="（三）-while循环的嵌套应用"><a href="#（三）-while循环的嵌套应用" class="headerlink" title="（三） while循环的嵌套应用"></a>（三） while循环的嵌套应用</h2><p>&amp;nbsp;1. 基本格式：</p>
<p>while 条件1：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;条件1满足时，做的事情1</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;条件1满足时，做的事情2</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;…（省略）…</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;while 条件2：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;条件2满足时，做的事情1</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;条件2满足时，做的事情2</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;…（省略）…</p>
<h2 id="（四）-while循环的嵌套应用"><a href="#（四）-while循环的嵌套应用" class="headerlink" title="（四） while循环的嵌套应用"></a>（四） while循环的嵌套应用</h2><p>补充：</p>
<ol>
<li>print输出不换行</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;Hello&quot;</span>,end=<span class="string">&#x27; &#x27;</span>)</span><br><span class="line">print(<span class="string">&quot;World!&quot;</span>,end=<span class="string">&#x27;&#x27;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&gt;&gt; Hello World!</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>制表符&#x2F;t</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;学号\t姓名\t语文\t数学\t英语&quot;</span>)</span><br><span class="line">print(<span class="string">&quot;2022001\t小王\t99\t\t88\t\t0&quot;</span>)</span><br><span class="line">print(<span class="string">&quot;2022002\t小周\t92\t\t45\t\t93&quot;</span>)</span><br><span class="line">print(<span class="string">&quot;2022008\t小黄\t77\t\t82\t\t100&quot;</span>)</span><br></pre></td></tr></table></figure>

<p><img src="https://img-blog.csdnimg.cn/788bb66ebe264cd19c8417b05dd0cdd0.png" alt="img"></p>
<h2 id="（五）-for循环的基础语法"><a href="#（五）-for循环的基础语法" class="headerlink" title="（五） for循环的基础语法"></a>（五） for循环的基础语法</h2><ol>
<li>基础语法</li>
</ol>
<p>for循环就是将“待办事项”逐个完成的循环机制</p>
<ul>
<li>while循环的循环条件是自定义，自行控制循环条件 </li>
<li>for循环是一种“轮询”机制，是对一批内容进行逐个处理</li>
</ul>
<p>！for循环无法定义循环条件，只能从被处理的数据集中，依次取出内容进行处理</p>
<p>1） 基本格式：</p>
<p>for 临时变量 in 待处理的数据集：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 循环满足条件时执行的语句</p>
<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/f544c630f57f42faae61d801c2fc5b52.png" alt="img"></p>
<ol start="2">
<li>range语句</li>
</ol>
<p>1） 功能：获得一个数字序列（可迭代类型的一种）</p>
<p>2） 语法格式：</p>
<blockquote>
<p>range（num）</p>
</blockquote>
<p>获取一个从0开始，到num结束的数字序列</p>
<blockquote>
<p>range（num1，num2）</p>
</blockquote>
<p>获取一个从num1开始，到num2结束的数字序列（不含num2本身）</p>
<blockquote>
<p>range（num1，num2，step）</p>
</blockquote>
<p>&amp;nbsp;获得一个从num1开始，到num2结束的数字序列（不含num2本身）</p>
<p>数字之间的步长，以step为准（step默认为1）</p>
<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/c9eae398caca4b388d980bba69a5403a.png" alt="img"></p>
<ol start="3">
<li>变量作用域</li>
</ol>
<h2 id="（六）for循环的嵌套应用"><a href="#（六）for循环的嵌套应用" class="headerlink" title="（六）for循环的嵌套应用"></a>（六）for循环的嵌套应用</h2><ol>
<li>基本格式：</li>
</ol>
<p>for 临时变量 in 待处理的数据集：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 循环满足条件时执行的语句1</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 循环满足条件时执行的语句2</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; ……</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; for 临时变量 in 待处理的数据集：</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 循环满足条件时执行的语句1</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 循环满足条件时执行的语句1&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;</p>
<p>$练习：</p>
<p><img src="https://img-blog.csdnimg.cn/addae357bbd9419ca8f2f75405c7cdd0.png" alt="img"></p>
<h2 id="（七）-循环中断：break和continue"><a href="#（七）-循环中断：break和continue" class="headerlink" title="（七） 循环中断：break和continue"></a>（七） 循环中断：break和continue</h2><ol>
<li><p>continue关键字作用：中断本次循环，直接进入下一次循环【for循环和while循环即可使用】</p>
</li>
<li><p>break关键字作用：所在循环永久中断【for循环和while循环即可使用】</p>
</li>
</ol>
<h2 id="（八）-综合案例"><a href="#（八）-综合案例" class="headerlink" title="（八） 综合案例"></a>（八） 综合案例</h2><p><img src="https://img-blog.csdnimg.cn/dd7198185ed542aeaf91d44b64a0948e.png" alt="img"></p>
<h2 id="（一）-函数介绍"><a href="#（一）-函数介绍" class="headerlink" title="（一） 函数介绍"></a>（一） 函数介绍</h2><ol>
<li>函数：是组织好的，可重复使用的，用来实现特定功能的代码段。</li>
</ol>
<p>eg. len()：实现统计长度这一特定功能的代码段。</p>
<ol start="2">
<li>函数好处：</li>
</ol>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; * 将功能封装在函数内，可随时随地重复利用</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; * 提高代码的复用性，减少重复代码，提高开发效率</p>
<h2 id="（二）-函数定义"><a href="#（二）-函数定义" class="headerlink" title="（二） 函数定义"></a>（二） 函数定义</h2><ol>
<li>基本语法：</li>
</ol>
<p>（1） 函数定义</p>
<p>def 函数名 （传入函数）：</p>
<p>函数体</p>
<p>return 返回值</p>
<p>（2） 函数调用</p>
<p>函数名（参数）</p>
<p>！参数可省略</p>
<p>！返回值可省略</p>
<p>！函数必须先定义后使用</p>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">printWelcome</span> <span class="params">()</span>:</span><br><span class="line">    print(<span class="string">&quot;欢迎来到黑马程序员！&quot;</span>)</span><br><span class="line">    print(<span class="string">&quot;请出示48小时核酸检测证明！&quot;</span>)</span><br><span class="line"></span><br><span class="line">printWelcome()</span><br></pre></td></tr></table></figure>

<h2 id="（三）-函数参数"><a href="#（三）-函数参数" class="headerlink" title="（三） 函数参数"></a>（三） 函数参数</h2><ol>
<li>传入参数的功能：在函数调用时，接受外部提供的数据</li>
</ol>
<p>！参数之间按顺序传入；使用，进行分隔</p>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">printWelcome</span><span class="params">(x)</span>:</span><br><span class="line">    print(<span class="string">&quot;欢迎来到黑马程序员！请出示48小时核酸检测证明！&quot;</span>)</span><br><span class="line">    <span class="keyword">if</span> x &lt;= <span class="number">37.5</span>:</span><br><span class="line">        print(f<span class="string">&quot;体温测量中，您的体温是：&#123;x&#125;，体温正常，请进！&quot;</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        print(f<span class="string">&quot;体温测量中，您的体温是&#123;x&#125;，需要隔离！&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">y = <span class="type">int</span>(input(<span class="string">&quot;请输入您的体温：&quot;</span>))</span><br><span class="line">printWelcome(y)</span><br></pre></td></tr></table></figure>

<h2 id="（四）-函数返回值"><a href="#（四）-函数返回值" class="headerlink" title="（四） 函数返回值"></a>（四） 函数返回值</h2><ol>
<li>通过return关键字，可向调用者返回数据</li>
</ol>
<p>2.. None类型：</p>
<p>（1） None，其类型是&lt;class ’NoneType’&gt;</p>
<p>（2） 无返回值的函数，实际上是返回了None这个字面量</p>
<p>（3） 使用：</p>
<ul>
<li>用于函数返回值中【空】 </li>
<li>用于if判断【None等同于False】 </li>
<li>用于声明无内容的变量上</li>
</ul>
<h2 id="（五）-函数说明文档"><a href="#（五）-函数说明文档" class="headerlink" title="（五） 函数说明文档"></a>（五） 函数说明文档</h2><ol>
<li>函数说明文档：对函数进行说明解释，帮助更好地理解函数</li>
</ol>
<p>2.. 定义语法：</p>
<p>def func（x，y）</p>
<p>“””</p>
<p>函数说明</p>
<p>：param x：参数x的说明</p>
<p>：param x：参数x的说明</p>
<p>：return：返回值的说明</p>
<p>“””</p>
<p>函数体</p>
<p>return 返回值</p>
<h2 id="（六）-函数嵌套使用"><a href="#（六）-函数嵌套使用" class="headerlink" title="（六） 函数嵌套使用"></a>（六） 函数嵌套使用</h2><ol>
<li><p>函数嵌套：在一个函数中，调用另一个函数。</p>
</li>
<li><p>执行流程：</p>
</li>
</ol>
<p>函数a中执行到调用函数b的语句，会将b全部执行；完成后，继续执行函数a的剩余内容</p>
<h2 id="（七）-变量的作用域"><a href="#（七）-变量的作用域" class="headerlink" title="（七） 变量的作用域"></a>（七） 变量的作用域</h2><ol>
<li>变量作用域：变量的作用范围</li>
</ol>
<p>2.. 局部变量：定义在函数体内部的变量，只在函数体内部生效</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp; 局部变量作用：在函数体内部，临时保存数据，即当函数调用完成后，销毁局部变量</p>
<ol start="3">
<li><p>全局变量：在函数体内外都能生效的变量</p>
</li>
<li><p>global关键字：可使函数内部声明变量为全局变量</p>
</li>
</ol>
<h2 id="（八）-综合案例-1"><a href="#（八）-综合案例-1" class="headerlink" title="（八） 综合案例"></a>（八） 综合案例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">money = <span class="number">5000000</span></span><br><span class="line">name = None</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def <span class="title function_">menu</span><span class="params">()</span>:</span><br><span class="line">    name = input(<span class="string">&quot;您好！请输入客户姓名：&quot;</span>)</span><br><span class="line">    print(<span class="string">&quot;您好，请问您需要什么服务呢？&quot;</span>)</span><br><span class="line">    print(<span class="string">&quot;输入1：查询余额&quot;</span>)</span><br><span class="line">    print(<span class="string">&quot;输入2：存款&quot;</span>)</span><br><span class="line">    print(<span class="string">&quot;输入3：取款&quot;</span>)</span><br><span class="line">    n = <span class="type">int</span>(input(<span class="string">&quot;请输入功能号：&quot;</span>))</span><br><span class="line">    <span class="keyword">if</span> n == <span class="number">1</span>:</span><br><span class="line">        check(money)</span><br><span class="line">    elif n == <span class="number">2</span>:</span><br><span class="line">        x = <span class="type">int</span>(input(<span class="string">&quot;请输入您想存入的金额：&quot;</span>))</span><br><span class="line">        deposit(x, money)</span><br><span class="line">    elif n == <span class="number">3</span>:</span><br><span class="line">        y = <span class="type">int</span>(input(<span class="string">&quot;请输入您想取出的金额：&quot;</span>))</span><br><span class="line">        withdraw(y, money)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def <span class="title function_">check</span><span class="params">(money)</span>:</span><br><span class="line">    print(f<span class="string">&quot;您好！您的余额为：&#123;money&#125;&quot;</span>)</span><br><span class="line">    menu()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def <span class="title function_">deposit</span><span class="params">(x, money)</span>:</span><br><span class="line">    money1 = x + money</span><br><span class="line">    <span class="title function_">print</span><span class="params">(f<span class="string">&quot;您好，存款成功后，您的余额为：&#123;money1&#125;&quot;</span>)</span></span><br><span class="line">    menu()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">def <span class="title function_">withdraw</span><span class="params">(x, money)</span>:</span><br><span class="line">    money2 = money - x</span><br><span class="line">    <span class="title function_">print</span><span class="params">(f<span class="string">&quot;您好，取款成功后，您的余额为：&#123;money2&#125;&quot;</span>)</span></span><br><span class="line">    menu()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">menu()</span><br></pre></td></tr></table></figure>

<h2 id="（一）-数据容器入门"><a href="#（一）-数据容器入门" class="headerlink" title="（一） 数据容器入门"></a>（一） 数据容器入门</h2><ol>
<li>数据容器：一种可以容纳多份数据的数据类型，容纳的每一份数据称之为一个元素。每一个元素，可以是任意类型的数据，如字符串、数字、布尔等。</li>
</ol>
<p>2.. 种类：list（列表）、tuple（元组）、str（字符串）、set（集合）、dict（字典）</p>
<h2 id="（二）-数据容器：list（列表）"><a href="#（二）-数据容器：list（列表）" class="headerlink" title="（二） 数据容器：list（列表）"></a>（二） 数据容器：list（列表）</h2><h3 id="1-定义"><a href="#1-定义" class="headerlink" title="1. 定义"></a>1. 定义</h3><p>（1） 基本语法：</p>
<h1 id="字面量"><a href="#字面量" class="headerlink" title="字面量"></a>字面量</h1><p>[元素1 ，元素2 ，元素3，……]</p>
<h1 id="定义变量"><a href="#定义变量" class="headerlink" title="定义变量"></a>定义变量</h1><p>变量名称 &#x3D; [元素1 ，元素2 ，元素3，……]</p>
<h1 id="定义空列表"><a href="#定义空列表" class="headerlink" title="定义空列表"></a>定义空列表</h1><p>变量名称 &#x3D; []</p>
<p>变量名称 &#x3D; list（）</p>
<p>（2） 元素：列表中的每个数据</p>
<p>！ 列表可以一次存储多个数据，且可以为不同类型，支持嵌套</p>
<h3 id="2-列表的下标索引"><a href="#2-列表的下标索引" class="headerlink" title="2. 列表的下标索引"></a>2. 列表的下标索引</h3><p>（1） 类型：正向：从前向后，从0开始，依次递增</p>
<p>&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 反向：从后向前，从-1开始，依次递减</p>
<p>（2） 使用：</p>
<p>列表[下标]</p>
<p>！ 下标超出范围，则无法取出元素，并且会报错</p>
<h3 id="3-列表的常用操作（方法）"><a href="#3-列表的常用操作（方法）" class="headerlink" title="3. 列表的常用操作（方法）"></a>3. 列表的常用操作（方法）</h3><ul>
<li>函数：封装的代码单元，可提供特定功能。</li>
</ul>
<p>如将函数定义为class（类）的成员，则称函数为方法。</p>
<h3 id="4-列表的特点"><a href="#4-列表的特点" class="headerlink" title="4. 列表的特点"></a>4. 列表的特点</h3><ul>
<li>可容纳多个元素 </li>
<li>可容纳不同类型的元素 </li>
<li>数据是有序存储的 </li>
<li>允许重复数据存在 </li>
<li>可进行增删查改</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">age = [<span class="number">21</span>, <span class="number">25</span>, <span class="number">21</span>, <span class="number">23</span>, <span class="number">22</span>, <span class="number">20</span>]</span><br><span class="line"></span><br><span class="line">age.append(<span class="number">31</span>)</span><br><span class="line">print(age)</span><br><span class="line"></span><br><span class="line">age.extend([<span class="number">29</span>, <span class="number">33</span>, <span class="number">30</span>])</span><br><span class="line">print(age)</span><br><span class="line"></span><br><span class="line">x = age[<span class="number">0</span>]</span><br><span class="line">y = age[-<span class="number">1</span>]</span><br><span class="line"></span><br><span class="line">print(x, y)</span><br><span class="line"></span><br><span class="line">a = age.index(<span class="number">31</span>)</span><br><span class="line">print(a)</span><br></pre></td></tr></table></figure>

<h2 id="（三）-list（列表）的遍历"><a href="#（三）-list（列表）的遍历" class="headerlink" title="（三） list（列表）的遍历"></a>（三） list（列表）的遍历</h2><ul>
<li>遍历：将容器内的元素依次取出，并处理</li>
</ul>
<h3 id="1-使用while遍历"><a href="#1-使用while遍历" class="headerlink" title="1. 使用while遍历"></a>1. 使用while遍历</h3><ul>
<li>可以指定循环条件，自行控制 </li>
<li>可以通过条件控制做到无限循环 </li>
<li>适用于任何想要循环的场景</li>
</ul>
<h3 id="2-使用for遍历"><a href="#2-使用for遍历" class="headerlink" title="2. 使用for遍历"></a>2. 使用for遍历</h3><ul>
<li>不可以指定循环条件，只可以一个个从容器内取出数据 </li>
<li>理论上不可以无限循环，因为被遍历的容器容量不是无限的 </li>
<li>适用于遍历数据容器的场景或简单的固定次数循环场景</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">L = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>, <span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>, <span class="number">10</span>]</span><br><span class="line">l1 = list()</span><br><span class="line">l2 = list()</span><br><span class="line">i = <span class="number">0</span></span><br><span class="line"></span><br><span class="line"># 取出偶数</span><br><span class="line"><span class="keyword">while</span> i &lt; len(L):</span><br><span class="line">    <span class="keyword">if</span> L[i] % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">        l1 . append(L[i])</span><br><span class="line">        i = i + <span class="number">1</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        i = i + <span class="number">1</span></span><br><span class="line">print(f<span class="string">&quot;通过while循环，从列表：&#123;L&#125;，中取出偶数，组成新列表&#123;l1&#125;&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> j in L:</span><br><span class="line">    <span class="keyword">if</span> j % <span class="number">2</span> == <span class="number">0</span>:</span><br><span class="line">        l2 . append(j)</span><br><span class="line">print(f<span class="string">&quot;通过for循环，从列表：&#123;L&#125;，中取出偶数，组成新列表&#123;l2&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="（四）-数据容器：tuple（元组）"><a href="#（四）-数据容器：tuple（元组）" class="headerlink" title="（四） 数据容器：tuple（元组）"></a>（四） 数据容器：tuple（元组）</h2><ul>
<li>元组一旦定义，不可进行修改</li>
</ul>
<h3 id="1-元组的定义"><a href="#1-元组的定义" class="headerlink" title="1. 元组的定义"></a>1. 元组的定义</h3><p>（1） 基本语法：</p>
<h1 id="字面量-1"><a href="#字面量-1" class="headerlink" title="字面量"></a>字面量</h1><p>（元素1 ，元素2 ，元素3，……）</p>
<h1 id="定义变量-1"><a href="#定义变量-1" class="headerlink" title="定义变量"></a>定义变量</h1><p>变量名称 &#x3D;（元素1 ，元素2 ，元素3，……）</p>
<h1 id="定义空列表-1"><a href="#定义空列表-1" class="headerlink" title="定义空列表"></a>定义空列表</h1><p>变量名称 &#x3D; （）</p>
<p>变量名称 &#x3D;tuple（）</p>
<p>！ 元组即使只含有一个数据，这个数据后也要添加，也可进行嵌套</p>
<h3 id="2-元组的操作（方法）"><a href="#2-元组的操作（方法）" class="headerlink" title="2. 元组的操作（方法）"></a>2. 元组的操作（方法）</h3><p>！ 不可修改元组内容，否则直接报错</p>
<p>！ 可修改元组内list的内容</p>
<p>！ 不可以替换元组内list为其他list或其他类型</p>
<h3 id="3-元组的特点"><a href="#3-元组的特点" class="headerlink" title="3. 元组的特点"></a>3. 元组的特点</h3><ul>
<li>可容纳多个元素 </li>
<li>可容纳不同类型的元素 </li>
<li>数据是有序存储的 </li>
<li>允许重复数据存在 </li>
<li>可进行增删查改 </li>
<li>可进行循环 </li>
<li>不可修改</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">T = (<span class="string">&#x27;周杰伦&#x27;</span>, <span class="number">11</span>, [<span class="string">&#x27;football&#x27;</span>, <span class="string">&#x27;music&#x27;</span>])</span><br><span class="line"></span><br><span class="line">a = T.index(<span class="number">11</span>)</span><br><span class="line">n = T[<span class="number">0</span>]</span><br><span class="line">print(a,n)</span><br><span class="line"></span><br><span class="line">del T[<span class="number">2</span>][<span class="number">0</span>]</span><br><span class="line">T[<span class="number">2</span>].append(<span class="string">&#x27;coding&#x27;</span>)</span><br><span class="line"></span><br><span class="line">print(T)</span><br></pre></td></tr></table></figure>

<h2 id="（五）-数据容器：str（字符串）"><a href="#（五）-数据容器：str（字符串）" class="headerlink" title="（五） 数据容器：str（字符串）"></a>（五） 数据容器：str（字符串）</h2><h3 id="1-字符串"><a href="#1-字符串" class="headerlink" title="1. 字符串"></a>1. 字符串</h3><p>字符的容器，一个字符串可以存放任意数量的字符。</p>
<h3 id="2-字符串的下标（索引）"><a href="#2-字符串的下标（索引）" class="headerlink" title="2. 字符串的下标（索引）"></a>2. 字符串的下标（索引）</h3><ul>
<li>从前向后，下标从0开始 </li>
<li>从后向前，下标从-1开始</li>
</ul>
<p>！ 字符串是一个无法修改的数据容器</p>
<p>！ 如果要修改，则会得到一个新的字符串，旧的字符串是无法修改的</p>
<h3 id="3-字符串的常用操作"><a href="#3-字符串的常用操作" class="headerlink" title="3. 字符串的常用操作"></a>3. 字符串的常用操作</h3><h3 id="4-字符串的特点"><a href="#4-字符串的特点" class="headerlink" title="4. 字符串的特点"></a>4. 字符串的特点</h3><ul>
<li>只可以存储字符串 </li>
<li>长度任意 </li>
<li>支持下标索引 </li>
<li>允许重复的字符串出现 </li>
<li>不可以修改 </li>
<li>支持循环</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">S = <span class="string">&quot;itheima itcast boxuegu&quot;</span></span><br><span class="line"></span><br><span class="line">n = S.count(<span class="string">&quot;it&quot;</span>)</span><br><span class="line">print(f<span class="string">&quot;字符串&#123;S&#125;中有&#123;n&#125;个it字符&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">S1 = S.replace(<span class="string">&quot; &quot;</span>, <span class="string">&quot;|&quot;</span>)</span><br><span class="line">print(f<span class="string">&quot;字符串&#123;S&#125;，被替换空格后，结果&#123;S1&#125;&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">L = S1.split(<span class="string">&quot;|&quot;</span>)</span><br><span class="line">print(f<span class="string">&quot;字符串&#123;S&#125;，按照“|”分割后，得到&#123;L&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="（六）-数据容器的切片"><a href="#（六）-数据容器的切片" class="headerlink" title="（六） 数据容器的切片"></a>（六） 数据容器的切片</h2><h3 id="1-序列"><a href="#1-序列" class="headerlink" title="1. 序列"></a>1. 序列</h3><p>内容连续、有序、可使用下标索引的一类数据容器。（如列表、元组、字符串等）</p>
<h3 id="2-切片"><a href="#2-切片" class="headerlink" title="2. 切片"></a>2. 切片</h3><p>（序列的常用操作）从一个序列中，取出一个子序列</p>
<h3 id="3-基本语法"><a href="#3-基本语法" class="headerlink" title="3. 基本语法"></a>3. 基本语法</h3><p>序列[起始下标：结束下标：步长]</p>
<ul>
<li><p>表示从序列中，从指定位置开始，依次取出指定元素，到指定位置结束，得到一个新序列</p>
</li>
<li><p>起始下标表示从何处开始，可以留空，留空视作从头开始</p>
</li>
<li><p>结束下标（不含）表示何处结束，可以留空，留空视作截取到结尾</p>
</li>
<li><p>步长表示，依次取元素的间隔</p>
</li>
</ul>
<ul>
<li><p>步长1表示，一个个取元素 </p>
</li>
<li><p>步长2表示，每次跳过1个元素取 </p>
</li>
<li><p>步长N表示，每次跳过N-1个元素取 </p>
</li>
<li><p>步长为负数表示，反向取（注意，起始下标和结束下标也要反向标记）</p>
</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">S = <span class="string">&quot;万过薪月，员序程马黑来，nohtyP学&quot;</span></span><br><span class="line"></span><br><span class="line"># 使用“切片”</span><br><span class="line">new_S = S[<span class="number">9</span>:<span class="number">4</span>:-<span class="number">1</span>]</span><br><span class="line">print(new_S)</span><br><span class="line"></span><br><span class="line"># 使用字符串常用操作</span><br><span class="line">i = -<span class="number">9</span></span><br><span class="line">S1 = S.replace(<span class="string">&quot;来&quot;</span>, <span class="string">&quot;&quot;</span>)</span><br><span class="line">S2 = S1.split(<span class="string">&quot;,&quot;</span>)</span><br><span class="line">print(S2)</span><br><span class="line">new_S1 = S2[<span class="number">0</span>][<span class="number">9</span>:<span class="number">4</span>:-<span class="number">1</span>]</span><br><span class="line">print(new_S1)</span><br></pre></td></tr></table></figure>

<h2 id="（七）-数据容器：set（集合）"><a href="#（七）-数据容器：set（集合）" class="headerlink" title="（七） 数据容器：set（集合）"></a>（七） 数据容器：set（集合）</h2><h3 id="1-集合的基本语法"><a href="#1-集合的基本语法" class="headerlink" title="1. 集合的基本语法"></a>1. 集合的基本语法</h3><h1 id="定义集合字面量"><a href="#定义集合字面量" class="headerlink" title="定义集合字面量"></a>定义集合字面量</h1><p>（元素，元素，……，元素）</p>
<h1 id="定义集合变量"><a href="#定义集合变量" class="headerlink" title="定义集合变量"></a>定义集合变量</h1><p>变量名称 &#x3D; {元素，元素，……，元素}</p>
<h1 id="定义空集合"><a href="#定义空集合" class="headerlink" title="定义空集合"></a>定义空集合</h1><p>变量名称 &#x3D; set（）</p>
<p><strong>！ 去重且无序，无法保证顺序与创建的时候一致</strong></p>
<p><strong>！ 不支持：下标索引访问，因此不支持while循环</strong></p>
<p>补充：</p>
<p>定义：</p>
<p>列表[]&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 可修改、支持重复元素且有序</p>
<p>元组（）&amp;nbsp;&amp;nbsp;&amp;nbsp; 不可修改、支持重复元素且有序</p>
<p>字符串“”&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 不可修改、支持重复元素且有序</p>
<p>集合{}&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp;&amp;nbsp; 可修改、不支持重复元素、无序</p>
<h3 id="2-集合的常用功能"><a href="#2-集合的常用功能" class="headerlink" title="2. 集合的常用功能"></a>2. 集合的常用功能</h3><h3 id="3-集合的特点"><a href="#3-集合的特点" class="headerlink" title="3. 集合的特点"></a>3. 集合的特点</h3><ul>
<li>可以容纳多个数据 </li>
<li>可以容纳不同类型的数据（混装） </li>
<li>数据是无序存储的（不支持下标存储） </li>
<li>不允许重复数据存在 </li>
<li>可以修改 </li>
<li>支持for循环，不支持while循环</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">my_list = [<span class="string">&#x27;黑马程序员&#x27;</span>,<span class="string">&#x27;传智播客&#x27;</span>,<span class="string">&#x27;黑马程序员&#x27;</span>,<span class="string">&#x27;传智播客&#x27;</span>,<span class="string">&#x27;itheima&#x27;</span>,<span class="string">&#x27;itcast&#x27;</span>,<span class="string">&#x27;itheima&#x27;</span>,<span class="string">&#x27;itcast&#x27;</span>,<span class="string">&#x27;best&#x27;</span>]</span><br><span class="line"></span><br><span class="line">S = set()</span><br><span class="line"><span class="keyword">for</span> i in my_list:</span><br><span class="line">    S.add(i)</span><br><span class="line"></span><br><span class="line">print(f<span class="string">&quot;有列表：&#123;my_list&#125;&quot;</span>)</span><br><span class="line">print(f<span class="string">&quot;存入集合后结果：&#123;S&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="（八）-数据容器：dict（字典、映射）"><a href="#（八）-数据容器：dict（字典、映射）" class="headerlink" title="（八） 数据容器：dict（字典、映射）"></a>（八） 数据容器：dict（字典、映射）</h2><h3 id="1-字典的基本语法"><a href="#1-字典的基本语法" class="headerlink" title="1. 字典的基本语法"></a>1. 字典的基本语法</h3><h1 id="定义字典字面量"><a href="#定义字典字面量" class="headerlink" title="定义字典字面量"></a>定义字典字面量</h1><p>{key：value，key：value，……，key：value}</p>
<h1 id="定义字面变量"><a href="#定义字面变量" class="headerlink" title="定义字面变量"></a>定义字面变量</h1><p>my_dict &#x3D; {key：value，key：value，……，key：value}</p>
<h1 id="定义空字典"><a href="#定义空字典" class="headerlink" title="定义空字典"></a>定义空字典</h1><p>my_dict &#x3D; {}</p>
<p>my_dict &#x3D; dict{}</p>
<p>！ 每个元素均为键值对，key和value可以为任意类型数据（key不可为字典）</p>
<p>！ key不可重复，重复会对原有数据覆盖</p>
<p>！ 不可以使用下标索引，但可通过key值取得对应的value，不可使用while循环</p>
<p>！ 字典可嵌套</p>
<h3 id="2-字典的常用操作"><a href="#2-字典的常用操作" class="headerlink" title="2. 字典的常用操作"></a>2. 字典的常用操作</h3><h3 id="3-字典的特点"><a href="#3-字典的特点" class="headerlink" title="3. 字典的特点"></a>3. 字典的特点</h3><ul>
<li>可以容纳多个数据 </li>
<li>可以容纳不同类型的数据（混装） </li>
<li>不支持下标索引 </li>
<li>可以修改 </li>
<li>支持for循环，不支持while循环</li>
</ul>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">D = &#123;</span><br><span class="line">    <span class="string">&quot;王力鸿&quot;</span>:&#123;<span class="string">&quot;部门&quot;</span>:<span class="string">&quot;科技部&quot;</span>,<span class="string">&quot;工资&quot;</span>:<span class="number">3000</span>,<span class="string">&quot;级别&quot;</span>:<span class="number">1</span>&#125;,</span><br><span class="line">    <span class="string">&quot;周婕纶&quot;</span>:&#123;<span class="string">&quot;部门&quot;</span>:<span class="string">&quot;市场部&quot;</span>,<span class="string">&quot;工资&quot;</span>:<span class="number">5000</span>,<span class="string">&quot;级别&quot;</span>:<span class="number">2</span>&#125;,</span><br><span class="line">    <span class="string">&quot;凌俊杰&quot;</span>:&#123;<span class="string">&quot;部门&quot;</span>:<span class="string">&quot;市场部&quot;</span>,<span class="string">&quot;工资&quot;</span>:<span class="number">7000</span>,<span class="string">&quot;级别&quot;</span>:<span class="number">3</span>&#125;,</span><br><span class="line">    <span class="string">&quot;张雪有&quot;</span>:&#123;<span class="string">&quot;部门&quot;</span>:<span class="string">&quot;科技部&quot;</span>,<span class="string">&quot;工资&quot;</span>:<span class="number">4000</span>,<span class="string">&quot;级别&quot;</span>:<span class="number">1</span>&#125;,</span><br><span class="line">    <span class="string">&quot;牛德华&quot;</span>:&#123;<span class="string">&quot;部门&quot;</span>:<span class="string">&quot;市场部&quot;</span>,<span class="string">&quot;工资&quot;</span>:<span class="number">6000</span>,<span class="string">&quot;级别&quot;</span>:<span class="number">2</span>&#125;</span><br><span class="line">&#125;</span><br><span class="line">keys = D.keys()</span><br><span class="line"></span><br><span class="line">print(D)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key in keys:</span><br><span class="line">    <span class="keyword">if</span> D[key][<span class="string">&quot;级别&quot;</span>] == <span class="number">1</span>:</span><br><span class="line">        D[key][<span class="string">&quot;工资&quot;</span>] = D[key][<span class="string">&quot;工资&quot;</span>] + <span class="number">1000</span></span><br><span class="line"></span><br><span class="line">print(D)</span><br></pre></td></tr></table></figure>

<h3 id="扩展：数据容器的对比总结"><a href="#扩展：数据容器的对比总结" class="headerlink" title="扩展：数据容器的对比总结"></a>扩展：数据容器的对比总结</h3><h2 id="（九）-数据容器的通用操作"><a href="#（九）-数据容器的通用操作" class="headerlink" title="（九） 数据容器的通用操作"></a>（九） 数据容器的通用操作</h2><h2 id="（十）综合案例"><a href="#（十）综合案例" class="headerlink" title="（十）综合案例"></a>（十）综合案例</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 幸运数字<span class="number">6</span>：</span><br><span class="line"># <span class="number">1.</span> 输入任意数字，如数字<span class="number">8</span>，生成nums列表，元素值为<span class="number">1</span>-<span class="number">8</span>，</span><br><span class="line">#  <span class="number">2.</span> 从中选取幸运数字（能被<span class="number">6</span>整除）移动到新列表lucky</span><br><span class="line"># <span class="number">3.</span> 打印nums和<span class="type">lucky</span></span><br><span class="line"></span><br><span class="line"><span class="variable">n</span> <span class="operator">=</span> <span class="type">int</span>(input(<span class="string">&quot;请输入您所想的数字：&quot;</span>))</span><br><span class="line">i = <span class="number">1</span></span><br><span class="line">j = <span class="number">0</span></span><br><span class="line">nums = list()</span><br><span class="line">lucky = list()</span><br><span class="line">print(n)</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> i &lt;= n:</span><br><span class="line">    nums.append(i)</span><br><span class="line">    i = i + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">print(f<span class="string">&quot;nums列表为：&#123;nums&#125;&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> j in nums:</span><br><span class="line">    <span class="keyword">if</span> j % <span class="number">6</span> == <span class="number">0</span>:</span><br><span class="line">        lucky.append(j)</span><br><span class="line">    j = j + <span class="number">1</span></span><br><span class="line"></span><br><span class="line">print(f<span class="string">&quot;lucky列表为：&#123;lucky&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 列表嵌套：</span><br><span class="line"># <span class="number">3</span>个教室[[],[],[]]</span><br><span class="line"># <span class="number">8</span>名讲师[<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;B&#x27;</span>,<span class="string">&#x27;C&#x27;</span>,<span class="string">&#x27;D&#x27;</span>,<span class="string">&#x27;E&#x27;</span>,<span class="string">&#x27;F&#x27;</span>,<span class="string">&#x27;G&#x27;</span>,<span class="string">&#x27;H&#x27;</span>]</span><br><span class="line"># 随机分配</span><br><span class="line"><span class="keyword">import</span> <span class="type">random</span></span><br><span class="line"></span><br><span class="line"><span class="variable">classroom</span> <span class="operator">=</span> [[],[],[]]</span><br><span class="line">teacher = [<span class="string">&#x27;A&#x27;</span>,<span class="string">&#x27;B&#x27;</span>,<span class="string">&#x27;C&#x27;</span>,<span class="string">&#x27;D&#x27;</span>,<span class="string">&#x27;E&#x27;</span>,<span class="string">&#x27;F&#x27;</span>,<span class="string">&#x27;G&#x27;</span>,<span class="string">&#x27;H&#x27;</span>]</span><br><span class="line"></span><br><span class="line">j = random.randint(<span class="number">0</span>,<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i in teacher:</span><br><span class="line">    classroom[j].append(i)</span><br><span class="line">    j = random.randint(<span class="number">0</span>,<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">print(f<span class="string">&quot;分配后为：&#123;classroom&#125;&quot;</span>)</span><br></pre></td></tr></table></figure>

<h2 id="（一）-函数多返回值"><a href="#（一）-函数多返回值" class="headerlink" title="（一） 函数多返回值"></a>（一） 函数多返回值</h2><ul>
<li>按照返回值的顺序，写对顺序的多个变量接收即可 </li>
<li>变量之间用逗号隔开 </li>
<li>支持不同类型的数据return</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">test_return</span><span class="params">()</span>:</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>,<span class="number">2</span></span><br><span class="line"></span><br><span class="line">x,y = test_return()</span><br><span class="line">print(x)</span><br><span class="line">print(y)</span><br><span class="line"></span><br><span class="line">&gt;&gt;<span class="number">1</span></span><br><span class="line">&gt;&gt;<span class="number">2</span></span><br></pre></td></tr></table></figure>

<h2 id="（二）-函数的多种传参方式"><a href="#（二）-函数的多种传参方式" class="headerlink" title="（二） 函数的多种传参方式"></a>（二） 函数的多种传参方式</h2><p>函数参数种类：根据使用方式不同：分为以下4种</p>
<h3 id="1-位置参数"><a href="#1-位置参数" class="headerlink" title="1. 位置参数"></a>1. 位置参数</h3><ul>
<li>根据函数定义的参数位置来传递参数</li>
</ul>
<p>！ 传递的参数和定义的参数的顺序及个数必须一致</p>
<h3 id="2-关键字参数"><a href="#2-关键字参数" class="headerlink" title="2. 关键字参数"></a>2. 关键字参数</h3><ul>
<li>函数调用时通过键值对的形式传递参数</li>
</ul>
<p>！ 函数调用时，如果有位置参数时，位置参数必须在关键字参数的前面，但关键字参数之间不存在先后顺序</p>
<h3 id="3-缺省参数"><a href="#3-缺省参数" class="headerlink" title="3. 缺省参数"></a>3. 缺省参数</h3><ul>
<li>也称默认参数，用于定义函数，为函数提供默认值，调用函数时可不传该默认参数的值</li>
</ul>
<p>！ 所有位置参数必须出现在默认参数前，包括函数定义和调用</p>
<p>！ 函数调用时，如果为缺省参数传值则修改默认参数值，否则使用这个默认值</p>
<h3 id="4-不定长参数"><a href="#4-不定长参数" class="headerlink" title="4. 不定长参数"></a>4. 不定长参数</h3><ul>
<li>也称可变参数，用于不确定调用的时候会传递多少个参数（不传参也可以）的场景。</li>
</ul>
<p>！ 当调用函数时不确定参数个数时，可以使用不定长参数。</p>
<p>（1） 位置传递</p>
<ul>
<li>传进的所有参数都会被args变量收集，他会根据传进参数的位置合并为一个元组（tuple），args是元组类型，这就是位置传递</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">user</span><span class="params">(*args)</span>:</span><br><span class="line">    print(args)</span><br><span class="line"></span><br><span class="line">user(<span class="string">&#x27;Belly&#x27;</span>,<span class="number">20</span>,<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">&gt;&gt;(<span class="string">&#x27;Belly&#x27;</span>, <span class="number">20</span>, <span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<p>（2） 关键字传递</p>
<ul>
<li>参数是键值对形式，所有键值对都会被kwargs接受，同时根据键值对组成字典</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">user</span><span class="params">(**kwargs)</span>:</span><br><span class="line">    print(kwargs)</span><br><span class="line"></span><br><span class="line">user(name = <span class="string">&#x27;Belly&#x27;</span>,age = <span class="number">20</span>,sex = <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">&gt;&gt;&#123;<span class="string">&#x27;name&#x27;</span>: <span class="string">&#x27;Belly&#x27;</span>, <span class="string">&#x27;age&#x27;</span>: <span class="number">20</span>, <span class="string">&#x27;sex&#x27;</span>: <span class="number">1</span>&#125;</span><br></pre></td></tr></table></figure>

<h2 id="（三）-匿名函数"><a href="#（三）-匿名函数" class="headerlink" title="（三） 匿名函数"></a>（三） 匿名函数</h2><h3 id="1-函数作为参数传递"><a href="#1-函数作为参数传递" class="headerlink" title="1. 函数作为参数传递"></a>1. 函数作为参数传递</h3><ul>
<li>函数本身可以作为参数，传入另一个函数中进行使用 </li>
<li>函数传入的作用在于：传入计算逻辑，而非传入数据</li>
</ul>
<h3 id="2-lambda匿名函数"><a href="#2-lambda匿名函数" class="headerlink" title="2. lambda匿名函数"></a>2. lambda匿名函数</h3><p>（1） 函数定义：</p>
<ul>
<li>def关键字：定义带有名称的函数（可以使用名称进行重复使用） </li>
<li>lambda关键字：定义匿名函数（只可临时使用一次）</li>
</ul>
<p>（2） lambda基本语法：</p>
<p>lambda 传入参数：函数体（一行代码）</p>
<p>！ 函数体只可写一行代码，无法写多行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">def <span class="title function_">test</span><span class="params">(compute)</span>:</span><br><span class="line">    result = compute(<span class="number">2</span>,<span class="number">3</span>)</span><br><span class="line">    print(result)</span><br><span class="line"></span><br><span class="line">test(lambda x,y:x + y)</span><br></pre></td></tr></table></figure>

<h2 id="（一）-文件的编码"><a href="#（一）-文件的编码" class="headerlink" title="（一） 文件的编码"></a>（一） 文件的编码</h2><h3 id="1-编码"><a href="#1-编码" class="headerlink" title="1. 编码"></a>1. 编码</h3><ul>
<li>编码是一种规则集合，记录了内容和二进制间进行相互转换的逻辑 </li>
<li>编码有许多种，最常用的是utf-8</li>
</ul>
<h3 id="2-使用编码的原因"><a href="#2-使用编码的原因" class="headerlink" title="2. 使用编码的原因"></a>2. 使用编码的原因</h3><ul>
<li>计算机只能识别二进制数，因此要将文件的内容翻译为二进制数，才能保存进计算机内 </li>
<li>同时也需要编码，将计算机保存的二进制数，反向编译回可识别的内容</li>
</ul>
<h2 id="（二）-文件的读取"><a href="#（二）-文件的读取" class="headerlink" title="（二） 文件的读取"></a>（二） 文件的读取</h2><ol>
<li>文件的基本操作步骤</li>
</ol>
<ul>
<li>打开文件 </li>
<li>读写文件 </li>
<li>关闭文件</li>
</ul>
<p>！ 可以只读，不写</p>
<ol start="2">
<li>文件的基本操作函数</li>
</ol>
<p>！ 读取文件后，要用进行关闭，否则文件一直被占用</p>
<p>$练习：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">word = open(<span class="string">&#x27;D:\Desktop\word.txt&#x27;</span>, <span class="string">&#x27;r&#x27;</span>, encoding=<span class="string">&quot;utf-8&quot;</span>)</span><br><span class="line"></span><br><span class="line">L = word.readlines()</span><br><span class="line">num = <span class="number">0</span></span><br><span class="line"></span><br><span class="line">print(L)</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i in L:</span><br><span class="line">    num = num + i.count(<span class="string">&quot;itheima&quot;</span>)</span><br><span class="line"></span><br><span class="line">print(num)</span><br></pre></td></tr></table></figure>

<h2 id="（三）-文件的写入"><a href="#（三）-文件的写入" class="headerlink" title="（三） 文件的写入"></a>（三） 文件的写入</h2><ol>
<li>写操作快速入门：</li>
</ol>
<p>（1） 使用open的“w”模式进行写入</p>
<p>（2） 文件写入：f.write（）</p>
<p>（3） 内容刷新：f.flush（）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 打开文件</span><br><span class="line">f = open(<span class="string">&#x27;D:\Desktop\word.txt&#x27;</span>,<span class="string">&#x27;w&#x27;</span>)</span><br><span class="line"></span><br><span class="line"># 写入文件</span><br><span class="line">f.write(<span class="string">&#x27;hello world!&#x27;</span>)</span><br><span class="line"></span><br><span class="line"># 内容刷新</span><br><span class="line">f.flush()</span><br></pre></td></tr></table></figure>

<p><strong>！ 直接调用write，内容并未真正写入文件，而是积攒在程序的内存中，称为缓冲区</strong></p>
<p><strong>！ 当调用flush时，文件才会真正写入文件</strong></p>
<p><strong>！ 这样避免频繁的操作硬盘，导致效率下降</strong></p>
<ol start="2">
<li>注意事项：</li>
</ol>
<ul>
<li>w模式，文件不存在，会创建新文件 </li>
<li>w模式，文件存在，会清空原有内容 </li>
<li>close方法，具有flush方法的作用</li>
</ul>
<h2 id="（四）-文件的追加"><a href="#（四）-文件的追加" class="headerlink" title="（四） 文件的追加"></a>（四） 文件的追加</h2><ol>
<li>追加写入操作的快速入门</li>
</ol>
<p>（1） 使用open的“a”模式进行写入</p>
<p>（2） 文件写入：f.write（）</p>
<p>（3） 内容刷新：f.flush（）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"># 打开文件</span><br><span class="line">f = open(<span class="string">&#x27;D:\Desktop\word.txt&#x27;</span>,<span class="string">&#x27;a&#x27;</span>)</span><br><span class="line"></span><br><span class="line"># 文件写入</span><br><span class="line">f.write(<span class="string">&#x27;I like pyhton!&#x27;</span>)</span><br><span class="line"></span><br><span class="line"># 内容刷新</span><br><span class="line">f.flush()</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>注意事项</li>
</ol>
<ul>
<li>a模式，文件不存在，不会创建文件 </li>
<li>a模式，文件存在，会在最后追加写入文件</li>
</ul>
<h2 id="（五）-综合案例"><a href="#（五）-综合案例" class="headerlink" title="（五） 综合案例"></a>（五） 综合案例</h2><h2 id="（一）-了解异常"><a href="#（一）-了解异常" class="headerlink" title="（一） 了解异常"></a>（一） 了解异常</h2><h3 id="1-异常"><a href="#1-异常" class="headerlink" title="1. 异常"></a>1. 异常</h3><ul>
<li>程序运行的过程中出现了错误</li>
</ul>
<h3 id="2-bug"><a href="#2-bug" class="headerlink" title="2. bug"></a>2. bug</h3><ul>
<li>bug就是指异常的意思，因为历史是因为小虫子导致计算机失灵的案例，所以延续至今，bug就代表软件出现错误</li>
</ul>
<h2 id="（二）-异常的捕获方法"><a href="#（二）-异常的捕获方法" class="headerlink" title="（二） 异常的捕获方法"></a>（二） 异常的捕获方法</h2><h3 id="1-程序出现bug后的情况"><a href="#1-程序出现bug后的情况" class="headerlink" title="1. 程序出现bug后的情况"></a>1. 程序出现bug后的情况</h3><ul>
<li>程序因为一个bug停止运行 </li>
<li>对bug进行提醒·，整个程序继续运行</li>
</ul>
<h3 id="2-为什么要捕获异常？"><a href="#2-为什么要捕获异常？" class="headerlink" title="2. 为什么要捕获异常？"></a>2. 为什么要捕获异常？</h3><ul>
<li>在可能发生异常的方法，进行捕获，当出现异常时，提供解决方式，而不是任由其导致程序无法运行 </li>
<li>作用：提前假设某处会出现异常，做好提前准备，当真的出现异常的时候，可以有后续手段</li>
</ul>
<h2 id="（三）-异常综合案例"><a href="#（三）-异常综合案例" class="headerlink" title="（三） 异常综合案例"></a>（三） 异常综合案例</h2><h2 id="（四）-Pyhton模块"><a href="#（四）-Pyhton模块" class="headerlink" title="（四） Pyhton模块"></a>（四） Pyhton模块</h2><h2 id="（五）-Python包"><a href="#（五）-Python包" class="headerlink" title="（五） Python包"></a>（五） Python包</h2><h2 id="（六）-安装第三类Python包"><a href="#（六）-安装第三类Python包" class="headerlink" title="（六） 安装第三类Python包"></a>（六） 安装第三类Python包</h2>]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>java基础学习笔记</title>
    <url>/2023/08/21/java%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<meta name="referrer" content="no-referrer" />

<meta name="referrer" content="no-referrer" />

<h3 id="Java语言概述"><a href="#Java语言概述" class="headerlink" title="Java语言概述"></a>Java语言概述</h3><h4 id="Java-语言的产生、应用前景和特点；（了解）"><a href="#Java-语言的产生、应用前景和特点；（了解）" class="headerlink" title="Java 语言的产生、应用前景和特点；（了解）"></a>Java 语言的产生、应用前景和特点；（了解）</h4><p>Java语言是由Sun Microsystems公司于1995年推出的一种面向对象编程语言，其产生源于对嵌入式设备和网络应用的需求。Java拥有跨平台、高效性、安全性等优点，被广泛应用于Web开发、企业级应用开发、移动应用开发、游戏开发等领域。</p>
<p>Java语言的主要特点包括：</p>
<ol>
<li><p>跨平台性：Java代码在编译后生成字节码，可以在任何支持Java虚拟机（JVM）的平台上运行，实现了”一次编写，到处运行”。</p>
</li>
<li><p>面向对象：Java是一种纯粹的面向对象编程语言，所有的数据类型都是对象，所有的操作都是方法调用。</p>
</li>
<li><p>自动内存管理：Java具有内置的垃圾收集器，可以自动回收不再使用的内存空间，避免了C++中常见的内存泄漏问题。</p>
</li>
<li><p>强类型检查：Java程序在编译时会进行类型检查，避免了类型转换错误等运行时错误。</p>
</li>
<li><p>安全性：Java提供了丰富的安全机制，如字节码校验、安全管理器等，可以保证程序的安全性。</p>
</li>
<li><p>多线程支持：Java提供了高级的多线程支持库和同步机制，方便开发人员编写多线程程序。</p>
</li>
</ol>
<p>从目前的趋势来看，Java仍然是一种非常流行的编程语言，它的应用前景非常广泛。在企业级应用开发、Web应用开发、大数据处理、云计算等领域，Java都有着广泛的应用。同时，Java社区也非常活跃，涌现出了很多优秀的开源框架和工具，如Spring、Hibernate、Maven等，它们可以帮助开发人员更高效地进行开发，并提高了Java应用程序的质量。此外，由于移动互联网的普及，Java在移动应用开发领域也有不少的应用，如Android应用程序开发。</p>
<p>总之，Java作为一种强大、灵活、安全和跨平台的编程语言，具有广泛的应用前景，在各行业都有着较为广泛的应用需求。同时，由于其开源性质，Java社区中不断涌现出更多优秀的框架和工具，使得Java语言变得更加易于使用和学习。</p>
<h4 id="Java虚拟机及Java运行系统；（了解）"><a href="#Java虚拟机及Java运行系统；（了解）" class="headerlink" title="Java虚拟机及Java运行系统；（了解）"></a>Java虚拟机及Java运行系统；（了解）</h4><p>Java虚拟机（JVM）是一个可以执行Java字节码的虚拟计算机，负责将编译后的Java源代码转换成可执行的字节码，并提供了内存管理、垃圾回收、安全检查和动态链接等功能。JVM的核心是一个解释器和一个即时编译器，能够在运行时动态优化代码，提高应用程序的性能。</p>
<p>Java运行系统（JRE）是Java应用程序的运行环境，包括Java虚拟机、Java类库和其他运行所需的文件。JRE可以在不同的平台上运行Java程序，并且与操作系统相对独立，因此Java程序能够实现跨平台运行。</p>
<p>Java虚拟机和Java运行系统的结构如下：</p>
<ol>
<li><p>Java虚拟机由三个子系统组成：类加载器、运行时数据区和执行引擎。类加载器负责将编写好的Java代码加载到JVM中；运行时数据区包括方法区、堆、栈、本地方法栈和程序计数器等几个部分，用于存储Java对象、JVM执行状态和方法信息等；执行引擎则负责执行Java字节码。</p>
</li>
<li><p>Java运行系统包括两部分：JRE和Java开发工具包（JDK）。JRE是Java应用程序的运行环境，包括Java虚拟机、Java类库和其他运行所需的文件；而JDK则是Java开发人员的工具包，除了包含JRE之外，还包括编译器、调试器、文档生成器等开发工具，用于编写和调试Java程序。</p>
</li>
</ol>
<p>总之，Java虚拟机和Java运行系统是Java程序运行的基础，Java程序通过这些机制来实现跨平台运行，并提供了强大的内存管理、安全检查和动态链接等功能，极大地简化了Java应用程序的开发和部署。因此，Java虚拟机和Java运行系统是Java语言成功的关键所在，也是Java语言广泛应用于各个领域的重要原因之一。</p>
<h4 id="Java-语言和-C-语言的相同及不同之处；"><a href="#Java-语言和-C-语言的相同及不同之处；" class="headerlink" title="Java 语言和 C++语言的相同及不同之处；"></a>Java 语言和 C++语言的相同及不同之处；</h4><p>Java 和 C++ 都是面向对象的编程语言，它们在语法和基本概念上有些相似，例如类、继承、多态等。但是，两者之间还存在一些不同之处：</p>
<ol>
<li><p>平台依赖性：Java是跨平台的，因为Java程序不直接运行于操作系统，而是通过Java虚拟机（JVM）来执行，可以在任何支持JVM的平台上运行；而C++是与特定操作系统相关的，需要编译成对应平台的可执行文件。</p>
</li>
<li><p>内存管理：Java中的内存管理是由垃圾回收器负责的，开发人员无需手动分配或释放内存，避免了C++中常见的内存泄漏问题。</p>
</li>
<li><p>异常处理：Java支持异常处理机制，在出现异常时会抛出异常并进行处理，而C++中则使用try&#x2F;catch机制实现异常处理。</p>
</li>
<li><p>多线程：Java天生支持多线程编程，提供了高级的线程支持库和同步机制，方便开发人员编写多线程程序。C++也支持多线程编程，但需要使用操作系统提供的线程API和同步机制。</p>
</li>
<li><p>代码复杂度：相对于C++，Java的代码更容易阅读和理解，因为Java具有更简单的语法，同时也有更多的自动化功能，如垃圾回收等。</p>
</li>
</ol>
<p>总体来说，Java更适合开发大型企业级应用程序，尤其是网络应用程序，而C++更适合低层次系统编程和性能要求高的应用程序。</p>
<h4 id="Java-应用程序的编写、编译和运行过程。"><a href="#Java-应用程序的编写、编译和运行过程。" class="headerlink" title="Java 应用程序的编写、编译和运行过程。"></a>Java 应用程序的编写、编译和运行过程。</h4><p>Java应用程序的编写、编译和运行过程主要包括以下几个步骤：</p>
<ol>
<li><p>编写Java源代码：使用文本编辑器或IDE（集成开发环境）编写Java源代码，以.java为文件扩展名保存。</p>
</li>
<li><p>编译Java源代码：使用Java编译器将Java源代码编译成字节码，以.class为文件扩展名保存。在命令行窗口中，可以使用“javac”命令进行编译，例如：“javac HelloWorld.java”。</p>
</li>
<li><p>运行Java程序：使用Java虚拟机（JVM）来执行编译后的Java字节码文件，以.class为文件扩展名。在命令行窗口中，可以使用“java”命令来运行Java程序，例如：“java HelloWorld”。</p>
</li>
<li><p>测试和调试：通过单元测试、集成测试等方式对Java程序进行测试，如果出现问题，则需要进行调试和修复，在开发过程中反复进行上述步骤。</p>
</li>
</ol>
<p>总之，Java应用程序的编写、编译和运行过程是一个由多个步骤组成的过程，需要遵循一定的规范和标准。开发人员在编写Java程序时，需要熟悉Java语言和编程环境，并且注意代码的质量和性能。同时，Java社区中也有很多优秀的开源框架和工具，如Spring、Hibernate、Maven等，可以帮助开发人员更高效地进行开发，并提高了Java应用程序的质量。</p>
<h3 id="java语言基础"><a href="#java语言基础" class="headerlink" title="java语言基础"></a>java语言基础</h3><h4 id="数据类型的转换（自动类型转换和强制类型转换）；运算符的优先级和结合性；（了解）"><a href="#数据类型的转换（自动类型转换和强制类型转换）；运算符的优先级和结合性；（了解）" class="headerlink" title="数据类型的转换（自动类型转换和强制类型转换）；运算符的优先级和结合性；（了解）"></a>数据类型的转换（自动类型转换和强制类型转换）；运算符的优先级和结合性；（了解）</h4><p>数据类型的转换：</p>
<p>Java中有两种数据类型的转换：自动类型转换和强制类型转换。</p>
<ol>
<li><p>自动类型转换：当一种数据类型的值赋值给另一种数据类型时，如果目标类型可以容纳原始类型的数据，则会自动进行类型转换。例如，将int类型的值赋值给double类型的变量，编译器会自动将int类型转换为double类型。</p>
</li>
<li><p>强制类型转换：当需要将一个较大的数值类型转换成一个较小的数值类型时，需要使用强制类型转换。在强制类型转换时，需要在括号内指定要转换的数据类型。例如，将double类型的值转换为int类型的值，可以使用强制类型转换运算符，例如：“int a &#x3D; (int) 3.14”。<br><img src="https://img-blog.csdnimg.cn/20210429150845555.png" alt="在这里插入图片描述"></p>
</li>
</ol>
<p>运算符的优先级和结合性：</p>
<p>Java中有多种运算符，它们具有不同的优先级和结合性，可以通过以下规则来确定表达式的计算顺序：</p>
<ol>
<li><p>括号优先级最高，先计算括号内的表达式。</p>
</li>
<li><p>先计算高优先级的运算符，再计算低优先级的运算符。如果有相同优先级的运算符，则按照结合性从左到右或从右到左计算，例如“+”和“-”运算符的结合性是从左到右。</p>
</li>
<li><p>赋值运算符“&#x3D;”的优先级最低。</p>
</li>
</ol>
<p>下面是常见运算符的优先级从高到低的顺序：</p>
<p>以下是Java中常见的运算符优先级，按照从高到低的顺序排列：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th align="center">描述</th>
</tr>
</thead>
<tbody><tr>
<td>() []</td>
<td align="center">括号，具有最高优先级，用于强制改变运算符的计算顺序</td>
</tr>
<tr>
<td>! ~ ++ – -</td>
<td align="center">一元运算符，包括取反、位非、自增、自减、负数等</td>
</tr>
<tr>
<td>* &#x2F; %</td>
<td align="center">乘法、除法、取模 new同级</td>
</tr>
<tr>
<td>+ -</td>
<td align="center">加法、减法</td>
</tr>
<tr>
<td>&lt;&lt; &gt;&gt; &gt;&gt;&gt;</td>
<td align="center">位移运算符，分别表示左移、右移、无符号右移</td>
</tr>
<tr>
<td>&lt; &lt;&#x3D; &gt; &gt;&#x3D;</td>
<td align="center">关系运算符，用于比较两个值的大小关系</td>
</tr>
<tr>
<td>&#x3D;&#x3D; !&#x3D;</td>
<td align="center">等于、不等于</td>
</tr>
<tr>
<td>&amp;</td>
<td align="center">按位与</td>
</tr>
<tr>
<td>^</td>
<td align="center">按位异或</td>
</tr>
<tr>
<td>|</td>
<td align="center">按位或</td>
</tr>
<tr>
<td>&amp;&amp;</td>
<td align="center">逻辑与</td>
</tr>
<tr>
<td>||</td>
<td align="center">逻辑或</td>
</tr>
<tr>
<td>?:</td>
<td align="center">三目运算符，用于条件判断</td>
</tr>
<tr>
<td>&#x3D; +&#x3D; -&#x3D; *&#x3D; &#x2F;&#x3D; %&#x3D; &amp;&#x3D; ^&#x3D; |&#x3D; &lt;&lt;&#x3D; &gt;&gt;&#x3D; &gt;&gt;&gt;&#x3D;</td>
<td align="center">赋值运算符，包括复合赋值运算符和位移赋值运算符</td>
</tr>
</tbody></table>
<p>在实际编程中，为了避免优先级和结合性引起的错误，建议使用括号来明确表达式的计算顺序。同时，也需要注意强制类型转换可能会导致数据丢失或溢出问题，需要进行适当的检查和处理。</p>
<h4 id="Java语言各种数据类型；"><a href="#Java语言各种数据类型；" class="headerlink" title="Java语言各种数据类型；"></a>Java语言各种数据类型；</h4><p>Java语言中有8种基本数据类型，可以分为4种整型、2种浮点型、1种字符型和1种布尔型。具体如下：</p>
<ol>
<li><p>整型（4种）：byte、short、int、long。</p>
</li>
<li><p>浮点型（2种）：float、double。</p>
</li>
<li><p>字符型（1种）：char。</p>
</li>
<li><p>布尔型（1种）：boolean。</p>
</li>
</ol>
<p>这些数据类型在Java中都是关键字，在程序中可以直接使用。各数据类型的范围和存储长度如下：</p>
<ol>
<li><p>byte：1字节，-128到127。</p>
</li>
<li><p>short：2字节，-32768到32767。</p>
</li>
<li><p>int：4字节，-2147483648到2147483647。</p>
</li>
<li><p>long：8字节，-9223372036854775808到9223372036854775807。</p>
</li>
<li><p>float：4字节，IEEE754标准表示的单精度浮点数。</p>
</li>
<li><p>double：8字节，IEEE754标准表示的双精度浮点数。</p>
</li>
<li><p>char：2字节，0到65535（Unicode编码）。</p>
</li>
<li><p>boolean：1位，true或false。</p>
</li>
</ol>
<p>可以根据需求选择不同的数据类型来存储数据，例如使用byte类型来存储文件内容，使用int类型来存储计数器等。同时，在进行数据类型转换时需要注意避免出现数据溢出或数据丢失的情况。</p>
<h4 id="Java语言算术运算符、关系运算符、逻辑运算符、位运算符和复合赋值运算符的功能及使用；"><a href="#Java语言算术运算符、关系运算符、逻辑运算符、位运算符和复合赋值运算符的功能及使用；" class="headerlink" title="Java语言算术运算符、关系运算符、逻辑运算符、位运算符和复合赋值运算符的功能及使用；"></a>Java语言算术运算符、关系运算符、逻辑运算符、位运算符和复合赋值运算符的功能及使用；</h4><p>Java语言中有多种运算符，包括算术运算符、关系运算符、逻辑运算符、位运算符和复合赋值运算符等。下面分别介绍这些运算符的功能及使用。</p>
<ol>
<li><p>算术运算符：用于处理数学运算，包括加减乘除、取余数等，常见的算术运算符有+、-、*、&#x2F;、%等。例如，a + b表示a与b的和，a % b表示a除以b的余数。</p>
</li>
<li><p>关系运算符：用于比较两个变量的关系，常见的关系运算符有&gt;、&gt;&#x3D;、&lt;、&lt;&#x3D;、&#x3D;&#x3D;、!&#x3D;等。例如，a &gt; b表示a是否大于b，a &#x3D;&#x3D; b表示a是否等于b。</p>
</li>
<li><p>逻辑运算符：用于连接两个或多个表达式，常见的逻辑运算符有&amp;&amp;（与）、||（或）、！（非）等。例如，a &gt; b &amp;&amp; c &gt; d表示当a大于b并且c大于d时为真。</p>
</li>
<li><p>位运算符：用于对数据的二进制位进行操作，常见的位运算符有&amp;（按位与）、|（按位或）、^（按位异或）、~（按位取反）、&lt;&lt;（左移）、&gt;&gt;（右移）和&gt;&gt;&gt;（无符号右移）等。例如，a &amp; b表示将a和b按位与运算。</p>
</li>
<li><p>复合赋值运算符：用于简化赋值语句的编写，常见的复合赋值运算符有+&#x3D;、-&#x3D;、*&#x3D;、&#x2F;&#x3D;、%&#x3D;、&amp;&#x3D;、|&#x3D;、^&#x3D;、&lt;&lt;&#x3D;、&gt;&gt;&#x3D;、&gt;&gt;&gt;&#x3D;等。例如，a +&#x3D; b可以写成a &#x3D; a + b的形式。</p>
</li>
</ol>
<p>总之，Java语言中的各种运算符可帮助开发人员进行数学、逻辑和位运算，更高效地完成程序的设计和实现。在使用运算符时，需要注意数据类型的匹配和运算符的优先级和结合性，以避免出现错误的结果。同时，也可以通过括号来明确表达式的计算顺序，提高代码的可读性和可维护性。例如：</p>
<p><code>int a = 10, b = 3, c = 5; int result = (a + b) * c; // 使用括号明确计算顺序 System.out.println(result); // 输出65</code></p>
<p>另外还需要注意，在进行位运算时，需要特别小心因为这些运算是基于二进制表示的，如果使用不当可能会导致数据被修改或出现意外的结果。</p>
<h4 id="Java语言变量、常量的使用及其运算操作；"><a href="#Java语言变量、常量的使用及其运算操作；" class="headerlink" title="Java语言变量、常量的使用及其运算操作；"></a>Java语言变量、常量的使用及其运算操作；</h4><p>Java语言中，变量和常量是程序中存储数据的基本方式。下面分别介绍变量、常量的使用及其运算操作。</p>
<ol>
<li>变量：在程序中用于存储可变的数据值，需要先声明再使用，可以多次赋值。例如：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a; // 声明变量a</span><br><span class="line"> a = 10; </span><br><span class="line">// 赋值为10</span><br><span class="line"> a = a + 5; </span><br><span class="line">// 加上5并重新赋值 </span><br><span class="line">System.out.println(a); // 输出15</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>常量：在程序中用于存储不可变的数据值，一旦被赋值就不能改变。通常使用关键字final来定义常量，例如：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">final int MAX_VALUE = 100; // 定义一个常量</span><br><span class="line">//MAX_VALUE = 200; // 编译错误，无法更改常量的值 System.out.println(MAX_VALUE); // 输出100</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>运算操作：在Java中，变量和常量可以进行各种数学和逻辑运算，在使用运算符时需要注意数据类型的匹配和运算符的优先级和结合性。例如：</li>
</ol>
<p>java复制代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = 10, b = 3; double c = 5.0;</span><br><span class="line"> int result = a / b;</span><br><span class="line"> // 整型除法，result的值为3 double result2 = a / c;</span><br><span class="line"> // 自动类型转换为浮点型，result2的值为2.0 int result3 = a % b;</span><br><span class="line"> // 取余数操作，result3的值为1  boolean flag = true; if (flag &amp;&amp; (a &gt; b)) &#123; // 使用逻辑与运算符和关系运算符     System.out.println(&quot;a is greater than b&quot;); &#125;</span><br></pre></td></tr></table></figure>

<p>总之，变量和常量是Java程序中存储数据的基本方式，可以通过各种运算操作来实现程序的需求。在使用过程中，需要遵循命名规范和数据类型规范，以提高代码的可读</p>
<p>性和可维护性，同时也需要注意变量的作用域和生命周期等问题。</p>
<p>在进行数学运算时，需要注意整型除法可能会出现数据截断的问题，因此可以使用浮点型变量或强制类型转换来避免这些问题。另外，还需要注意整型溢出问题，当超出了数据类型所能表示的范围时，结果将不可预测。在使用常量时，建议使用大写字母和下划线来命名，以便于区分变量和常量。</p>
<h4 id="Java语言流程控制语句的功能及使用"><a href="#Java语言流程控制语句的功能及使用" class="headerlink" title="Java语言流程控制语句的功能及使用"></a>Java语言流程控制语句的功能及使用</h4><p>Java语言中，流程控制语句用于控制程序的执行顺序，可以根据条件选择不同的分支或者重复执行某些代码。下面分别介绍流程控制语句的功能及使用。</p>
<ol>
<li><p>if语句：用于根据特定条件执行不同的代码块，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = 10, b = 20;</span><br><span class="line">if (a &gt; b) &#123;</span><br><span class="line">    System.out.println(&quot;a is greater than b&quot;);</span><br><span class="line">&#125; else if (a &lt; b) &#123;</span><br><span class="line">    System.out.println(&quot;a is less than b&quot;);</span><br><span class="line">&#125; else &#123;</span><br><span class="line">    System.out.println(&quot;a is equal to b&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>switch语句：用于根据不同条件执行不同的代码块，比if语句更加简洁，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int dayOfWeek = 3;</span><br><span class="line">switch (dayOfWeek) &#123;</span><br><span class="line">    case 1:</span><br><span class="line">        System.out.println(&quot;Monday&quot;);</span><br><span class="line">        break;</span><br><span class="line">    case 2:</span><br><span class="line">        System.out.println(&quot;Tuesday&quot;);</span><br><span class="line">        break;</span><br><span class="line">    case 3:</span><br><span class="line">        System.out.println(&quot;Wednesday&quot;);</span><br><span class="line">        break;</span><br><span class="line">    default:</span><br><span class="line">        System.out.println(&quot;Other day&quot;);</span><br><span class="line">        break;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>for循环：用于对一段代码进行固定次数的重复执行，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for (int i = 0; i &lt; 10; i++) &#123;</span><br><span class="line">    System.out.println(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>while循环和do-while循环：用于在满足特定条件的情况下重复执行一段代码，其中while循环先检查条件再执行代码，而do-while循环先执行代码再检查条件，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int i = 0;</span><br><span class="line">while (i &lt; 10) &#123;</span><br><span class="line">    System.out.println(i);</span><br><span class="line">    i++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int j = 0;</span><br><span class="line">do &#123;</span><br><span class="line">    System.out.println(j);</span><br><span class="line">    j++;</span><br><span class="line">&#125; while (j &lt; 10);</span><br></pre></td></tr></table></figure>
</li>
<li><p>break和continue语句：用于控制循环的执行，break语句可用于跳出循环，continue语句可用于跳过当前迭代并进入下一迭代，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">for (int i = 0; i &lt; 10; i++) &#123;</span><br><span class="line">    if (i == 5) &#123;</span><br><span class="line">        break; // 跳出循环</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(i);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">for (int j = 0; j &lt; 10; j++) &#123;</span><br><span class="line">    if (j % 2 == 0) &#123;</span><br><span class="line">        continue; // 跳过当前迭代</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println(j);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>return语句：用于退出函数并返回一个值，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public int add(int a, int b) &#123;</span><br><span class="line">    return a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>throw语句：用于在程序中抛出异常，需要与try-catch语句一起使用，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (a &lt; 0) &#123;</span><br><span class="line">    throw new IllegalArgumentException(&quot;a must be greater than 0&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>总之，Java语言中的流程控制语句可以帮助开发人员根据条件选择执行不同的代码块或重复执行某些代码，从而实现程序的需求。在使用时需要注意语法和使用场景，以避免出现逻辑错误和性能问题。</p>
<h4 id="Java数组的定义；数组的初始化和数组的应用；二维数组的应用。"><a href="#Java数组的定义；数组的初始化和数组的应用；二维数组的应用。" class="headerlink" title="Java数组的定义；数组的初始化和数组的应用；二维数组的应用。"></a>Java数组的定义；数组的初始化和数组的应用；二维数组的应用。</h4><p>Java数组是一组相同数据类型的元素的集合，可以通过下标来访问和修改数组中的元素。下面分别介绍Java数组的定义、初始化和应用，以及二维数组的应用。</p>
<ol>
<li><p>数组的定义：在Java中，使用方括号[]来定义一个数组，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[] numbers; // 定义一个整型数组</span><br><span class="line">String[] names; // 定义一个字符串数组</span><br><span class="line">double[] prices; // 定义一个双精度浮点数数组</span><br></pre></td></tr></table></figure>
</li>
<li><p>数组的初始化：在Java中，可以使用大括号{}来初始化一个数组，也可以使用new关键字在堆上分配空间进行初始化，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[] numbers = &#123;1, 2, 3&#125;; // 使用大括号初始化数组</span><br><span class="line">String[] names = new String[5]; // 在堆上分配空间并初始化为null</span><br><span class="line">double[] prices = new double[]&#123;1.0, 2.0, 3.0&#125;; // 使用new关键字和大括号初始化数组</span><br></pre></td></tr></table></figure>

<p>3.数组的应用：在Java中，可以使用下标来访问和修改数组中的元素，数组下标从0开始，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[] numbers = &#123;1, 2, 3&#125;;</span><br><span class="line">System.out.println(numbers[0]); // 输出1</span><br><span class="line">numbers[1] = 4;</span><br><span class="line">System.out.println(numbers[1]); // 输出4</span><br><span class="line"></span><br><span class="line">//可以使用Arrays.toString()来输出一个完整数组</span><br><span class="line"> System.out.println(Arrays.toString(num));</span><br></pre></td></tr></table></figure>

<p>4.数组排序<br>（1）<strong>数组遍历查找最小值法</strong>是一种常用的查找数组中最小值的方法。其基本思路是使用循环遍历整个数组，通过比较每个元素的大小来找到数组中的最小值。<br>具体实现步骤如下：</p>
<ol>
<li>使用一个变量temp记录当前所遍历到的最小值，将数组中的第一个元素赋值给temp作为初始值；</li>
<li>使用for循环遍历数组中的每个元素，从第二个元素开始比较；</li>
<li>如果当前元素比temp小，则将当前元素赋值给temp，以更新最小值；</li>
<li>循环结束后，temp中存储的就是数组中的最小值。</li>
</ol>
<p>该方法简单易懂，适用于各种类型的数组，并且空间复杂度和时间复杂度都比较低，是一种常见的查找数组最小值的方法。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a[] = &#123;5, 2, 3, 4, 5,&#125;; //数组遍历查找最小值法</span><br><span class="line">        int temp = a[0];</span><br><span class="line">        for (int index = 1; index &lt; 5; index++) &#123;</span><br><span class="line">            if (a[index] &lt; temp) &#123;</span><br><span class="line">                temp=a[index];</span><br><span class="line">                System.out.println(temp);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<p>（2）<strong>冒泡排序</strong>是一种简单的排序算法，它通过比较相邻元素的大小来重复地交换、移动数组中的元素，从而将一个无序的数组按照升序（或降序）排列。<br><img src="https://img-blog.csdnimg.cn/a78e034ed81343a1859783161a83f193.gif" alt="a78e034ed81343a1859783161a83f193"><br>找规律：总共比了4轮，轮数&#x3D;数组长度-1（arr.length-1）,<br>                  每轮比较的次数&#x3D;素组长度 — 轮数（arr.length-1-i），<br>   比较的轮数用一个for循环表示，每一轮里比较的次数用一个for循环表示，此处用双重for循环，<br><img src="https://img-blog.csdnimg.cn/4e744525a72247c1a091deacc3508ceb.png" alt="4e744525a72247c1a091deacc3508ceb"></p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//排序算法——冒泡排序</span><br><span class="line">       int[] num=&#123;90,99,25,24,23,22&#125;;</span><br><span class="line"></span><br><span class="line">       for (int i = 0; i &lt; num.length - 1; i++) &#123;  //外层循环控制  轮数=数组长度-1（arr.length-1）</span><br><span class="line">           //内层循环控制元素两两比较的次数</span><br><span class="line">           //每轮比较的次数=素组长度 — 轮数（arr.length-1-i）</span><br><span class="line">           for (int j = 0; j &lt; num.length - 1 - i; j++) &#123;</span><br><span class="line">                //判断是否满足交换条件</span><br><span class="line">               if (num[j] &gt; num[j + 1]) &#123;</span><br><span class="line">                   //如果满足--交换两个元素的值</span><br><span class="line">                   int temp = num[j];</span><br><span class="line">                   num[j] = num[j + 1];</span><br><span class="line">                   num[j + 1] = temp;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       System.out.println(Arrays.toString(num));</span><br></pre></td></tr></table></figure>



<p>   5.二维数组的应用：在Java中，可以使用二维数组来表示表格等结构化数据，二维数组可以看做是一个由数组构成的数组，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[][] matrix = &#123;&#123;1, 2&#125;, &#123;3, 4&#125;&#125;;</span><br><span class="line">System.out.println(matrix[0][1]); // 输出2</span><br><span class="line">matrix[1][0] = 5;</span><br><span class="line">System.out.println(matrix[1][0]); // 输出5</span><br><span class="line"></span><br><span class="line">//可以使用Arrays.deepToString()输出完整二维数组</span><br><span class="line">Arrays.deepToString(allScore)</span><br></pre></td></tr></table></figure>

<p>总之，Java数组是一种重要的数据结构，可以用于存储和处理一组相同类型的数据。在使用时需要注意数组下标从0开始、数组越界问题、数组的初始化和数组越界问题等，特别是在遍历数组时需要格外小心。另外，Java中还提供了大量的数组操作方法，如排序、查找、求和、复制等，可以大大简化数组的处理过程。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[] numbers = &#123;3, 1, 4, 1, 5, 9&#125;;</span><br><span class="line">Arrays.sort(numbers); // 对数组进行排序</span><br><span class="line">int index = Arrays.binarySearch(numbers, 5); // 查找元素5的下标</span><br><span class="line">int sum = Arrays.stream(numbers).sum(); // 求和</span><br><span class="line">int[] copy = Arrays.copyOf(numbers, 3); // 复制前3个元素</span><br></pre></td></tr></table></figure>

<p>二维数组可以看做是一个由一维数组构成的数组，在使用时需要注意行列的顺序以及各维度的大小等问题。可以使用多重循环来遍历和处理二维数组，也可以使用流式API进行处理。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[][] matrix = &#123;&#123;1, 2&#125;, &#123;3, 4&#125;&#125;;</span><br><span class="line">for (int i = 0; i &lt; matrix.length; i++) &#123;</span><br><span class="line">    for (int j = 0; j &lt; matrix[i].length; j++) &#123;</span><br><span class="line">        System.out.println(matrix[i][j]); // 遍历二维数组</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int sum = Arrays.stream(matrix)</span><br><span class="line">                .flatMapToInt(Arrays::stream)</span><br><span class="line">                .sum(); // 使用流式API求和</span><br></pre></td></tr></table></figure>

<p>总之，Java数组是一个非常有用的数据结构，可以用于存储和处理一组相同类型的数据。在使用时需要注意数组下标从0开始、数组越界问题、多维数组的应用等问题，同时也可以使用大量的数组操作方法来简化处理过程.</p>
<h3 id="面向对象编程"><a href="#面向对象编程" class="headerlink" title="面向对象编程"></a>面向对象编程</h3><h4 id="面向对象的概念（了解）"><a href="#面向对象的概念（了解）" class="headerlink" title="面向对象的概念（了解）"></a>面向对象的概念（了解）</h4><p>面向对象是一种程序设计的思想和方法，它将程序中的所有数据和操作都看做是一个对象，每个对象有自己的属性和行为，并且可以相互交互和协作。通过面向对象的方式，可以更加直观、灵活和可维护地实现程序逻辑。</p>
<p>在Java语言中，面向对象是一种基本的编程范式。面向对象的编程包括以下几个概念：</p>
<ol>
<li><p>类(class)：类是一种定义对象属性和行为的模板，可以看做是一种用户自定义的数据类型。每个类定义了对象的状态和行为，并提供了一组公共接口来访问和修改对象的状态。</p>
</li>
<li><p>对象(object)：对象是类的一个实例，具有类所定义的属性和行为。每个对象都具有独立的状态和行为，并可以与其他对象进行交互和协作。</p>
</li>
<li><p>方法(method)：方法是类中定义的用于执行特定任务的代码块，可以访问和修改对象的状态，并返回一个值或执行一些操作。</p>
</li>
<li><p>继承(inheritance)：继承是一种从一个类派生出新类的机制，新类可以继承原类的属性和方法，并可以添加自己的属性和方法。继承可以大大简化程序的设计和实现过程。</p>
</li>
<li><p>多态(polymorphism)：多态是一种在运行时选择不同实现的机制，允许使用相同的接口来访问不同的对象。多态可以提高程序的灵活性和可扩展性。</p>
</li>
<li><p>封装(encapsulation)：封装是一种将对象的状态和行为隐藏起来，只提供必要的接口供其他对象访问的机制。封装可以保证对象的安全性和稳定性。</p>
</li>
</ol>
<p>总之，面向对象是一种重要的编程思想和方法，它可以使程序更加清晰、灵活和可维护。在Java语言中，面向对象是一种基本的编程范式，包括类、对象、方法、继承、多态和封装等概念。通过面向对象的方式，可以将问题分解为更小的模块，并且可以更加直观地描述和实现系统的逻辑。同时，面向对象也可以提高程序的复用性、可扩展性和可测试性，从而大大提高程序的质量和效率。</p>
<p>在实践中，为了更好地应用面向对象的思想和方法，需要遵循一些设计原则和最佳实践。例如，单一职责原则(SRP)、开放封闭原则(OCP)、里氏替换原则(LSP)、依赖倒置原则(DIP)和接口隔离原则(ISP)等，这些原则可以帮助开发人员设计出更加优秀、可维护和可扩展的系统。另外，在Java中还有一些常用的设计模式，如工厂模式、单例模式、观察者模式等，这些模式可以在具体的场景中帮助开发人员更加高效地实现系统需求。</p>
<p>总之，面向对象是一种重要的编程思想和方法，可以使程序更加清晰、灵活和可维护。在Java语言中，面向对象是一种基本的编程范式，包括类、对象、方法、继承、多态和封装等概念，同时也需要遵循一些设计原则和最佳实践，以实现高质量的软件系统。</p>
<h4 id="类的创建与使用；"><a href="#类的创建与使用；" class="headerlink" title="类的创建与使用；"></a>类的创建与使用；</h4><p>在Java中，类是一种用户自定义的数据类型，它用于描述对象的属性和行为，并提供了一组公共接口来访问和修改对象的状态。下面介绍如何创建和使用Java类。</p>
<ol>
<li><p>类的创建：在Java中，可以使用关键字class来定义一个类，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Person &#123;</span><br><span class="line">    private String name;</span><br><span class="line">    private int age;</span><br><span class="line"></span><br><span class="line">    public Person(String name, int age) &#123;</span><br><span class="line">        this.name = name;</span><br><span class="line">        this.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getName() &#123;</span><br><span class="line">        return name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setName(String name) &#123;</span><br><span class="line">        this.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public int getAge() &#123;</span><br><span class="line">        return age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setAge(int age) &#123;</span><br><span class="line">        this.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>这个例子定义了一个名为Person的类，它具有两个私有属性name和age以及四个公共方法getName、setName、getAge和setAge。其中，构造函数用于创建对象，并初始化对象的状态</p>
<ol start="2">
<li><p>对象的创建：在Java中，可以使用new关键字来创建一个对象，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Person p = new Person(&quot;Alice&quot;, 20);</span><br></pre></td></tr></table></figure></li>
</ol>
<p>这个例子创建了一个名为p的Person对象，并将其初始化为姓名为Alice、年龄为20。</p>
<ol start="3">
<li><p>对象的使用：在Java中，使用点号(.)来访问和修改对象的属性和调用对象的方法，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String name = p.getName(); // 获取对象的姓名</span><br><span class="line">p.setAge(21); // 修改对象的年龄</span><br></pre></td></tr></table></figure>
</li>
<li><p>类的继承：在Java中，可以通过extends关键字来实现类的继承，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Student extends Person &#123;</span><br><span class="line">    private String major;</span><br><span class="line"></span><br><span class="line">    public Student(String name, int age, String major) &#123;</span><br><span class="line">        super(name, age);</span><br><span class="line">        this.major = major;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public String getMajor() &#123;</span><br><span class="line">        return major;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setMajor(String major) &#123;</span><br><span class="line">        this.major = major;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>这个例子定义了一个名为Student的类，它是Person类的子类，具有一个额外的属性major。在构造函数中，使用super关键字调用父类的构造函数来初始化姓名和年龄。</p>
<ol start="5">
<li><p>对象的多态：在Java中，可以通过向上转型和方法重写等机制实现对象的多态性。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Person p = new Student(&quot;Bob&quot;, 22, &quot;Computer Science&quot;);</span><br><span class="line">String name = p.getName(); // 调用父类的方法</span><br></pre></td></tr></table></figure></li>
</ol>
<p>这个例子创建了一个名为p的Person对象，并将其指向一个Student对象。由于Student是Person的子类，因此可以向上转型为Person类型，并访问Person中定义的公共接口。</p>
<p>总之，在Java中，创建和使用类是一种基本的编程技能。需要熟悉类的定义、对象的创建和使用、继承和多态等概念，并遵循相关的设计原则和最佳实践，以实现高质量的软件系统。同时，在实践中还需要注意类的命名规范、属性的封装、方法的参数和返回值等问题，以保证程序的正确性和可维护性。</p>
<h4 id="方法的定义和使用"><a href="#方法的定义和使用" class="headerlink" title="方法的定义和使用"></a>方法的定义和使用</h4><p>方法是一段可重复调用的代码块，用于完成特定的任务。在程序中定义方法后，可以在需要的地方多次调用该方法，从而避免重复编写相同的代码。</p>
<p>方法通常由方法名、参数列表和方法体组成。方法名是唯一的标识符，描述了方法执行的任务。参数列表包含了传递给方法的参数，这些参数可以在方法体中使用。方法体是实现方法功能的代码块。</p>
<p>要使用一个方法，只需通过方法名和必要的参数调用它即可。方法的返回值可以用来提供操作结果或状态信息。</p>
<p>例如，下面是一个简单的Java方法，用于计算两个整数的和：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public int add(int x, int y) &#123; </span><br><span class="line"></span><br><span class="line">   return x + y</span><br><span class="line"></span><br><span class="line">;&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>在程序中，可以通过以下方式调用该方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int result = add(2, 3);</span><br></pre></td></tr></table></figure>

<p>此时，result 的值将为 5，因为传递给 add 方法的两个参数分别为 2 和 3，方法计算它们的和并返回结果。</p>
<h4 id="对象的基本操作方式"><a href="#对象的基本操作方式" class="headerlink" title="对象的基本操作方式"></a>对象的基本操作方式</h4><p>对象是类的一个实例，通常用来表示现实世界中的某个实体或概念。在程序中，我们可以通过创建对象来操作它们的属性和方法，实现特定的功能。</p>
<p>以下是对象的基本操作方式：</p>
<ol>
<li><pre><code>创建对象：使用关键字“new”加上类名来创建一个对象，并将其赋值给一个变量。
Person person = new Person();
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">2. ```</span><br><span class="line">   访问对象属性：使用点号（&quot;.&quot;）操作符来访问对象的属性。</span><br><span class="line">   String name = person.name;</span><br></pre></td></tr></table></figure>
</code></pre>
</li>
<li><pre><code>修改对象属性：使用点号（&quot;.&quot;）操作符来修改对象的属性。
person.age = 25;
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">4. ```</span><br><span class="line">   调用对象方法：使用点号（&quot;.&quot;）操作符来调用对象的方法。</span><br><span class="line">   person.sayHello();</span><br></pre></td></tr></table></figure>
</code></pre>
</li>
<li><pre><code>比较对象：可以使用“==”运算符来比较两个对象是否相等，或者使用对象的 equals 方法。
if (person1 == person2) &#123;    
System.out.println(&quot;They are the same person.&quot;);
&#125;
if (person1.equals(person2)) 
&#123;    
System.out.println(&quot;They have the same name and age.&quot;);
&#125;
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">6. 销毁对象：虽然Java有自动垃圾回收机制，但如果需要立即销毁一个对象，可以将其引用设置为 null，以便系统回收该对象的内存空间。</span><br><span class="line">   person = null;</span><br><span class="line"></span><br><span class="line">希望这些基本的对象操作能够帮助您更好地理解如何使用对象。</span><br><span class="line"></span><br><span class="line">#### 构造方法的定义和使用</span><br><span class="line"></span><br><span class="line">构造方法是一种特殊类型的方法，它用于在创建对象时初始化对象的属性。当我们使用“new”操作符来创建一个对象时，实际上就是调用了该类的构造方法。</span><br><span class="line"></span><br><span class="line">在Java中，构造方法与类名相同，并且没有返回类型。构造方法可以有参数，也可以没有参数。如果没有定义任何构造方法，则Java会自动提供一个无参构造方法（即默认构造方法）。</span><br><span class="line"></span><br><span class="line">下面是一个简单的Java构造方法的例子：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
public class Person &#123;
 private String name;
 private int age;

 public Person(String name, int age) &#123;
     this.name = name;
     this.age = age;
 &#125;
</code></pre>
</li>
</ol>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在这个例子中，Person 类有一个带有两个参数的构造方法，用于初始化对象的属性。当我们创建一个 Person 对象时，需要提供 name 和 age 两个参数。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>Person person1 &#x3D; new Person(“Alice”, 25);<br>Person person2 &#x3D; new Person(“Bob”, 30);</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在以上代码中，我们分别创建了 person1 和 person2 两个 Person 对象，并分别传递了不同的参数。这些参数被用于初始化相应对象的属性值。</span><br><span class="line"></span><br><span class="line">需要注意的是，构造方法的名称必须与类名相同，而且不能使用返回类型。构造方法可以重载，即在同一个类中可以定义多个具有不同参数的构造方法，以便满足不同的需求。</span><br><span class="line"></span><br><span class="line">希望这个例子能够帮助您了解如何定义和使用 Java 构造方法。</span><br><span class="line"></span><br><span class="line">#### this关键字使用</span><br><span class="line"></span><br><span class="line">在Java中，this关键字用于指代当前对象。可以使用this关键字访问当前对象的属性、方法或者构造函数。this关键字经常被用于以下情况：</span><br><span class="line"></span><br><span class="line">1. 引用当前对象的成员变量</span><br><span class="line"></span><br><span class="line">在类的实例方法中，可以使用this关键字来引用当前对象的成员变量。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    private String name;</p>
<pre><code>public void setName(String name) &#123;
    this.name = name;
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的代码中，使用了this关键字来引用当前对象的name属性。</span><br><span class="line"></span><br><span class="line">2. 调用当前对象的其他方法</span><br><span class="line"></span><br><span class="line">在类的某个实例方法中，可以使用this关键字来调用该对象的其他方法。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    public void sayHello() {<br>        System.out.println(“Hello”);<br>    }</p>
<pre><code>public void sayHi() &#123;
    this.sayHello(); // 调用当前对象的sayHello方法
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的代码中，在sayHi方法中使用了this关键字来调用当前对象的sayHello方法。</span><br><span class="line"></span><br><span class="line">3. 在构造函数中调用其他构造函数</span><br><span class="line"></span><br><span class="line">在一个类的构造函数中，可以使用this关键字来调用该类的其他构造函数。这种方式称为构造函数的重载。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    private String name;<br>    private int age;</p>
<pre><code>public Person() &#123;
    this(&quot;&quot;, 0); // 调用另一个构造函数
&#125;

public Person(String name, int age) &#123;
    this.name = name;
    this.age = age;
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的代码中，定义了两个构造函数。第一个构造函数不带参数，当创建Person对象时会自动调用该构造函数。在该构造函数中使用了this关键字来调用另一个构造函数。</span><br><span class="line"></span><br><span class="line">#### static关键字使用</span><br><span class="line"></span><br><span class="line">在Java中，static关键字用于定义静态变量、静态方法和静态代码块。被static修饰的成员属于类而非实例化对象，可以通过类名直接访问，不需要先创建对象。</span><br><span class="line"></span><br><span class="line">1. 静态变量</span><br><span class="line"></span><br><span class="line">静态变量是类级别的变量，被该类的所有对象所共享，通常用于记录某个类的全局状态。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    public static int count;</p>
<pre><code>public Person() &#123;
    count++;
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了一个静态变量count，它的初始值为0。每当创建Person对象时，会自动调用构造函数，并将count加1。</span><br><span class="line"></span><br><span class="line">2. 静态方法</span><br><span class="line"></span><br><span class="line">静态方法也是类级别的方法，可以直接通过类名来调用，不需要先创建实例化对象。静态方法不能访问非静态成员，因为非静态成员的存在依赖于具体的对象。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class MathUtil {<br>    public static int add(int a, int b) {<br>        return a + b;<br>    }<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了一个静态方法add，用于计算两个整数的和。该方法可以直接通过类名来调用，例如MathUtil.add(1, 2)。</span><br><span class="line"></span><br><span class="line">3. 静态代码块</span><br><span class="line"></span><br><span class="line">静态代码块是在类加载时执行的一段代码块，用于初始化静态变量或其他操作。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class MyClass {<br>    private static int count;<br>    static {<br>        count &#x3D; 0;<br>    }<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了一个静态变量count和一个静态代码块，用于将count的初始值设为0。</span><br><span class="line"></span><br><span class="line">#### 成员变量和局部变量的区别</span><br><span class="line"></span><br><span class="line">成员变量和局部变量是Java中两种不同的变量类型，它们有以下区别：</span><br><span class="line"></span><br><span class="line">1. 定义位置</span><br><span class="line"></span><br><span class="line">成员变量定义在类中，而局部变量定义在代码块、方法或构造函数中。</span><br><span class="line"></span><br><span class="line">2. 生命周期</span><br><span class="line"></span><br><span class="line">成员变量的生命周期与对象相同，即只有在创建对象时才会被初始化，并且在对象被销毁时才会被销毁。而局部变量的生命周期仅限于所在的代码块、方法或构造函数的执行过程中。</span><br><span class="line"></span><br><span class="line">3. 访问权限</span><br><span class="line"></span><br><span class="line">成员变量可以被类中的其他方法和对象访问，而局部变量只能在其所在的代码块、方法或构造函数中访问。</span><br><span class="line"></span><br><span class="line">4. 初始化</span><br><span class="line"></span><br><span class="line">成员变量可以自动初始化，如果没有显式地为其赋初始值，则会被赋予默认值（如int类型的默认值为0）。而局部变量必须显式地进行初始化，否则编译器会提示错误。</span><br><span class="line"></span><br><span class="line">5. 存储方式</span><br><span class="line"></span><br><span class="line">成员变量存储在堆内存中的对象中，而局部变量存储在栈内存中。</span><br><span class="line"></span><br><span class="line">总之，成员变量和局部变量在定义位置、生命周期、访问权限、初始化和存储方式等方面都存在一些差异，需要根据具体需求选用合适的变量类型。</span><br><span class="line"></span><br><span class="line">### 面向对象的特性</span><br><span class="line"></span><br><span class="line">#### 封装的概念</span><br><span class="line"></span><br><span class="line">封装是面向对象编程中的一个重要概念，指的是将一个对象的属性和行为都隐藏在对象内部，对外只提供必要的接口，从而保证数据的安全性和代码的稳定性。</span><br><span class="line"></span><br><span class="line">封装包括两个方面：访问控制和方法暴露。访问控制指的是控制对象属性的访问权限，可以使用private、protected、public等关键字进行修饰，使得只有需要访问该属性的方法才能够访问它，其他方法或类无法访问。方法暴露则指的是设计良好的公共方法，通过这些公共方法来访问对象的属性和行为，而不是直接访问属性或调用对象的方法。</span><br><span class="line"></span><br><span class="line">封装的好处：</span><br><span class="line"></span><br><span class="line">1. 提高安全性：封装可以隐藏数据的实现细节，防止外部破坏或者误操作导致数据错误或程序故障。</span><br><span class="line"></span><br><span class="line">2. 简化编程：封装可以简化编程，封装对象的功能，减少代码复杂度，降低了编程的难度。</span><br><span class="line"></span><br><span class="line">3. 提高重用性：封装使得实现细节不可见，对外只提供接口，可以降低代码之间的耦合度，提高代码的重用性。</span><br><span class="line"></span><br><span class="line">4. 方便修改和维护：封装可以将变化隔离，使得修改某一部分的实现不影响其他部分，利于程序的维护。</span><br><span class="line"></span><br><span class="line">总之，封装是面向对象编程中很重要的概念，能够提高代码的质量和可维护性，使得程序更加健壮和可靠</span><br><span class="line"></span><br><span class="line">#### 继承的概念</span><br><span class="line"></span><br><span class="line">继承是面向对象编程中的一个重要概念，指的是基于已有类创建新类的过程。子类（派生类）可以继承父类（基类、超类）的属性和方法，同时还可以在此基础上添加新的属性和方法。</span><br><span class="line"></span><br><span class="line">继承是一种代码复用的机制，通过继承可以避免重复编写代码，提高了代码的可维护性和重用性。同时，继承也是实现多态的基础，在继承关系中，子类可以替换掉父类，从而产生不同的行为。</span><br><span class="line"></span><br><span class="line">在Java中，使用extends关键字来实现继承，例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    private String name;<br>    private int age;</p>
<pre><code>// 构造函数
public Person(String name, int age) &#123;
    this.name = name;
    this.age = age;
&#125;

// getter和setter方法
public String getName() &#123;
    return name;
&#125;

public void setName(String name) &#123;
    this.name = name;
&#125;

public int getAge() &#123;
    return age;
&#125;

public void setAge(int age) &#123;
    this.age = age;
&#125;
</code></pre>
<p>}</p>
<p>&#x2F;&#x2F; Student类继承自Person类<br>public class Student extends Person {<br>    private String school;</p>
<pre><code>// Student类的构造函数
public Student(String name, int age, String school) &#123;
    super(name, age); // 调用父类的构造函数
    this.school = school;
&#125;

// 新增的getter和setter方法
public String getSchool() &#123;
    return school;
&#125;

public void setSchool(String school) &#123;
    this.school = school;
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了一个Person类和一个Student类，Student类继承自Person类。在其中，Student类除了继承了Person类的所有属性和方法之外，还新增了一个school属性和相应的getter/setter方法。</span><br><span class="line"></span><br><span class="line">总之，继承是面向对象编程中很</span><br><span class="line"></span><br><span class="line">重要的概念，它可以帮助我们避免重复编写代码，提高代码的可维护性和重用性。在使用继承时，需要注意以下几点：</span><br><span class="line"></span><br><span class="line">1. 父类的属性和方法都是默认继承的，但是如果父类中有private属性或方法，则子类无法访问。</span><br><span class="line"></span><br><span class="line">2. 子类可以覆盖父类的方法，即在子类中重新定义与父类同名的方法，并改变其实现方式。如果子类没有覆盖父类的方法，则调用该方法时会优先调用父类的实现。</span><br><span class="line"></span><br><span class="line">3. 子类可以新增属性和方法，并且可以在父类的基础上进行拓展。但是，子类新增的属性和方法不能被父类所使用。</span><br><span class="line"></span><br><span class="line">4. 在Java中，一个子类只能继承一个父类，但是一个类可以被多个子类继承，形成多层继承关系。</span><br><span class="line"></span><br><span class="line">5. 可以使用super关键字来调用父类的构造函数、属性和方法。</span><br><span class="line"></span><br><span class="line">总之，继承是面向对象编程中很重要的概念，通过继承可以实现代码复用、拓展功能等目的，同时需要注意继承的规范和合理性。</span><br><span class="line"></span><br><span class="line">#### 多态的概念</span><br><span class="line"></span><br><span class="line">多态是面向对象编程中的一个重要概念，指的是同一种行为或操作，对不同的对象有不同的解释或实现方式。简单来说，多态就是同一个接口，不同的实现。</span><br><span class="line"></span><br><span class="line">在Java中，多态性通过继承和接口实现。当子类继承父类或者实现了某个接口时，可以使用父类或者接口的引用来引用子类对象，从而实现多态性。</span><br><span class="line"></span><br><span class="line">例如，定义了一个动物类Animal和两个子类Dog和Cat：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Animal {<br>    public void makeSound() {<br>        System.out.println(“动物发出声音”);<br>    }<br>}</p>
<p>public class Dog extends Animal {<br>    @Override<br>    public void makeSound() {<br>        System.out.println(“狗吠叫”);<br>    }<br>}</p>
<p>public class Cat extends Animal {<br>    @Override<br>    public void makeSound() {<br>        System.out.println(“猫喵喵叫”);<br>    }<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，所有的动物都可以发出声音，但是不同的动物有不同的叫声。这里使用方法重写（Override）实现了多态性。</span><br><span class="line"></span><br><span class="line">然后定义了一个测试类Test：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Test {<br>    public static void main(String[] args) {<br>        Animal animal1 &#x3D; new Dog();<br>        Animal animal2 &#x3D; new Cat();</p>
<pre><code>    animal1.makeSound(); // 输出：狗吠叫
    animal2.makeSound(); // 输出：猫喵喵叫
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了两个Animal类型的引用变量animal1和animal2，并分别引用了Dog和Cat对象。调用它们的makeSound方法时，会分别输出狗吠叫和猫喵喵叫，这就是多态性的体现。</span><br><span class="line"></span><br><span class="line">总之，多态是面向对象编程中很重要的概念，它可以提高代码的灵活性、可维护性和重用性。通过多态，我们可以以最小的代价完成代码的拓展和修改，同时也更容易实现代码的复用。在使用多态时需要注意以下几点：</span><br><span class="line"></span><br><span class="line">1. 多态是基于继承和重写实现的，子类必须重写父类的方法。</span><br><span class="line"></span><br><span class="line">2. 多态时，引用变量类型应该为父类或者接口类型，而不是具体的子类类型。</span><br><span class="line"></span><br><span class="line">3. 在运行时，由JVM动态地确定调用的方法，这就要求被调用的方法必须存在于对象的实际类型中。</span><br><span class="line"></span><br><span class="line">总之，多态是面向对象编程中很重要的概念，它可以提高代码的灵活性、可维护性和重用性，同时需要注意多态的规范和合理性。</span><br><span class="line"></span><br><span class="line">#### final关键字的使用</span><br><span class="line"></span><br><span class="line"> 在Java中，final是一个修饰符，可以用于修饰变量、方法和类。下面分别介绍final关键字的使用：</span><br><span class="line"></span><br><span class="line">1. final变量</span><br><span class="line"></span><br><span class="line">final变量是一种常量，即其值不能被修改。如果在声明时没有进行初始化，则必须在构造函数中对其赋值。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class MyClass {<br>    private final int MAX_VALUE &#x3D; 100;</p>
<pre><code>public MyClass() &#123;
    // 此处可以对MAX_VALUE重新赋值，但是只能赋值一次
&#125;
</code></pre>
<p>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，定义了一个final变量MAX_VALUE，它的值为100，无法被修改。</span><br><span class="line"></span><br><span class="line">2. final方法</span><br><span class="line"></span><br><span class="line">final方法是一种不能被重写的方法，即该方法在子类中不能被重写或覆盖。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public class Person {<br>    public final void sayHello() {<br>        System.out.println(“你好！”);<br>    }<br>}</p>
<p>public class Student extends Person {<br>    &#x2F;&#x2F; 无法重写Person类的sayHello方法<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，Person类的sayHello方法被定义为final方法，在Student类中无法进行重写。</span><br><span class="line"></span><br><span class="line">3. final类</span><br><span class="line"></span><br><span class="line">final类是一种不能被继承的类，即该类不能有子类。例如：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>public final class MyFinalClass {<br>    &#x2F;&#x2F; 类的内容<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">在上面的例子中，MyFinalClass类被定义为final类，无法被其他类所继承。</span><br><span class="line"></span><br><span class="line">总之，final关键字可以用于修饰变量、方法和类，分别表示常量、不能被重写或覆盖、不能被继承等含义。使用final关键字可以提高代码的安全性和稳定性，防止意外的修改或错误。</span><br><span class="line"></span><br><span class="line">#### Lambda表示式的使用</span><br><span class="line"></span><br><span class="line">Lambda表达式是Java 8引入的新特性，提供了一种更加简洁、高效的方式来编写代码。Lambda表达式可以看作是一种匿名方法，它没有名称、返回类型和修饰符等属性，但是可以被赋值给一个变量，也可以作为参数传递给方法。</span><br><span class="line"></span><br><span class="line">下面是Lambda表达式的基本语法：</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>(parameter1, parameter2, …) -&gt; {<br>    &#x2F;&#x2F; Lambda表达式的代码块<br>}</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">其中，parameter1、parameter2等表示Lambda表达式的参数列表，可以根据实际情况进行定义。箭头“-&gt;”表示Lambda表达式的分隔符，其前面是参数列表，后面是Lambda表达式的执行体，通常是一个代码块。</span><br><span class="line"></span><br><span class="line">Lambda表达式的使用需要满足以下两个条件：</span><br><span class="line"></span><br><span class="line">1. 函数接口：Lambda表达式只能作用于函数接口（Functional Interface），即只有一个抽象方法的接口。例如，Java中的Runnable和Comparator接口就是函数接口。</span><br><span class="line"></span><br><span class="line">2. 上下文推导：Lambda表达式的参数类型和返回类型可以由上下文自动推导出来，不需要显式地指定。</span><br><span class="line"></span><br><span class="line">下面是Lambda表达式的几个示例：</span><br><span class="line"></span><br><span class="line">1. 使用Lambda表达式实现Runnable接口：</span><br><span class="line">2. ```</span><br><span class="line">   Runnable r = () -&gt; &#123;</span><br><span class="line">       System.out.println(&quot;Hello, Lambda!&quot;);</span><br><span class="line">   &#125;;</span><br><span class="line">   Thread t = new Thread(r);</span><br><span class="line">   t.start();</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，使用Lambda表达式定义了一个Runnable对象r，并在其执行体中输出了一条信息。然后将该对象赋值给Thread类的构造方法，创建一个线程并启动。</p>
<ol start="2">
<li><p>使用Lambda表达式实现Comparator接口：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = Arrays.asList(&quot;apple&quot;, &quot;banana&quot;, &quot;orange&quot;);</span><br><span class="line">Collections.sort(list, (first, second) -&gt; first.length() - second.length());</span><br><span class="line">System.out.println(list);</span><br></pre></td></tr></table></figure></li>
</ol>
<p>在上面的例子中，使用Lambda表达式实现了Comparator接口的compare方法，根据字符串长度进行排序。使用Collections类的sort方法对字符串列表进行排序，并输出结果。</p>
<ol start="3">
<li>使用Lambda表达式作为参数：</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static void process(Runnable r) &#123;</span><br><span class="line">    r.run();</span><br><span class="line">&#125;</span><br><span class="line">process(() -&gt; System.out.println(&quot;Hello, Lambda!&quot;));</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，定义了一个process方法，该方法接收一个Runnable对象作为参数，并执行其run方法。然后使用Lambda表达式作为参数，输出一条信息。</p>
<p>总之，Lambda表达式是Java 8引入的新特性，可以提高代码的简洁度和可读性，适用于函数接口的简单实现、匿名内部类的替代等场景。在使用Lambda表达式时需要注意上下文推导和函数接口的限制。</p>
<h3 id="抽象类和接口"><a href="#抽象类和接口" class="headerlink" title="抽象类和接口"></a>抽象类和接口</h3><h4 id="抽象类和接口的使用"><a href="#抽象类和接口的使用" class="headerlink" title="抽象类和接口的使用"></a>抽象类和接口的使用</h4><p>抽象类和接口是Java中两种重要的概念，它们都可以用于实现多态性和代码复用。</p>
<ol>
<li>抽象类</li>
</ol>
<p>抽象类是一种不能被实例化的类，其主要作用是为子类提供一个模板或者共同的行为。抽象类通过abstract关键字进行定义，其中定义了至少一个抽象方法，即没有实现体的方法。子类必须实现这些抽象方法才能被实例化。</p>
<p>以下是抽象类的一个简单示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public abstract class Animal &#123;</span><br><span class="line">    public abstract void eat();</span><br><span class="line">    public abstract void move();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Dog extends Animal &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void eat() &#123;</span><br><span class="line">        System.out.println(&quot;狗在吃饭&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void move() &#123;</span><br><span class="line">        System.out.println(&quot;狗在奔跑&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，定义了一个抽象类Animal，其中包含了两个抽象方法eat和move，表示动物的基本行为。然后定义了一个Dog类，并继承自Animal类，实现了eat和move方法。</p>
<ol start="2">
<li>接口</li>
</ol>
<p>接口是一种定义行为的规范，它定义了一组方法（包括默认方法和静态方法），但是没有实现体。接口通过interface关键字进行定义，可以被多个类实现。一个类可以实现多个接口，从而获得不同的行为。</p>
<p>以下是接口的一个简单示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Shape &#123;</span><br><span class="line">    double getArea();</span><br><span class="line">    double getPerimeter();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Circle implements Shape &#123;</span><br><span class="line">    private double radius;</span><br><span class="line"></span><br><span class="line">    public Circle(double radius) &#123;</span><br><span class="line">        this.radius = radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public double getArea() &#123;</span><br><span class="line">        return Math.PI * radius * radius;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public double getPerimeter() &#123;</span><br><span class="line">        return 2 * Math.PI * radius;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，定义了一个Shape接口，其中定义了两个方法getArea和getPerimeter，表示形状的基本属性。然后定义了一个Circle类，并实现了Shape接口，实现了getArea和getPerimeter方法。</p>
<p>总之，抽象类和接口都是Java中重要的概念，它们可以用于实现多态性和代码复用。使用抽象类和接口需要注意以下几点：</p>
<ol>
<li><p>抽象类和接口都不能被实例化，只能被子类或者实现类继承或实现。</p>
</li>
<li><p>抽象类可以包含非抽象方法，而接口只能包含抽象方法、默认方法和静态方法。</p>
</li>
<li><p>子类必须实现抽象类中的所有抽象方法，否则子类也必须被声明为抽象类。实现类必须实现接口中的所有方法。</p>
</li>
<li><p>一个类可以继承一个抽象类并实现多个接口。</p>
</li>
<li><p>接口中的方法默认是public和abstract修饰的，而抽象类中的方法可以有不同的访问控制符和非抽象方法。</p>
</li>
</ol>
<p>总之，抽象类和接口都是面向对象编程中很重要的概念，它们可以帮助我们实现多态和代码复用等目的，同时需要注意抽象类和接口的规范和合理性。</p>
<h4 id="Java中的内部类"><a href="#Java中的内部类" class="headerlink" title="Java中的内部类"></a>Java中的内部类</h4><p>1.成员内部类是Java中的一种内部类，它定义在外部类的内部，并与外部类的成员变量和方法具有相同的访问权限。一个成员内部类可以使用public、protected、private或默认修饰符。</p>
<p>成员内部类可以访问外部类的私有成员和方法，而外部类也可以通过创建成员内部类对象来访问内部类的成员。成员内部类不能定义static变量或方法，但可以包含静态final常量。</p>
<p>要创建成员内部类对象，必须先创建外部类对象，然后使用外部类对象来创建内部类对象。例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class OuterClass &#123;</span><br><span class="line">    private int outerVar;</span><br><span class="line"></span><br><span class="line">    public class InnerClass &#123;</span><br><span class="line">        private int innerVar;</span><br><span class="line"></span><br><span class="line">        public InnerClass(int var) &#123;</span><br><span class="line">            this.innerVar = var;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public void print() &#123;</span><br><span class="line">            System.out.println(&quot;Outer variable: &quot; + outerVar);</span><br><span class="line">            System.out.println(&quot;Inner variable: &quot; + this.innerVar);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void test() &#123;</span><br><span class="line">        InnerClass inner = new InnerClass(10);</span><br><span class="line">        inner.print();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们定义了一个OuterClass外部类和一个InnerClass成员内部类。在test方法中，我们创建了一个InnerClass对象inner，并调用了它的print方法来打印outerVar和innerVar。注意，在InnerClass类中，我们可以直接访问outerVar变量。</p>
<p>2.静态内部类是Java中的一种内部类，它定义在外部类的内部，但必须使用static关键字修饰。静态内部类只能访问外部类的静态成员和方法，不能访问非静态成员。</p>
<p>静态内部类可以包含静态变量、静态方法和非静态成员变量，但不能包含非静态方法。静态内部类可以使用public、protected、private或默认修饰符。</p>
<p>要创建静态内部类对象，不需要先创建外部类对象。可以直接使用”外部类名.内部类名”的形式来创建静态内部类对象，例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class OuterClass &#123;</span><br><span class="line">    private static int outerVar;</span><br><span class="line"></span><br><span class="line">    public static class InnerClass &#123;</span><br><span class="line">        private int innerVar;</span><br><span class="line"></span><br><span class="line">        public InnerClass(int var) &#123;</span><br><span class="line">            this.innerVar = var;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public void print() &#123;</span><br><span class="line">            System.out.println(&quot;Outer variable: &quot; + outerVar);</span><br><span class="line">            System.out.println(&quot;Inner variable: &quot; + this.innerVar);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 创建静态内部类对象</span><br><span class="line">OuterClass.InnerClass inner = new OuterClass.InnerClass(10);</span><br><span class="line">inner.print();</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们定义了一个OuterClass外部类和一个InnerClass静态内部类。注意，在InnerClass类中，我们只能访问outerVar静态变量。我们可以直接使用”OuterClass.InnerClass”的形式来创建静态内部类对象，而不需要先创建OuterClass对象。</p>
<p>3.局部内部类是Java中的一种内部类，它定义在方法或作用域块内部的类，只在定义它的方法或代码块内可见。局部内部类不能使用public、protected、private或static修饰符。</p>
<p>局部内部类可以访问外部类和外部方法（或代码块）的局部变量，但这些局部变量必须声明为final类型或者隐式地被final修饰。</p>
<p>要创建局部内部类对象，需要先创建外部类对象，然后通过外部类对象来创建内部类对象。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class OuterClass &#123;</span><br><span class="line">    public void test() &#123;</span><br><span class="line">        final int outerVar = 10;</span><br><span class="line"></span><br><span class="line">        class InnerClass &#123;</span><br><span class="line">            private int innerVar;</span><br><span class="line"></span><br><span class="line">            public InnerClass(int var) &#123;</span><br><span class="line">                this.innerVar = var;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            public void print() &#123;</span><br><span class="line">                System.out.println(&quot;Outer variable: &quot; + outerVar);</span><br><span class="line">                System.out.println(&quot;Inner variable: &quot; + this.innerVar);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        InnerClass inner = new InnerClass(20);</span><br><span class="line">        inner.print();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们定义了一个OuterClass外部类和一个InnerClass局部内部类。在test方法中，我们创建了一个InnerClass对象inner，并调用了它的print方法来打印outerVar和innerVar。注意，在InnerClass类中，我们可以直接访问outerVar变量，但必须将它声明为final类型。</p>
<p>4.匿名内部类是Java中的一种内部类，它没有类名，通常用于简化代码。匿名内部类可以直接继承一个父类或实现一个接口。</p>
<p>匿名内部类不能有构造方法，但可以在定义时直接实例化并重写其中的方法。匿名内部类只能使用一次，因为无法对它进行引用，所以通常用于创建临时对象。匿名内部类也可以访问外部类的成员变量和方法。</p>
<p>例如，我们可以通过继承Thread类来创建一个匿名内部类：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Thread thread = new Thread() &#123;</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;Hello, world!&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line">thread.start();</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们创建了一个Thread类的匿名内部类，并实现了它的run方法。然后我们调用了start方法来启动线程。注意，在匿名内部类中，我们可以直接访问外部类的成员变量和方法。</p>
<h4 id="单例模式（了解）"><a href="#单例模式（了解）" class="headerlink" title="单例模式（了解）"></a>单例模式（了解）</h4><p>单例模式是一种常用的设计模式，它保证一个类只有一个实例，并提供一个全局访问点。</p>
<p>单例模式通常有两种实现方式：</p>
<ol>
<li><p>饿汉式单例模式：在类加载时就创建实例对象，并提供一个公共的静态方法来获取这个唯一的实例。这种方式实现简单，线程安全，但可能会浪费系统资源。</p>
</li>
<li><p>懒汉式单例模式：在第一次使用时才创建实例对象，并提供一个公共的静态方法来获取这个唯一的实例。这种方式可以节省系统资源，但需要考虑线程安全问题。<br>下面是饿汉式单例模式的示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">    private static final Singleton instance = new Singleton();</span><br><span class="line"></span><br><span class="line">    private Singleton() &#123;&#125;</span><br><span class="line"></span><br><span class="line">    public static Singleton getInstance() &#123;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ol>
<p>在上面的例子中，我们将构造函数设为私有的，然后在类加载时就创建了一个Singleton实例，并提供了一个公共的静态方法getInstance来获取这个唯一的实例。由于instance被定义为final类型，所以它只能被赋值一次，从而保证了单例模式的实现。需要注意的是，在多线程环境下，饿汉式单例模式的实现是线程安全的，因为在类加载时就已经创建了实例对象，不需要考虑线程同步问题。</p>
<p>下面是懒汉式单例模式的示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">    private static Singleton instance = null;</span><br><span class="line"></span><br><span class="line">    private Singleton() &#123;&#125;</span><br><span class="line"></span><br><span class="line">    public static synchronized Singleton getInstance() &#123;</span><br><span class="line">        if (instance == null) &#123;</span><br><span class="line">            instance = new Singleton();</span><br><span class="line">        &#125;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们将构造函数设为私有的，然后在getInstance方法中判断是否已经创建了Singleton实例，如果没有则创建一个实例并返回。由于getInstance方法被定义为synchronized类型，所以它能保证在多线程环境下只有一个线程能够进入该方法，从而避免了多个线程同时创建实例的问题。</p>
<p>需要注意的是，在高并发场景下，懒汉式单例模式的性能可能会受到影响。可以考虑使用双重检查锁定（Double-Checked Locking）或静态内部类等方式来优化单例模式的性能</p>
<h4 id="模板设计方法（了解）"><a href="#模板设计方法（了解）" class="headerlink" title="模板设计方法（了解）"></a>模板设计方法（了解）</h4><p>模板方法设计模式（Template Method Design Pattern）是一种基于继承的设计模式，它定义了一个算法框架，把一些步骤的实现留给子类来完成。</p>
<p>模板方法设计模式通常由两部分组成：抽象父类和具体子类。抽象父类提供了一个算法框架，其中包含了若干个抽象方法，这些抽象方法由子类来实现。具体子类继承了抽象父类，并重写其中的抽象方法，从而完成了算法中的具体步骤。</p>
<p>下面是模板方法设计模式的示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public abstract class AbstractClass &#123;</span><br><span class="line">    public void templateMethod() &#123;</span><br><span class="line">        step1();</span><br><span class="line">        step2();</span><br><span class="line">        step3();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    protected abstract void step1();</span><br><span class="line"></span><br><span class="line">    protected abstract void step2();</span><br><span class="line"></span><br><span class="line">    protected abstract void step3();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class ConcreteClass extends AbstractClass &#123;</span><br><span class="line">    protected void step1() &#123;</span><br><span class="line">        System.out.println(&quot;ConcreteClass: step1&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    protected void step2() &#123;</span><br><span class="line">        System.out.println(&quot;ConcreteClass: step2&quot;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    protected void step3() &#123;</span><br><span class="line">        System.out.println(&quot;ConcreteClass: step3&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Main &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        AbstractClass obj = new ConcreteClass();</span><br><span class="line">        obj.templateMethod();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们定义了一个抽象类AbstractClass和一个具体子类ConcreteClass。在AbstractClass中，我们提供了一个templateMethod方法作为算法框架，并定义了三个抽象方法step1、step2和step3，这些抽象方法由具体子类来实现。在具体子类ConcreteClass中，我们重写了其中的三个抽象方法，并实现了具体的算法步骤。最后，在Main函数中，我们创建了ConcreteClass对象，并调用了它的templateMethod方法来执行算法。</p>
<p>需要注意的是，在模板方法设计模式中，抽象父类控制算法的流程和逻辑，而具体子类负责实现具体的步骤。这样可以避免在具体子类中重复编写相同的代码，提高了代码的复用性。</p>
<h3 id="java异常"><a href="#java异常" class="headerlink" title="java异常"></a>java异常</h3><h4 id="异常的概念"><a href="#异常的概念" class="headerlink" title="异常的概念"></a>异常的概念</h4><p>在Java中，异常（Exception）是程序运行时发生的意外情况，如：数组下标越界、空指针引用、文件不存在等。如果不加处理，这些异常可能会导致程序崩溃。</p>
<p>为了更好地处理异常，Java提供了异常处理机制。当程序运行时遇到异常，它会抛出一个异常对象，如果没有对这个异常进行处理，程序就会终止执行。可以使用try-catch语句来捕获并处理异常。try块中放置可能会引发异常的代码，catch块用于处理异常，通常包含错误信息的输出或者异常的修复代码。finally块中放置无论是否发生异常都需要执行的代码。</p>
<p><img src="https://img-blog.csdnimg.cn/548f0ac51a2c413a9032552d8603ea0f.png" alt="548f0ac51a2c413a9032552d8603ea0f"></p>
<p>在Java中，异常分为两种：受检异常（Checked Exception）和非受检异常（Unchecked Exception）。受检异常必须在方法签名中显式地声明，并且必须被捕获或者抛出，否则编译器将会报错。非受检异常通常由程序员编写代码时犯的逻辑错误或者系统环境的问题导致，如NullPointerException、ArrayIndexOutOfBoundsException等，编译器不会强制要求进行处理。</p>
<p>例如，以下是一个简单的异常处理示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Example &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            int num1 = Integer.parseInt(args[0]);</span><br><span class="line">            int num2 = Integer.parseInt(args[1]);</span><br><span class="line">            int result = num1 / num2;</span><br><span class="line">            System.out.println(&quot;Result: &quot; + result);</span><br><span class="line">        &#125; catch (ArithmeticException e) &#123;</span><br><span class="line">            System.out.println(&quot;Error: &quot; + e.getMessage());</span><br><span class="line">        &#125; catch (NumberFormatException e) &#123;</span><br><span class="line">            System.out.println(&quot;Error: &quot; + e.getMessage());</span><br><span class="line">        &#125; catch (ArrayIndexOutOfBoundsException e) &#123;</span><br><span class="line">            System.out.println(&quot;Error: &quot; + e.getMessage());</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            System.out.println(&quot;End of program&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们尝试将命令行参数转换为整数，并计算它们的商。如果遇到异常，就通过catch块输出错误信息。无论是否发生异常，finally块中的代码都会被执行。</p>
<h4 id="异常的处理机制"><a href="#异常的处理机制" class="headerlink" title="异常的处理机制"></a>异常的处理机制</h4><h5 id="try-catch-finally"><a href="#try-catch-finally" class="headerlink" title="try-catch-finally"></a>try-catch-finally</h5><p>Java中的异常处理机制可以通过 try-catch-finally 语句块来实现。try块用于放置可能会抛出异常的代码，catch块用于捕获并处理抛出的异常，finally块用于放置无论是否发生异常都需要执行的代码。</p>
<p>下面是一个使用try-catch-finally语句块处理异常的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Example &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            // 可能会抛出异常的代码</span><br><span class="line">            int result = divide(10, 0);</span><br><span class="line">            System.out.println(&quot;Result: &quot; + result);</span><br><span class="line">        &#125; catch (ArithmeticException e) &#123;</span><br><span class="line">            // 捕获并处理异常</span><br><span class="line">            System.out.println(&quot;Error: &quot; + e.getMessage());</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            // 无论是否异常都会执行的代码</span><br><span class="line">            System.out.println(&quot;End of program&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static int divide(int a, int b) &#123;</span><br><span class="line">        return a / b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们定义了一个divide方法来计算两个整数的商，这个方法可能会抛出ArithmeticException异常（例如，被除数为0）。在main函数中，我们调用divide方法，并通过try-catch-finally语句块来捕获并处理可能抛出的异常。无论是否发生异常，finally块中的代码都会被执行。</p>
<p>需要注意的是，异常分为受检异常（Checked Exception）和非受检异常（Unchecked Exception）。对于受检异常，在方法签名中必须显式地声明，并且必须被捕获或者抛出；对于非受检异常，编译器不会强制要求进行处理。</p>
<p>throws和throw都与Java中的异常处理有关，但用法略有不同。</p>
<h5 id="throws"><a href="#throws" class="headerlink" title="throws"></a>throws</h5><p>throws是用于声明方法可能会抛出哪些异常。在Java中，当一个方法可能会抛出某些受检查异常时，应该在方法声明中使用throws关键字来<strong>声明这些异常</strong>，以便调用者知道需要捕获和处理哪些异常。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public void doSomething() throws IOException, SQLException &#123;</span><br><span class="line">    // ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的方法声明表示doSomething()方法可能会抛出IOException或SQLException异常，调用者必须在调用该方法时进行异常捕获或继续声明这些异常。</p>
<h5 id="throw"><a href="#throw" class="headerlink" title="throw"></a>throw</h5><p>throw语句用于手动抛出一个异常。在Java中，当程序遇到某些错误或异常情况时，可以使用throw语句抛出相应的异常对象，以提示程序出现了错误。例如：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">if (age &lt; 0) &#123;</span><br><span class="line">    throw new IllegalArgumentException(&quot;Age cannot be negative!&quot;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的代码表示如果年龄小于0，则抛出IllegalArgumentException异常，并附带一个错误信息。</p>
<p>需要注意的是，当使用throw抛出异常时，程序会立即停止执行当前方法，并从当前方法返回，而不会继续执行后面的代码。因此，在实际开发中，应该谨慎使用throw语句，确保只在必要的情况下才抛出异常。</p>
<p>throws和throw是两个在Java中用于异常处理的关键字，它们有以下区别：</p>
<ol>
<li>用法不同</li>
</ol>
<p>throws是用于方法声明中，用于声明该方法可能会抛出哪些受检查异常；而throw是用于方法体中，用于手动抛出一个异常对象。</p>
<ol start="2">
<li>作用不同</li>
</ol>
<p>throws主要用于告知调用者可能会发生的异常情况，以便让调用者进行相应的异常处理；而throw则是在具体的方法实现中，用于手动抛出异常对象，并中断当前方法的执行流程。</p>
<ol start="3">
<li>处理方式不同</li>
</ol>
<p>当方法使用throws声明了某些受检查异常时，调用该方法的代码必须对这些异常进行捕获或继续向上层抛出；而当方法在执行过程中使用throw抛出了某个异常时，程序会立即停止执行当前方法，并从当前方法返回，继续执行上层方法或结束程序运行。</p>
<p>需要注意的是，虽然throws和throw都与Java中的异常处理有关，但它们并不是完全互斥的。实际上，在处理异常时，我们通常会同时使用到它们，例如在一个方法中先使用try-catch语句捕获异常，然后再使用throw将异常抛出到更高层次的调用者，最终由这些调用者统一处理异常。</p>
<h4 id="自定义异常的使用"><a href="#自定义异常的使用" class="headerlink" title="自定义异常的使用"></a>自定义异常的使用</h4><p>在Java中，我们可以通过继承Exception或者RuntimeException类来创建自定义异常。自定义异常适用于需要捕获并处理特定类型的异常情况的场景。</p>
<p>下面是一个自定义异常的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class CustomException extends Exception &#123;</span><br><span class="line">    public CustomException(String message) &#123;</span><br><span class="line">        super(message);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public CustomException(String message, Throwable cause) &#123;</span><br><span class="line">        super(message, cause);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们创建了一个CustomException类，它继承自Exception类。通过重写Exception类的构造函数，我们为CustomException类提供了两个构造函数，分别用于设置异常信息和异常原因（可选）。</p>
<p>下面是一个使用自定义异常的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Example &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        try &#123;</span><br><span class="line">            // 可能会抛出自定义异常的代码</span><br><span class="line">            int result = divide(10, 0);</span><br><span class="line">            System.out.println(&quot;Result: &quot; + result);</span><br><span class="line">        &#125; catch (CustomException e) &#123;</span><br><span class="line">            // 捕获并处理自定义异常</span><br><span class="line">            System.out.println(&quot;Error: &quot; + e.getMessage());</span><br><span class="line">        &#125; finally &#123;</span><br><span class="line">            System.out.println(&quot;End of program&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static int divide(int a, int b) throws CustomException &#123;</span><br><span class="line">        if (b == 0) &#123;</span><br><span class="line">            throw new CustomException(&quot;除数不能为0&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">        return a / b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在上面的例子中，我们通过throw关键字抛出了一个自定义异常CustomException，并在divide方法的声明中使用throws关键字将这个异常向上抛出。在main函数中，我们通过try-catch-finally语句块来捕获并处理可能抛出的CustomException异常。</p>
<p>需要注意的是，在自定义异常时，通常应该定义有意义的异常信息，并考虑继承现有的异常类型，以便更好地利用异常处理机制。</p>
<h3 id="java常用类"><a href="#java常用类" class="headerlink" title="java常用类"></a>java常用类</h3><h4 id="字符串相关类的使用"><a href="#字符串相关类的使用" class="headerlink" title="字符串相关类的使用"></a>字符串相关类的使用</h4><p>在Java中，字符串相关的类有很多，比较常用的包括String、StringBuilder和StringBuffer等。</p>
<ol>
<li>String类</li>
</ol>
<p>String类是Java中用于表示字符串的类，它是不可变的（immutable）类。这意味着一旦创建了一个String对象，就不能更改该对象的值。由于String对象的不可变性，每当对String对象进行修改时，都会创建一个新的String对象，并将修改后的字符串赋值给新的对象。</p>
<p>下面是一些常用的String类方法：</p>
<ul>
<li>length()：返回字符串的长度。</li>
<li>charAt(int index)：返回指定位置的字符。</li>
<li>substring(int beginIndex, int endIndex)：返回从beginIndex开始到endIndex-1结束的子字符串。</li>
<li>equals(Object obj)：判断字符串是否相等。</li>
<li>toUpperCase()：将字符串转换为大写字母。</li>
<li>toLowerCase()：将字符串转换为小写字母。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String str1 = &quot;Hello&quot;;</span><br><span class="line">String str2 = &quot;World&quot;;</span><br><span class="line">String str3 = str1 + str2; // 拼接字符串</span><br><span class="line">System.out.println(str3); // 输出 HelloWorld</span><br><span class="line">System.out.println(str3.substring(0, 5)); // 输出 Hello</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>StringBuilder类</li>
</ol>
<p>StringBuilder类是Java中用于处理字符串的可变类，它提供了许多修改字符串的方法，如添加、插入、删除、替换等操作。与String类不同，StringBuilder类的对象可以直接进行修改，而不需要创建新的对象。</p>
<p>下面是一些常用的StringBuilder类方法：</p>
<ul>
<li>append(String str)：向字符串末尾添加内容。</li>
<li>insert(int offset, String str)：在指定位置插入内容。</li>
<li>delete(int start, int end)：删除指定范围内的内容。</li>
<li>replace(int start, int end, String str)：替换指定范围内的内容。</li>
<li>reverse()：反转字符串。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">StringBuilder builder = new StringBuilder(&quot;Hello&quot;);</span><br><span class="line">builder.append(&quot;, World!&quot;);</span><br><span class="line">builder.insert(5, &quot;Java &quot;);</span><br><span class="line">builder.delete(1, 4);</span><br><span class="line">builder.replace(6, 11, &quot;GPT&quot;);</span><br><span class="line">builder.reverse();</span><br><span class="line">System.out.println(builder.toString()); // 输出TPG avaj olleH,!</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>StringBuffer类</li>
</ol>
<p>StringBuffer类与StringBuilder类类似，也是可变的字符串类，提供了许多修改字符串的方法。与StringBuilder类不同的是，StringBuffer类是线程安全的，适用于多线程环境下的字符串操作。但是，在单线程环境下，StringBuilder类比StringBuffer类的性能更好。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">StringBuffer buffer = new StringBuffer(&quot;Hello&quot;);</span><br><span class="line">buffer.append(&quot;, World!&quot;);</span><br><span class="line">buffer.insert(5, &quot;Java &quot;);</span><br><span class="line">buffer.delete(1, 4);</span><br><span class="line">buffer.replace(6, 11, &quot;GPT&quot;);</span><br><span class="line">buffer.reverse();</span><br><span class="line">System.out.println(buffer.toString()); // 输出TPG avaj olleH,!</span><br></pre></td></tr></table></figure>

<p>需要注意的是，当需要频繁地对字符串进行修改时，应该使用StringBuilder或StringBuffer类，而不是String类。如果字符串不需要修改，那么可以使用String类来代替StringBuilder和StringBuffer，因为String类更加简洁、易读</p>
<h4 id="System类与Runtime类的使用；"><a href="#System类与Runtime类的使用；" class="headerlink" title="System类与Runtime类的使用；"></a>System类与Runtime类的使用；</h4><ol>
<li>System类</li>
</ol>
<p>System类提供了一些有用的类变量和方法，如：</p>
<ul>
<li>out：标准输出流。</li>
<li>err：标准错误流。</li>
<li>currentTimeMillis()：返回当前时间的毫秒数。</li>
<li>getProperty(String key)：获取指定系统属性的值。</li>
<li>arraycopy(Object src, int srcPos, Object dest, int destPos, int length)：将一个数组中的一部分复制到另一个数组中。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">long start = System.currentTimeMillis();</span><br><span class="line">// 执行一些操作</span><br><span class="line">long end = System.currentTimeMillis();</span><br><span class="line">System.out.println(&quot;耗时：&quot; + (end - start) + &quot; 毫秒&quot;);</span><br><span class="line"></span><br><span class="line">String osName = System.getProperty(&quot;os.name&quot;);</span><br><span class="line">System.out.println(&quot;操作系统：&quot; + osName);</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Runtime类</li>
</ol>
<p>Runtime类提供了与Java虚拟机运行时环境相关的方法，如：</p>
<ul>
<li>exec(String cmd)：在单独的进程中执行命令。</li>
<li>totalMemory()：JVM总内存量。</li>
<li>freeMemory()：JVM空闲内存量。</li>
<li>maxMemory()：JVM最大可用内存量。</li>
<li>gc()：垃圾回收器。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Runtime runtime = Runtime.getRuntime();</span><br><span class="line">try &#123;</span><br><span class="line">    // 在单独的进程中执行命令</span><br><span class="line">    Process process = runtime.exec(&quot;notepad.exe&quot;);</span><br><span class="line">&#125; catch (IOException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">System.out.println(&quot;JVM总内存量：&quot; + runtime.totalMemory());</span><br><span class="line">System.out.println(&quot;JVM空闲内存量：&quot; + runtime.freeMemory());</span><br><span class="line">System.out.println(&quot;JVM最大可用内存量：&quot; + runtime.maxMemory());</span><br><span class="line"></span><br><span class="line">runtime.gc(); // 手动触发垃圾回收器</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用exec方法时，需要注意处理异常和流，以避免出现死锁或者内存泄漏等问题；在使用Runtime类的方法时，应该小心谨慎地管理内存，避免出现OutOfMemoryError等问题。</p>
<h4 id="Math类与Random类的使用"><a href="#Math类与Random类的使用" class="headerlink" title="Math类与Random类的使用"></a>Math类与Random类的使用</h4><p>在Java中，Math类和Random类都是Java标准库提供的数学相关类。它们可以用来执行一些数学计算、生成随机数等。</p>
<ol>
<li>Math类</li>
</ol>
<p>Math类提供了许多数学相关的静态方法，如：</p>
<ul>
<li>abs(int a)：返回一个整数的绝对值。</li>
<li>ceil(double a)：返回大于或等于指定 double 值的最小 double 值，等于一个整数。</li>
<li>floor(double a)：返回小于或等于指定 double 值的最大 double 值，等于一个整数。</li>
<li>max(int a, int b)：返回两个整数中较大的那个。</li>
<li>min(int a, int b)：返回两个整数中较小的那个。</li>
<li>pow(double a, double b)：返回第一个参数的第二个参数次幂的值。</li>
<li>random()：返回一个介于0.0和1.0之间的随机数。</li>
<li>round(float a)：将一个float值舍入为最接近的整数。</li>
<li>sqrt(double a)：返回平方根。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int a = -10;</span><br><span class="line">double b = 3.14;</span><br><span class="line">System.out.println(Math.abs(a)); // 输出 10</span><br><span class="line">System.out.println(Math.ceil(b)); // 输出 4.0</span><br><span class="line">System.out.println(Math.floor(b)); // 输出 3.0</span><br><span class="line">System.out.println(Math.max(2, 5)); // 输出 5</span><br><span class="line">System.out.println(Math.min(2, 5)); // 输出 2</span><br><span class="line">System.out.println(Math.pow(2, 3)); // 输出 8.0</span><br><span class="line">System.out.println(Math.random()); // 输出介于0.0和1.0之间的随机数</span><br><span class="line">System.out.println(Math.round(3.6f)); // 输出 4</span><br><span class="line">System.out.println(Math.sqrt(9)); // 输出 3.0</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Random类</li>
</ol>
<p>Random类提供了生成伪随机数的方法，包括nextInt、nextLong、nextDouble等方法。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Random random = new Random();</span><br><span class="line">System.out.println(random.nextInt(100)); // 输出小于100的随机整数</span><br><span class="line">System.out.println(random.nextDouble()); // 输出小于1.0的随机浮点数</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在生成随机数时，应该使用真正的随机数生成器，而不是伪随机数生成器，以避免出现安全问题。例如，在密码生成、加密解密等场景下，应该使用更加安全的随机数生成器。</p>
<h4 id="日期类的使用"><a href="#日期类的使用" class="headerlink" title="日期类的使用"></a>日期类的使用</h4><p>在Java中，日期相关的类主要包括Date、Calendar和SimpleDateFormat等。其中，Date类是Java早期提供的日期类，已经过时，建议使用Calendar或者LocalDate代替。</p>
<ol>
<li>LocalDate类</li>
</ol>
<p>LocalDate类是Java 8之后提供的日期类，它提供了许多便捷的方法来操作日期，如：</p>
<ul>
<li>now()：获取当前日期。</li>
<li>of(int year, int month, int dayOfMonth)：创建指定年月日的日期。</li>
<li>plusDays(long daysToAdd)：增加几天。</li>
<li>minusDays(long daysToSubtract)：减少几天。</li>
<li>getYear()：获取年份。</li>
<li>getMonth()：获取月份。</li>
<li>getDayOfMonth()：获取日份。</li>
<li>format(DateTimeFormatter formatter)：将日期格式化为指定的字符串。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">LocalDate date = LocalDate.now();</span><br><span class="line">System.out.println(date); // 输出当前日期</span><br><span class="line">date = LocalDate.of(2023, 3, 12);</span><br><span class="line">System.out.println(date); // 输出指定日期</span><br><span class="line">date = date.plusDays(1);</span><br><span class="line">System.out.println(date); // 输出增加一天后的日期</span><br><span class="line">date = date.minusDays(1);</span><br><span class="line">System.out.println(date); // 输出减少一天后的日期</span><br><span class="line">System.out.println(date.getYear()); // 输出年份</span><br><span class="line">System.out.println(date.getMonth()); // 输出月份</span><br><span class="line">System.out.println(date.getDayOfMonth()); // 输出日份</span><br><span class="line"></span><br><span class="line">DateTimeFormatter formatter = DateTimeFormatter.ofPattern(&quot;yyyy-MM-dd&quot;);</span><br><span class="line">String strDate = date.format(formatter);</span><br><span class="line">System.out.println(strDate); // 输出格式化后的日期字符串</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>SimpleDateFormat类</li>
</ol>
<p>SimpleDateFormat类是Java提供的日期格式化工具类，可以根据指定的格式将日期转换为字符串或者将字符串转换为日期。其中，常用的格式符有：</p>
<ul>
<li>y：年份（4位数）。</li>
<li>M：月份（1~12）。</li>
<li>d：日期（1~31）。</li>
<li>H：小时（0~23）。</li>
<li>m：分钟（0~59）。</li>
<li>s：秒钟（0~59）。</li>
<li>S：毫秒（0~999）。</li>
</ul>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Date date = new Date();</span><br><span class="line">SimpleDateFormat sdf = new SimpleDateFormat(&quot;yyyy年MM月dd日 HH:mm:ss&quot;);</span><br><span class="line">String strDate = sdf.format(date);</span><br><span class="line">System.out.println(strDate); // 输出格式化后的日期字符串</span><br><span class="line"></span><br><span class="line">strDate = &quot;2023-03-12&quot;;</span><br><span class="line">sdf.applyPattern(&quot;yyyy-MM-dd&quot;);</span><br><span class="line">date = sdf.parse(strDate);</span><br><span class="line">System.out.println(date); // 输出解析后的日期对象</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在处理日期时，应该尽可能使用新的日期类（如LocalDate）代替旧的日期类（如Date），以避免出现线程安全问题和易错问题。同时，在进行日期格式化时，应该使用正确的格式符，以避免出现异常。</p>
<h3 id="集合框架"><a href="#集合框架" class="headerlink" title="集合框架"></a>集合框架</h3><h4 id="List、Map、Set集合的使用"><a href="#List、Map、Set集合的使用" class="headerlink" title="List、Map、Set集合的使用"></a>List、Map、Set集合的使用</h4><ol>
<li>List类</li>
</ol>
<p>List是一种有序的集合，可以存储重复的元素。常用的List实现类有ArrayList和LinkedList。其中，ArrayList是基于动态数组实现的，支持随机访问，而LinkedList是基于双向链表实现的，支持高效的插入和删除操作。</p>
<p>用的List实现类有：</p>
<ul>
<li><p>ArrayList：基于数组实现的动态数组，支持快速随机访问和插入&#x2F;删除元素等操作，但在插入&#x2F;删除元素时需要移动后面的所有元素，因此效率较低。</p>
</li>
<li><p>LinkedList：基于链表实现的双向链表，支持快速在任意位置进行插入&#x2F;删除操作，但只能顺序访问元素，且每个节点需要额外存储指向前驱节点和后继节点的引用，因此比ArrayList更占用内存空间。</p>
</li>
<li><p>Vector：与ArrayList类似，也是基于数组实现的动态数组，但线程安全，具有同步锁，适合在多线程环境下使用，但效率较低。<br><img src="file:///C:/Users/26927/Pictures/Typedown/6dda1686-b38e-4a88-b7b9-cdf767405545.png" alt="6dda1686-b38e-4a88-b7b9-cdf767405545"></p>
</li>
</ul>
<p>除了上述实现类之外，还有一些其他的List实现类，如Stack、CopyOnWriteArrayList等，都继承自List接口，并提供了特定的功能。</p>
<p>List接口提供了很多常用的方法，例如：add()、remove()、get()、set()、indexOf()、size()等，它们都遵循了Java集合框架的规范。在使用List时，建议先根据具体需求选择合适的实现类，再根据需要选择合适的方法进行操作。</p>
<p><img src="file:///C:/Users/26927/Pictures/Typedown/519ae451-a421-4cb6-a575-ff46187487c4.png" alt="519ae451-a421-4cb6-a575-ff46187487c4"></p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(&quot;apple&quot;);</span><br><span class="line">list.add(&quot;banana&quot;);</span><br><span class="line">list.add(&quot;orange&quot;);</span><br><span class="line">System.out.println(list); // 输出 [apple, banana, orange]</span><br><span class="line"></span><br><span class="line">list.remove(1);</span><br><span class="line">System.out.println(list); // 输出 [apple, orange]</span><br><span class="line"></span><br><span class="line">System.out.println(list.get(1)); // 输出 orange</span><br></pre></td></tr></table></figure>

<p>2.Set类</p>
<p>Set是一种无序的集合，不允许重复元素。常用的Set实现类有HashSet和TreeSet。其中，HashSet是基于哈希表实现的，支持快速查找，而TreeSet是基于红黑树实现的，支持按元素排序和范围查询。</p>
<p>常用的Set实现类有：</p>
<ul>
<li>HashSet：基于哈希表实现的Set，支持快速的插入和查找操作，但不保证元素的顺序。<img src="file:///C:/Users/26927/Pictures/Typedown/2be7d61a-f513-4e5d-aa4b-ccf3c16e1cfd.png?msec=1681038201688" alt="2be7d61af5134e5daa4bccf3c16e1cfd"></li>
<li>TreeSet：基于红黑树实现的有序Set，支持按照元素进行排序，但插入和查询操作的效率较低。</li>
<li>LinkedHashSet：基于哈希表和双向链表实现的Set，可以按照插入顺序或者访问顺序进行迭代，其性能介于HashSet和TreeSet之间。</li>
</ul>
<p>除了上述实现类之外，还有一些其他的Set实现类，如EnumSet、CopyOnWriteArraySet等，它们都继承自Set接口，并提供了特定的功能。</p>
<p>Set接口提供了很多常用的方法，例如：add()、remove()、contains()、size()、isEmpty()等，它们都遵循了Java集合框架的规范。在使用Set时，建议先根据具体需求选择合适的实现类，再根据需要选择合适的方法进行操作。</p>
<p>示例代码：<br>    Set<String> set &#x3D; new HashSet&lt;&gt;();<br>    set.add(“apple”);<br>    set.add(“banana”);<br>    set.add(“orange”);<br>    System.out.println(set); &#x2F;&#x2F; 输出 [orange, apple, banana]<br>    set.remove(“banana”);<br>    System.out.println(set); &#x2F;&#x2F; 输出 [orange, apple]<br>    System.out.println(set.contains(“apple”)); &#x2F;&#x2F; 输出 true</p>
<p>需要注意的是，在使用集合类时，应该根据具体需求选择合适的实现类。同时，对于需要保证线程安全的场景，应该考虑使用线程安全的集合类（如ConcurrentHashMap和CopyOnWriteArrayList等）。</p>
<p>3.Map类</p>
<p>Map是一种键值对（key-value）映射的集合，每个键只能对应一个值。常用的Map实现类有HashMap和TreeMap。其中，HashMap是基于哈希表实现的，支持快速查找，而TreeMap是基于红黑树实现的，支持按键排序。</p>
<p>常用的Map实现类有：</p>
<ul>
<li>HashMap：基于哈希表实现的Map，支持快速的插入和查找操作，但不保证元素的顺序，且在处理冲突时需要进行重新哈希。</li>
<li>TreeMap：基于红黑树实现的有序Map，支持按照键进行排序，但插入和查询操作的效率较低。</li>
<li>LinkedHashMap：基于哈希表和双向链表实现的Map，可以按照插入顺序或者访问顺序进行迭代，其性能介于HashMap和TreeMap之间。</li>
</ul>
<p>除了上述实现类之外，还有一些其他的Map实现类，如HashTable、ConcurrentHashMap等，它们都继承自Map接口，并提供了特定的功能。</p>
<p>Map接口提供了很多常用的方法，例如：put()、get()、remove()、containsKey()、containsValue()、keySet()、values()等，它们都遵循了Java集合框架的规范。在使用Map时，建议先根据具体需求选择合适的实现类，再根据需要选择合适的方法进行操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Integer&gt; map = new HashMap&lt;&gt;();</span><br><span class="line">map.put(&quot;apple&quot;, 2);</span><br><span class="line">map.put(&quot;banana&quot;, 3);</span><br><span class="line">map.put(&quot;orange&quot;, 4);</span><br><span class="line">System.out.println(map); // 输出 &#123;orange=4, apple=2, banana=3&#125;</span><br><span class="line"></span><br><span class="line">map.remove(&quot;banana&quot;);</span><br><span class="line">System.out.println(map); // 输出 &#123;orange=4, apple=2&#125;</span><br><span class="line"></span><br><span class="line">System.out.println(map.get(&quot;apple&quot;)); // 输出 2</span><br></pre></td></tr></table></figure>

<h4 id="集合遍历的方法"><a href="#集合遍历的方法" class="headerlink" title="集合遍历的方法"></a>集合遍历的方法</h4><p>在Java中，集合遍历有多种方法，下面介绍几种常用的方式。</p>
<ol>
<li>for循环</li>
</ol>
<p>通过for循环可以遍历List、数组等有序集合。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(&quot;apple&quot;);</span><br><span class="line">list.add(&quot;banana&quot;);</span><br><span class="line">list.add(&quot;orange&quot;);</span><br><span class="line"></span><br><span class="line">for (int i = 0; i &lt; list.size(); i++) &#123;</span><br><span class="line">    System.out.println(list.get(i));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>增强型for循环</li>
</ol>
<p>增强型for循环（也称为foreach）是一种简化版的for循环，用于遍历数组或Iterable类型的集合。它可以方便地遍历集合元素，但无法获取当前元素的索引。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(&quot;apple&quot;);</span><br><span class="line">list.add(&quot;banana&quot;);</span><br><span class="line">list.add(&quot;orange&quot;);</span><br><span class="line"></span><br><span class="line">for (String fruit : list) &#123;</span><br><span class="line">    System.out.println(fruit);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>迭代器</li>
</ol>
<p>迭代器可以通过next()方法逐个获取集合中的元素，并且支持删除操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(&quot;apple&quot;);</span><br><span class="line">list.add(&quot;banana&quot;);</span><br><span class="line">list.add(&quot;orange&quot;);</span><br><span class="line"></span><br><span class="line">Iterator&lt;String&gt; iterator = list.iterator();</span><br><span class="line">while (iterator.hasNext()) &#123;</span><br><span class="line">    String fruit = iterator.next();</span><br><span class="line">    System.out.println(fruit);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>lambda表达式</li>
</ol>
<p>lambda表达式可以使用Stream API来遍历集合。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(&quot;apple&quot;);</span><br><span class="line">list.add(&quot;banana&quot;);</span><br><span class="line">list.add(&quot;orange&quot;);</span><br><span class="line"></span><br><span class="line">list.stream().forEach(fruit -&gt; System.out.println(fruit));</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在遍历集合时，应该根据具体需求选择合适的方式。同时，对于需要保证线程安全的集合类（如ConcurrentHashMap和CopyOnWriteArrayList等），应该采用线程安全的方式进行遍历。</p>
<h4 id="泛型的使用"><a href="#泛型的使用" class="headerlink" title="泛型的使用"></a>泛型的使用</h4><p>泛型是Java中的一个重要特性，它可以将数据类型作为参数传递给类、接口和方法。使用泛型可以提高代码的可重用性和安全性。</p>
<ol>
<li>泛型类</li>
</ol>
<p>定义一个泛型类时，需要在类名后面加上尖括号&lt;&gt;，并在其中指定泛型的类型。在类的属性、方法和构造方法中，可以使用泛型类型作为变量类型。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Box&lt;T&gt; &#123;</span><br><span class="line">    private T data;</span><br><span class="line"></span><br><span class="line">    public Box(T data) &#123;</span><br><span class="line">        this.data = data;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public T getData() &#123;</span><br><span class="line">        return data;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void setData(T data) &#123;</span><br><span class="line">        this.data = data;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>泛型接口</li>
</ol>
<p>定义一个泛型接口时，需要在接口名后面加上尖括号&lt;&gt;，并在其中指定泛型的类型。在接口的方法中，可以使用泛型类型作为参数类型或返回值类型。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface List&lt;E&gt; &#123;</span><br><span class="line">    boolean add(E element);</span><br><span class="line">    E get(int index);</span><br><span class="line">    int size();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>泛型方法</li>
</ol>
<p>定义一个泛型方法时，需要在方法名前面加上尖括号&lt;&gt;，并在其中指定泛型的类型。在方法的参数、返回值和局部变量中，可以使用泛型类型。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public static &lt;T&gt; void printArray(T[] array) &#123;</span><br><span class="line">    for (T element : array) &#123;</span><br><span class="line">        System.out.println(element);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用泛型时，应该根据具体情况选择合适的泛型类型。同时，对于需要保证类型安全的场景，应该采用泛型的方式进行编程。</p>
<h4 id="集合工具类的使用"><a href="#集合工具类的使用" class="headerlink" title="集合工具类的使用"></a>集合工具类的使用</h4><p>Java中提供了一些集合工具类，可以方便地对集合进行操作，下面介绍几个常用的工具类。</p>
<ol>
<li>Collections类</li>
</ol>
<p>Collections类是Java提供的集合工具类，其中包含了许多静态方法，用于对集合进行排序、查找、替换、打乱等操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; list = new ArrayList&lt;&gt;();</span><br><span class="line">list.add(3);</span><br><span class="line">list.add(1);</span><br><span class="line">list.add(2);</span><br><span class="line"></span><br><span class="line">Collections.sort(list); // 对集合进行排序</span><br><span class="line">System.out.println(list); // 输出 [1, 2, 3]</span><br><span class="line"></span><br><span class="line">int index = Collections.binarySearch(list, 2); // 在集合中查找元素</span><br><span class="line">System.out.println(index); // 输出 1</span><br><span class="line"></span><br><span class="line">Collections.replaceAll(list, 2, 4); // 将集合中的元素替换为指定值</span><br><span class="line">System.out.println(list); // 输出 [1, 4, 3]</span><br><span class="line"></span><br><span class="line">Collections.shuffle(list); // 随机打乱集合中的元素</span><br><span class="line">System.out.println(list); // 输出 [3, 1, 4]</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Arrays类</li>
</ol>
<p>Arrays类是Java提供的数组工具类，其中包含了一些静态方法，用于对数组进行排序、查找、比较等操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int[] array = &#123;3, 1, 2&#125;;</span><br><span class="line"></span><br><span class="line">Arrays.sort(array); // 对数组进行排序</span><br><span class="line">System.out.println(Arrays.toString(array)); // 输出 [1, 2, 3]</span><br><span class="line"></span><br><span class="line">int index = Arrays.binarySearch(array, 2); // 在数组中查找元素</span><br><span class="line">System.out.println(index); // 输出 1</span><br><span class="line"></span><br><span class="line">boolean isEqual = Arrays.equals(array, new int[]&#123;1, 2, 3&#125;); // 比较两个数组是否相等</span><br><span class="line">System.out.println(isEqual); // 输出 true</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>StringUtils类</li>
</ol>
<p>StringUtils类是Apache Commons Lang库提供的字符串工具类，其中包含了一些静态方法，用于对字符串进行处理，如判断是否为空、去除空格、拼接字符串等操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String str = &quot;  hello world! &quot;;</span><br><span class="line"></span><br><span class="line">boolean isBlank = StringUtils.isBlank(str); // 判断字符串是否为空或者只包含空格</span><br><span class="line">System.out.println(isBlank); // 输出 false</span><br><span class="line"></span><br><span class="line">String trimmedStr = StringUtils.trim(str); // 去除字符串前后的空格</span><br><span class="line">System.out.println(trimmedStr); // 输出 &quot;hello world!&quot;</span><br><span class="line"></span><br><span class="line">String joinedStr = StringUtils.join(new String[]&#123;&quot;hello&quot;, &quot;world&quot;&#125;, &quot;, &quot;); // 将数组中的元素以指定分隔符拼接成字符串</span><br><span class="line">System.out.println(joinedStr); // 输出 &quot;hello, world&quot;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用集合工具类时，应该根据具体需求选择合适的工具类和方法。同时，对于需要保证线程安全的场景，应该考虑使用线程安全的集合工具类（如ConcurrentHashMap和CopyOnWriteArrayList等）。</p>
<h4 id="Stream-API的使用"><a href="#Stream-API的使用" class="headerlink" title="Stream API的使用"></a>Stream API的使用</h4><p>Stream API是Java 8中新增的一个功能，它提供了一种流式处理数据的方式，可以方便地对集合、数组和I&#x2F;O等数据源进行操作。</p>
<p>使用Stream API可以大幅度减少代码量，并且提高程序的可读性和可维护性。</p>
<p>下面介绍Stream API的常用方法。</p>
<ol>
<li>filter()</li>
</ol>
<p>filter()方法用于过滤集合中不符合条件的元素。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; list = Arrays.asList(1, 2, 3, 4, 5);</span><br><span class="line"></span><br><span class="line">List&lt;Integer&gt; filteredList = list.stream()</span><br><span class="line">        .filter(i -&gt; i % 2 == 0)</span><br><span class="line">        .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">System.out.println(filteredList); // 输出 [2, 4]</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>map()</li>
</ol>
<p>map()方法用于将集合中的每个元素映射为另外一个值。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;String&gt; list = Arrays.asList(&quot;apple&quot;, &quot;banana&quot;, &quot;orange&quot;);</span><br><span class="line"></span><br><span class="line">List&lt;Integer&gt; mappedList = list.stream()</span><br><span class="line">        .map(s -&gt; s.length())</span><br><span class="line">        .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">System.out.println(mappedList); // 输出 [5, 6, 6]</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>reduce()</li>
</ol>
<p>reduce()方法用于对集合中的元素进行归约操作，生成一个新的值。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; list = Arrays.asList(1, 2, 3, 4, 5);</span><br><span class="line"></span><br><span class="line">int sum = list.stream()</span><br><span class="line">        .reduce(0, (a, b) -&gt; a + b);</span><br><span class="line"></span><br><span class="line">System.out.println(sum); // 输出 15</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>sorted()</li>
</ol>
<p>sorted()方法用于对集合中的元素进行排序。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">List&lt;Integer&gt; list = Arrays.asList(5, 3, 1, 2, 4);</span><br><span class="line"></span><br><span class="line">List&lt;Integer&gt; sortedList = list.stream()</span><br><span class="line">        .sorted()</span><br><span class="line">        .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line">System.out.println(sortedList); // 输出 [1, 2, 3, 4, 5]</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用Stream API时，应该根据具体情况选择合适的方法。同时，Stream API中的方法都是惰性求值的，只有在最后调用终止操作（如collect()、forEach()）时才会执行。</p>
<h3 id="Java-IO"><a href="#Java-IO" class="headerlink" title="Java IO"></a>Java IO</h3><h4 id="File类及其用法"><a href="#File类及其用法" class="headerlink" title="File类及其用法"></a>File类及其用法</h4><p>File类是Java中用来表示文件或目录的类，它提供了一系列操作文件和目录的方法。</p>
<p>下面介绍File类的常用方法。</p>
<p><img src="https://img-blog.csdnimg.cn/20210616175436943.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3dlaXhpbl80Mzg4NDIzNA==,size_16,color_FFFFFF,t_70"></p>
<ol>
<li>构造方法</li>
</ol>
<p>File类提供了多个构造方法，可以根据给定的路径创建一个File对象。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">File file1 = new File(&quot;D:/demo/file.txt&quot;); // 绝对路径</span><br><span class="line">File file2 = new File(&quot;src/main/java/com/example/demo/FileDemo.java&quot;); // 相对路径</span><br><span class="line">File file3 = new File(&quot;D:/demo&quot;, &quot;file.txt&quot;); // 父目录和子文件名</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>创建和删除文件与目录</li>
</ol>
<p>File类提供了多个方法，可以创建和删除文件和目录。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">File file = new File(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">boolean isSuccess = file.createNewFile(); // 创建新文件</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br><span class="line"></span><br><span class="line">isSuccess = file.delete(); // 删除文件</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br><span class="line"></span><br><span class="line">File directory = new File(&quot;D:/demo/newdir&quot;);</span><br><span class="line">isSuccess = directory.mkdir(); // 创建新目录</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br><span class="line"></span><br><span class="line">isSuccess = directory.delete(); // 删除目录</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>文件信息查询</li>
</ol>
<p>File类提供了多个方法，可以查询文件的属性和状态。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">File file = new File(&quot;D:/demo/file.txt&quot;);</span><br><span class="line"></span><br><span class="line">String absolutePath = file.getAbsolutePath(); // 获取文件的绝对路径</span><br><span class="line">System.out.println(absolutePath); // 输出 D:\demo\file.txt</span><br><span class="line"></span><br><span class="line">long length = file.length(); // 获取文件的长度（字节数）</span><br><span class="line">System.out.println(length); // 输出 0</span><br><span class="line"></span><br><span class="line">boolean isDirectory = file.isDirectory(); // 判断是否为目录</span><br><span class="line">System.out.println(isDirectory); // 输出 false</span><br><span class="line"></span><br><span class="line">boolean isFile = file.isFile(); // 判断是否为文件</span><br><span class="line">System.out.println(isFile); // 输出 true</span><br><span class="line"></span><br><span class="line">boolean exists = file.exists(); // 判断文件是否存在</span><br><span class="line">System.out.println(exists); // 输出 true</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>文件操作</li>
</ol>
<p>File类提供了多个方法，可以对文件进行复制、移动、重命名等操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">File file1 = new File(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">File file2 = new File(&quot;D:/demo/newfile.txt&quot;);</span><br><span class="line"></span><br><span class="line">boolean isSuccess = file1.renameTo(file2); // 重命名文件</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br><span class="line"></span><br><span class="line">isSuccess = file1.createNewFile(); // 创建新文件</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br><span class="line"></span><br><span class="line">InputStream in = new FileInputStream(file1);</span><br><span class="line">OutputStream out = new FileOutputStream(file2);</span><br><span class="line"></span><br><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">int len;</span><br><span class="line">while ((len = in.read(buffer)) &gt; 0) &#123; // 复制文件</span><br><span class="line">    out.write(buffer, 0, len);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">in.close();</span><br><span class="line">out.close();</span><br><span class="line"></span><br><span class="line">isSuccess = file1.delete(); // 删除文件</span><br><span class="line">System.out.println(isSuccess); // 输出 true</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用File类时，应该根据具体情况选择合适的方法。同时，对于需要保证线程安全的场景，应该考虑使用线程安全的文件操作类（如Apache Commons IO库提供的工具类）</p>
<h4 id="操作字节流和字符流读写文件"><a href="#操作字节流和字符流读写文件" class="headerlink" title="操作字节流和字符流读写文件"></a>操作字节流和字符流读写文件</h4><p><img src="file:///C:/Users/26927/Pictures/Typedown/b31caab1-f83b-49a4-a25d-cd74f0631ffe.png" alt="b31caab1-f83b-49a4-a25d-cd74f0631ffe"></p>
<p>Java中提供了字节流和字符流两种方式来读写文件。</p>
<ol>
<li>操作字节流读写文件</li>
</ol>
<p>使用字节流读写文件需要使用InputStream和OutputStream类。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 读取文件内容</span><br><span class="line">InputStream in = new FileInputStream(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">int len;</span><br><span class="line">while ((len = in.read(buffer)) != -1) &#123;</span><br><span class="line">    System.out.write(buffer, 0, len);</span><br><span class="line">&#125;</span><br><span class="line">in.close();</span><br><span class="line"></span><br><span class="line">// 写入文件内容</span><br><span class="line">OutputStream out = new FileOutputStream(&quot;D:/demo/newfile.txt&quot;);</span><br><span class="line">out.write(&quot;Hello, world!&quot;.getBytes());</span><br><span class="line">out.close();</span><br></pre></td></tr></table></figure>

<p>上面的示例代码中，InputStream的read()方法会读取文件输入流中的字节到缓冲区中，并返回读取的字节数。当读取到文件末尾时，该方法会返回-1。</p>
<ol start="2">
<li>操作字符流读写文件</li>
</ol>
<p>使用字符流读写文件需要使用Reader和Writer类。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 读取文件内容</span><br><span class="line">Reader reader = new FileReader(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">char[] buffer = new char[1024];</span><br><span class="line">int len;</span><br><span class="line">while ((len = reader.read(buffer)) != -1) &#123;</span><br><span class="line">    System.out.print(new String(buffer, 0, len));</span><br><span class="line">&#125;</span><br><span class="line">reader.close();</span><br><span class="line"></span><br><span class="line">// 写入文件内容</span><br><span class="line">Writer writer = new FileWriter(&quot;D:/demo/newfile.txt&quot;);</span><br><span class="line">writer.write(&quot;Hello, world!&quot;);</span><br><span class="line">writer.close();</span><br></pre></td></tr></table></figure>

<p>上面的示例代码中，Reader的read()方法会读取文件输入流中的字符到缓冲区中，并返回读取的字符数。当读取到文件末尾时，该方法会返回-1。</p>
<p>需要注意的是，在读写文件时，应该根据文件的编码格式选择合适的字节流或字符流。另外，为避免资源泄漏，应该使用try-with-resources语句来自动关闭输入输出流。</p>
<p><img src="file:///C:/Users/26927/Pictures/Typedown/7ae67b77-245e-4b50-8f22-3872be2cf7d9.png" alt="7ae67b77-245e-4b50-8f22-3872be2cf7d9"></p>
<h4 id="其他IO流（了解）"><a href="#其他IO流（了解）" class="headerlink" title="其他IO流（了解）"></a>其他IO流（了解）</h4><p>除了字节流和字符流之外，Java中还提供了其他多种类型的IO流，包括：</p>
<ol>
<li>缓冲流</li>
</ol>
<p>缓冲流是一种性能更好的字节流或字符流，它可以减少访问底层资源的次数，提高读写效率。Java中提供了BufferedInputStream、BufferedOutputStream、BufferedReader、BufferedWriter等缓冲流类。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 使用缓冲字节流复制文件内容</span><br><span class="line">InputStream in = new FileInputStream(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">OutputStream out = new FileOutputStream(&quot;D:/demo/newfile.txt&quot;);</span><br><span class="line">BufferedInputStream bis = new BufferedInputStream(in);</span><br><span class="line">BufferedOutputStream bos = new BufferedOutputStream(out);</span><br><span class="line"></span><br><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">int len;</span><br><span class="line">while ((len = bis.read(buffer)) != -1) &#123;</span><br><span class="line">    bos.write(buffer, 0, len);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">bis.close();</span><br><span class="line">bos.close();</span><br><span class="line"></span><br><span class="line">// 使用缓冲字符流读取文件内容</span><br><span class="line">Reader reader = new FileReader(&quot;D:/demo/file.txt&quot;);</span><br><span class="line">Writer writer = new FileWriter(&quot;D:/demo/newfile.txt&quot;);</span><br><span class="line">BufferedReader br = new BufferedReader(reader);</span><br><span class="line">BufferedWriter bw = new BufferedWriter(writer);</span><br><span class="line"></span><br><span class="line">String line;</span><br><span class="line">while ((line = br.readLine()) != null) &#123;</span><br><span class="line">    bw.write(line);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">br.close();</span><br><span class="line">bw.close();</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>数据流</li>
</ol>
<p>数据流可以用来读写Java中的基本数据类型和对象，在读写时会将数据转换为二进制格式。Java中提供了DataInputStream和DataOutputStream两个数据流类。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 使用数据流写入和读取基本数据类型</span><br><span class="line">OutputStream out = new FileOutputStream(&quot;D:/demo/data.bin&quot;);</span><br><span class="line">DataOutputStream dos = new DataOutputStream(out);</span><br><span class="line"></span><br><span class="line">dos.writeInt(123);</span><br><span class="line">dos.writeDouble(3.14);</span><br><span class="line">dos.writeBoolean(true);</span><br><span class="line"></span><br><span class="line">dos.close();</span><br><span class="line"></span><br><span class="line">InputStream in = new FileInputStream(&quot;D:/demo/data.bin&quot;);</span><br><span class="line">DataInputStream dis = new DataInputStream(in);</span><br><span class="line"></span><br><span class="line">int intValue = dis.readInt();</span><br><span class="line">double doubleValue = dis.readDouble();</span><br><span class="line">boolean booleanValue = dis.readBoolean();</span><br><span class="line"></span><br><span class="line">dis.close();</span><br><span class="line"></span><br><span class="line">System.out.println(intValue); // 输出 123</span><br><span class="line">System.out.println(doubleValue); // 输出 3.14</span><br><span class="line">System.out.println(booleanValue); // 输出 true</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>对象流</li>
</ol>
<p>对象流可以用来读写Java中的对象，在读写时会将对象序列化为二进制格式。Java中提供了ObjectInputStream和ObjectOutputStream两个对象流类。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 使用对象流写入和读取对象</span><br><span class="line">OutputStream out = new FileOutputStream(&quot;D:/demo/person.obj&quot;);</span><br><span class="line">ObjectOutputStream oos = new ObjectOutputStream(out);</span><br><span class="line">Person person = new Person(&quot;张三&quot;, 20);</span><br><span class="line">oos.writeObject(person);</span><br><span class="line">oos.close();</span><br><span class="line"></span><br><span class="line">InputStream in = new FileInputStream(&quot;D:/demo/person.obj&quot;);</span><br><span class="line">ObjectInputStream ois = new ObjectInputStream(in);</span><br><span class="line">person = (Person) ois.readObject();</span><br><span class="line">ois.close();</span><br><span class="line"></span><br><span class="line">System.out.println(person.getName()); // 输出 张三</span><br><span class="line">System.out.println(person.getAge()); // 输出 20</span><br></pre></td></tr></table></figure>

<p><img src="file:///C:/Users/26927/Pictures/Typedown/d6e042ae-9eba-412c-a522-a2840c05601f.png" alt="d6e042ae-9eba-412c-a522-a2840c05601f"></p>
<p>需要注意的是，在使用IO流时，应该根据具体情况选择合适的流类型，并在读写完毕后及时关闭流以释放资源。</p>
<h4 id="NIO的概念及其用法（了解）"><a href="#NIO的概念及其用法（了解）" class="headerlink" title="NIO的概念及其用法（了解）"></a>NIO的概念及其用法（了解）</h4><p>NIO（New IO）是Java中提供的一种新的IO处理方式，它相对于传统的IO方式具有更高的效率和灵活性。</p>
<ol>
<li>NIO的概念</li>
</ol>
<p>在传统IO中，数据的读写是通过流（InputStream&#x2F;OutputStream）进行的。而在NIO中，数据的读写是通过缓冲区（Buffer）进行的。NIO中的核心类包括ByteBuffer、CharBuffer、ShortBuffer、IntBuffer、LongBuffer、DoubleBuffer等缓冲区类，以及Channel、Selector等类。</p>
<ol start="2">
<li>NIO的用法</li>
</ol>
<p>使用NIO进行数据读写需要以下几个步骤：</p>
<p>2.1 打开Channel</p>
<p>首先需要打开一个Channel，可以是FileChannel、DatagramChannel、SocketChannel或ServerSocketChannel等类型的通道。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 打开文件通道</span><br><span class="line">FileChannel channel = FileChannel.open(Paths.get(&quot;D:/demo/file.txt&quot;), StandardOpenOption.READ);</span><br></pre></td></tr></table></figure>

<p>2.2 创建Buffer</p>
<p>创建一个Buffer来存储数据。在NIO中，所有数据都是通过Buffer来处理的，因此需要根据实际需要创建合适类型的Buffer。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">java</span><br><span class="line">// 创建字节缓冲区</span><br><span class="line">ByteBuffer buffer = ByteBuffer.allocate(1024);</span><br></pre></td></tr></table></figure>

<p>2.3 从Channel中读取数据</p>
<p>使用Channel的read()方法从Channel中读取数据到Buffer中。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 从文件通道读取数据到缓冲区</span><br><span class="line">int bytesRead = channel.read(buffer);</span><br></pre></td></tr></table></figure>

<p>2.4 写入数据到Channel中</p>
<p>使用Channel的write()方法将数据从Buffer中写入到Channel中。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 将缓冲区中的数据写入到文件通道中</span><br><span class="line">buffer.flip(); // 切换读模式到写模式</span><br><span class="line">channel.write(buffer);</span><br></pre></td></tr></table></figure>

<p>2.5 关闭Channel</p>
<p>使用完Channel后应该手动关闭它以释放资源。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 关闭文件通道</span><br><span class="line">channel.close();</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>NIO Selector的用法</li>
</ol>
<p>NIO中的Selector可以用来监听多个Channel的事件，并在有事件发生时进行处理。Selector主要包括以下几个方法：</p>
<ul>
<li><p>Selector.open()：打开一个Selector。</p>
</li>
<li><p>select()：阻塞并等待至少一个Channel有就绪事件产生。</p>
</li>
<li><p>selectedKeys()：获取所有就绪的SelectionKey集合。</p>
</li>
<li><p>Selector.close()：关闭Selecto<br>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 打开Selector</span><br><span class="line">Selector selector = Selector.open();</span><br><span class="line"></span><br><span class="line">// 向通道注册Selector及所关心的事件</span><br><span class="line">SelectableChannel channel = SocketChannel.open();</span><br><span class="line">channel.configureBlocking(false); // 设置为非阻塞IO</span><br><span class="line">SelectionKey key = channel.register(selector, SelectionKey.OP_READ);</span><br><span class="line"></span><br><span class="line">// 阻塞并等待就绪事件的产生</span><br><span class="line">selector.select();</span><br><span class="line"></span><br><span class="line">// 处理就绪事件</span><br><span class="line">Set&lt;SelectionKey&gt; selectedKeys = selector.selectedKeys();</span><br><span class="line">Iterator&lt;SelectionKey&gt; iterator = selectedKeys.iterator();</span><br><span class="line">while (iterator.hasNext()) &#123;</span><br><span class="line">    SelectionKey selectionKey = iterator.next();</span><br><span class="line">    if (selectionKey.isReadable()) &#123;</span><br><span class="line">        // Channel可读事件处理</span><br><span class="line">    &#125; else if (selectionKey.isWritable()) &#123;</span><br><span class="line">        // Channel可写事件处理</span><br><span class="line">    &#125;</span><br><span class="line">    iterator.remove();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 关闭Selector</span><br><span class="line">selector.close();</span><br></pre></td></tr></table></figure></li>
</ul>
<p>需要注意的是，在使用NIO进行数据读写时，应该根据实际需求选择使用哪种类型的Channel和Buffer，并保证线程安全。同时，在使用Selector时也应该避免过度使用，以免造成系统资源浪费。</p>
<h4 id="常见字符编码（了解）"><a href="#常见字符编码（了解）" class="headerlink" title="常见字符编码（了解）"></a>常见字符编码（了解）</h4><p>在计算机中，字符编码是用来表示字符的数值代码的一种系统。常见的字符编码包括以下几种：</p>
<ol>
<li>ASCII</li>
</ol>
<p>ASCII（American Standard Code for Information Interchange）是最早的字符编码标准之一，使用7位二进制数来表示128个不同的字符，包括英文字母、数字、标点符号等。</p>
<ol start="2">
<li>ISO-8859</li>
</ol>
<p>ISO-8859是一系列字符编码标准，其中最常用的是ISO-8859-1，也称为Latin-1。ISO-8859-1使用8位二进制数来表示256个不同的字符，包括ASCII中的所有字符以及欧洲各国语言中出现的字符。</p>
<ol start="3">
<li>Unicode</li>
</ol>
<p>Unicode是一种全球化字符编码标准，它使用32位二进制数来表示超过100,000个字符，包括各种语言的字符、符号、表情等。Unicode的两种主要实现方式是UTF-8和UTF-16。</p>
<ol start="4">
<li>UTF-8</li>
</ol>
<p>UTF-8（Unicode Transformation Format 8-bit）是一种变长编码方式，它使用1~4个字节来表示不同的字符。UTF-8可以表示Unicode中的任意字符，并且向下兼容ASCII编码。</p>
<ol start="5">
<li>UTF-16</li>
</ol>
<p>UTF-16（Unicode Transformation Format 16-bit）是一种定长编码方式，它使用2个字节来表示大部分常用字符，使用4个字节来表示较少使用的字符。UTF-16可以表示Unicode中的任意字符，但是它不向下兼容ASCII编码。</p>
<p>需要注意的是，在进行文本处理时应该根据具体情况选择合适的字符编码，避免因编码不匹配而导致乱码等问题。同时，在进行字符编码转换时也应该谨慎处理，以免产生意外的结果。</p>
<h3 id="图形用户界面"><a href="#图形用户界面" class="headerlink" title="图形用户界面"></a>图形用户界面</h3><h3 id="AWT组件和Swing组件的联系和区别（了解）"><a href="#AWT组件和Swing组件的联系和区别（了解）" class="headerlink" title="AWT组件和Swing组件的联系和区别（了解）"></a>AWT组件和Swing组件的联系和区别（了解）</h3><p>AWT（Abstract Window Toolkit）和Swing都是Java GUI编程中常用的组件库，它们有很多相似之处，但也存在一些区别。</p>
<ol>
<li>AWT组件和Swing组件的联系</li>
</ol>
<p>AWT和Swing都提供了大量的GUI组件，例如按钮、文本框、标签、列表框、下拉框等。这些组件都可以使用Java代码进行创建、配置和布局，并且可以添加事件监听器来处理交互事件。</p>
<p>同时，AWT和Swing都使用轻量级组件模型，即将所有GUI组件封装在一个统一的容器中，并由容器自行管理和绘制组件。</p>
<ol start="2">
<li>AWT组件和Swing组件的区别</li>
</ol>
<p>虽然AWT和Swing都是GUI组件库，但是它们也存在着一些不同之处：</p>
<ul>
<li>组件外观：AWT使用本地系统窗口来呈现组件，因此它的外观会随操作系统的不同而有所差异；而Swing则采用了一套独立的外观设计，称为“Pluggable Look and Feel”，使得Swing组件在不同平台上具有一致的外观。</li>
<li>组件性能：由于AWT使用了本地系统窗口，因此在跨平台时可能会出现性能问题；而Swing则更加轻量级，可以在不同平台上获得更好的性能表现。</li>
<li>组件数量：AWT提供的组件数量相对较少，而Swing则提供了更丰富的组件库，例如JTable、JTree、JFileChooser等。</li>
<li>组件功能：AWT的组件功能相对简单，适合于开发基础的GUI应用程序；而Swing的组件功能更加强大，可以用于开发复杂的GUI应用程序。</li>
</ul>
<p>总的来说，如果需要开发简单的GUI应用程序，可以选择使用AWT，它更加稳定并且易于学习；而如果需要开发复杂的GUI应用程序，可以选择Swing，它更加灵活、强大并且具有良好的跨平台性能。</p>
<h3 id="常用的Swing组件的使用"><a href="#常用的Swing组件的使用" class="headerlink" title="常用的Swing组件的使用"></a>常用的Swing组件的使用</h3><p>Swing是Java中常用的GUI组件库，提供了大量的组件和布局管理器，可以用于开发丰富的GUI应用程序。以下是一些常用的Swing组件及其使用方法：</p>
<ol>
<li>JFrame</li>
</ol>
<p>JFrame是Swing中最基本的顶层容器，它可以作为窗口来显示Swing中的其他组件。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JFrame frame = new JFrame(&quot;My Frame&quot;);</span><br><span class="line">frame.setSize(400, 300);</span><br><span class="line">frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // 设置关闭行为</span><br><span class="line">frame.setVisible(true); // 显示窗口</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>JPanel</li>
</ol>
<p>JPanel是一个面板容器，可以将其他组件添加进来，并使用布局管理器进行管理。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JPanel panel = new JPanel(new BorderLayout());</span><br><span class="line">panel.add(new JButton(&quot;Button&quot;), BorderLayout.CENTER);</span><br><span class="line">panel.add(new JLabel(&quot;Label&quot;), BorderLayout.NORTH);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>JButton</li>
</ol>
<p>JButton是一个按钮组件，可以响应用户的单击事件。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JButton button = new JButton(&quot;Click me&quot;);</span><br><span class="line">button.addActionListener(e -&gt; &#123;</span><br><span class="line">    // 处理按钮单击事件</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>JTextField</li>
</ol>
<p>JTextField是一个文本框组件，可以让用户输入和编辑文本。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JTextField textField = new JTextField(&quot;default text&quot;, 20);</span><br><span class="line">String text = textField.getText(); // 获取文本框中的文本</span><br><span class="line">textField.setText(&quot;new text&quot;); // 设置文本框中的文本JCheckBox</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>JCheckBox</li>
</ol>
<p>JCheckBox是一个复选框组件，可以让用户进行多选操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JCheckBox checkBox = new JCheckBox(&quot;Check me&quot;);</span><br><span class="line">boolean checked = checkBox.isSelected(); // 判断复选框是否被选中</span><br><span class="line">checkBox.setSelected(true); // 设置复选框为选中状态</span><br></pre></td></tr></table></figure>

<ol start="6">
<li>JRadioButton</li>
</ol>
<p>JRadioButton是一个单选按钮组件，与JCheckBox不同的是，只允许用户进行单选操作。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JRadioButton radioButton1 = new JRadioButton(&quot;Option 1&quot;);</span><br><span class="line">JRadioButton radioButton2 = new JRadioButton(&quot;Option 2&quot;);</span><br><span class="line">ButtonGroup group = new ButtonGroup();</span><br><span class="line">group.add(radioButton1);</span><br><span class="line">group.add(radioButton2);</span><br></pre></td></tr></table></figure>

<ol start="7">
<li>JComboBox</li>
</ol>
<p>JComboBox是一个下拉框组件，可以提供多个选项供用户选择。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JComboBox&lt;String&gt; comboBox = new JComboBox&lt;&gt;(new String[]&#123;&quot;Option 1&quot;, &quot;Option 2&quot;, &quot;Option 3&quot;&#125;);</span><br><span class="line">String selected = comboBox.getSelectedItem().toString(); // 获取当前选中的选项</span><br><span class="line">comboBox.setSelectedIndex(1); // 设置选中指定索引位置的选项</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用Swing组件时应该根据实际需求选择合适的组件、布局管理器以及事件处理方式，以便实现所需的功能并提升用户体验。</p>
<h4 id="常用的窗体和布局管理器"><a href="#常用的窗体和布局管理器" class="headerlink" title="常用的窗体和布局管理器"></a>常用的窗体和布局管理器</h4><p>在Java Swing中，窗体和布局管理器是常用的GUI开发工具。以下是一些常用的窗体和布局管理器：</p>
<ol>
<li>JFrame</li>
</ol>
<p>JFrame是Swing中最基本的顶层容器，可以作为窗口来显示Swing中的其他组件。使用setDefaultCloseOperation()方法设置关闭窗口时的行为，使用setSize()、setLocation()等方法设置窗口大小和位置。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JFrame frame = new JFrame(&quot;My Frame&quot;);</span><br><span class="line">frame.setSize(400, 300);</span><br><span class="line">frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // 设置关闭行为</span><br><span class="line">frame.setVisible(true); // 显示窗口</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>JPanel</li>
</ol>
<p>JPanel是一个面板容器，可以将其他组件添加进来，并使用布局管理器进行管理。使用setLayout()方法设置面板的布局管理器，使用add()方法将其他组件添加到面板中。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JPanel panel = new JPanel(new BorderLayout());</span><br><span class="line">panel.add(new JButton(&quot;Button&quot;), BorderLayout.CENTER);</span><br><span class="line">panel.add(new JLabel(&quot;Label&quot;), BorderLayout.NORTH);</span><br></pre></td></tr></table></figure>

<h4 id="1-1-边框布局管理器-BorderLayout"><a href="#1-1-边框布局管理器-BorderLayout" class="headerlink" title="1.1 边框布局管理器(BorderLayout)"></a>1.1 边框布局管理器(BorderLayout)</h4><blockquote>
<p>BorderLayout（边框布局管理器）是 Window、JFrame 和 JDialog 的默认布局管理器。边框布局管理器将窗口分为 5 个区域：North、South、East、West 和 Center。其中，North 表示北，将占据面板的上方；Soufe 表示南，将占据面板的下方；East表示东，将占据面板的右侧；West 表示西，将占据面板的左侧；中间区域 Center 是在东、南、西、北都填满后剩下的区域，如图 所示：<br><img src="https://img-blog.csdnimg.cn/20190305000445812.png" alt="在这里插入图片描述"><br><code>提示：边框布局管理器并不要求所有区域都必须有组件，如果四周的区域（North、South、East 和 West 区域）没有组件，则由 Center 区域去补充。如果单个区域中添加的不只一个组件，那么后来添加的组件将覆盖原来的组件，所以，区域中只显示最后添加的一个组件。</code></p>
</blockquote>
<h5 id="1-2-BorderLayout-布局管理器的构造方法"><a href="#1-2-BorderLayout-布局管理器的构造方法" class="headerlink" title="1.2 BorderLayout 布局管理器的构造方法"></a>1.2 BorderLayout 布局管理器的构造方法</h5><pre><code>BorderLayout()
创建一个 Border 布局，组件之间没有间隙。
BorderLayout(int hgap,int vgap)
创建一个 Border 布局，其中 hgap 表示组件之间的横向间隔；vgap 表示组件之间的纵向间隔，单位是像素。
</code></pre>
<h5 id="1-3-示例"><a href="#1-3-示例" class="headerlink" title="1.3 示例"></a>1.3 示例</h5><blockquote>
<p>使用 BorderLayout 将窗口分割为 5 个区域，并在每个区域添加一个标签按钮。实现代码如下：</p>
</blockquote>
<pre><code>import javax.swing.*;
import java.awt.*;

public class BorderLayoutDemo &#123;
    public static void main(String[] agrs) &#123;
        JFrame frame=new JFrame(&quot;BorderLayoutDemo&quot;);    //创建Frame窗口
        frame.setSize(400,200);

        frame.setLayout(new BorderLayout());    //为Frame窗口设置布局为BorderLayout

        JButton button1=new JButton (&quot;上&quot;);
        JButton button2=new JButton(&quot;左&quot;);
        JButton button3=new JButton(&quot;中&quot;);
        JButton button4=new JButton(&quot;右&quot;);
        JButton button5=new JButton(&quot;下&quot;);

        frame.add(button1,BorderLayout.NORTH);
        frame.add(button2,BorderLayout.WEST);
        frame.add(button3,BorderLayout.CENTER);
        frame.add(button4,BorderLayout.EAST);
        frame.add(button5,BorderLayout.SOUTH);

        frame.setBounds(300,200,600,300);
        frame.setVisible(true);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    &#125;
&#125;
</code></pre>
<blockquote>
<p><code>运行效果如下：</code><br><img src="https://img-blog.csdnimg.cn/20190305001135544.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzI3ODcwNDIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
</blockquote>
<h4 id="二、流式布局管理器-FlowLayout"><a href="#二、流式布局管理器-FlowLayout" class="headerlink" title="二、流式布局管理器(FlowLayout)"></a><code>二、流式布局管理器(FlowLayout)</code></h4><h5 id="2-1-FlowLayout"><a href="#2-1-FlowLayout" class="headerlink" title="2.1 FlowLayout"></a>2.1 FlowLayout</h5><blockquote>
<p>FlowLayout（流式布局管理器）是 JPanel 和 JApplet 的默认布局管理器。<code>FlowLayout 会将组件按照从从左到右、上到下的放置规律逐行进行定位。与其他布局管理器不同的是，FlowLayout 布局管理器不限制它所管理组件的大小，而是允许它们有自己的最佳大小。</code></p>
</blockquote>
<h5 id="2-2-FlowLayout-布局管理器的构造方法"><a href="#2-2-FlowLayout-布局管理器的构造方法" class="headerlink" title="2.2 FlowLayout 布局管理器的构造方法"></a>2.2 FlowLayout 布局管理器的构造方法</h5><pre><code>FlowLayout()：创建一个布局管理器，使用默认的居中对齐方式和默认 5 像素的水平和垂直间隔。
FlowLayout(int align)：创建一个布局管理器，使用默认 5 像素的水平和垂直间隔。其中，align 表示组件的对齐方式，对齐的值必须是 FlowLayout.LEFT、FlowLayout.RIGHT 和 FlowLayout.CENTER，指定组件在这一行的位置是居左对齐、居右对齐或居中对齐。
FlowLayout(int align, int hgap,int vgap)：创建一个布局管理器，其中 align 表示组件的对齐方式；hgap 表示组件之间的横向间隔；vgap 表示组件之间的纵向间隔，单位是像素。
</code></pre>
<h5 id="2-3-示例"><a href="#2-3-示例" class="headerlink" title="2.3 示例"></a>2.3 示例</h5><blockquote>
<p>使用 FlowLayout 类对窗口进行布局，向容器内添加 9 个按钮，并设置横向和纵向的间隔都为 20 像素。具体实现代码如下：</p>
</blockquote>
<pre><code>import javax.swing.*;
import java.awt.*;

public class FlowLayoutDemo &#123;
    public static void main(String[] agrs)  &#123;
        JFrame jFrame=new JFrame(&quot;FlowLayoutDemo&quot;);    //创建Frame窗口

        JPanel jPanel=new JPanel();    //创建面板
        JButton btn1=new JButton(&quot;1&quot;);    //创建按钮
        JButton btn2=new JButton(&quot;2&quot;);
        JButton btn3=new JButton(&quot;3&quot;);
        JButton btn4=new JButton(&quot;4&quot;);
        JButton btn5=new JButton(&quot;5&quot;);
        JButton btn6=new JButton(&quot;6&quot;);
        JButton btn7=new JButton(&quot;7&quot;);
        JButton btn8=new JButton(&quot;8&quot;);
        JButton btn9=new JButton(&quot;9&quot;);

        jPanel.add(btn1);    //面板中添加按钮
        jPanel.add(btn2);
        jPanel.add(btn3);
        jPanel.add(btn4);
        jPanel.add(btn5);
        jPanel.add(btn6);
        jPanel.add(btn7);
        jPanel.add(btn8);
        jPanel.add(btn9);

        //向JPanel添加FlowLayout布局管理器，将组件间的横向和纵向间隙都设置为20像素
        jPanel.setLayout(new FlowLayout(FlowLayout.LEADING,20,20));
        jPanel.setBackground(Color.gray);    //设置背景色
        jFrame.add(jPanel);    //添加面板到容器
        jFrame.setBounds(300,200,300,150);    //设置容器的大小
        jFrame.setVisible(true);
        jFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    &#125;
&#125;
</code></pre>
<blockquote>
<p>上述程序向 JPanel 面板中添加了 9 个按钮，并使用 FlowLayout 布局管理器使 9 个按钮间的横向和纵向间隙都为 20 像素。此时这些按钮将在容器上按照从上到下、从左到右的顺序排列，如果一行剩余空间不足容纳组件将会换行显示，最终运行结果如图 所示：<br><img src="https://img-blog.csdnimg.cn/20190305002233296.png" alt="在这里插入图片描述"><br><img src="https://img-blog.csdnimg.cn/20190305002306124.png" alt="在这里插入图片描述"></p>
</blockquote>
<h4 id="三、卡片布局管理器-CardLayout"><a href="#三、卡片布局管理器-CardLayout" class="headerlink" title="三、卡片布局管理器(CardLayout)"></a><code>三、卡片布局管理器(CardLayout)</code></h4><h5 id="3-1-CardLayout"><a href="#3-1-CardLayout" class="headerlink" title="3.1 CardLayout"></a>3.1 CardLayout</h5><blockquote>
<p>CardLayout（卡片布局管理器）能够帮助用户实现多个成员共享同一个显示空间，并且一次只显示一个容器组件的内容。<br><code>CardLayout 布局管理器将容器分成许多层，每层的显示空间占据整个容器的大小，但是每层只允许放置一个组件。</code></p>
</blockquote>
<h5 id="3-2-CardLayout-的构造方法"><a href="#3-2-CardLayout-的构造方法" class="headerlink" title="3.2 CardLayout 的构造方法"></a>3.2 CardLayout 的构造方法</h5><pre><code>CardLayout()：构造一个新布局，默认间隔为 0。
CardLayout(int hgap, int vgap)：创建布局管理器，并指定组件间的水平间隔（hgap）和垂直间隔（vgap）。
</code></pre>
<h5 id="3-3-示例"><a href="#3-3-示例" class="headerlink" title="3.3 示例"></a>3.3 示例</h5><blockquote>
<p>使用 CardLayout 类对容器内的两个面板进行布局。其中第一个面板上包括三个按钮，第二个面板上包括三个文本框。最后调用 CardLayout 类的 show() 方法显示指定面板的内容，代码如下：</p>
</blockquote>
<pre><code>import javax.swing.*;
import java.awt.*;

public class CardLayoutDemo &#123;
    public static void main(String[] agrs) &#123;
        JFrame frame=new JFrame(&quot;CardLayoutDemo&quot;);    //创建Frame窗口

        JPanel p1=new JPanel();    //面板1
        JPanel p2=new JPanel();    //面板2
        JPanel cards=new JPanel(new CardLayout());    //卡片式布局的面板

        p1.add(new JButton(&quot;登录按钮&quot;));
        p1.add(new JButton(&quot;注册按钮&quot;));
        p1.add(new JButton(&quot;找回密码按钮&quot;));

        p2.add(new JTextField(&quot;用户名文本框&quot;,20));
        p2.add(new JTextField(&quot;密码文本框&quot;,20));
        p2.add(new JTextField(&quot;验证码文本框&quot;,20));

        cards.add(p1,&quot;card1&quot;);    //向卡片式布局面板中添加面板1
        cards.add(p2,&quot;card2&quot;);    //向卡片式布局面板中添加面板2

        CardLayout cl=(CardLayout)(cards.getLayout());
        cl.show(cards,&quot;card1&quot;);    //调用show()方法显示面板2

        frame.add(cards);
        frame.setBounds(300,200,400,200);
        frame.setVisible(true);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    &#125;
&#125;
</code></pre>
<blockquote>
<p>上述代码创建了一个卡片式布局的面板 cards，该面板包含两个大小相同的子面板 p1 和 p2。需要注意的是，在将 p1 和 p2 添加到 cards 面板中时使用了含有两个参数的 add() 方法，该方法的第二个参数用来标识子面板。当需要显示某一个面板时，只需要调用卡片式布局管理器的 show() 方法，并在参数中指定子面板所对应的字符串即可，这里显示的是 p1 面板，运行效果如图 所示：</p>
</blockquote>
<p><img src="https://img-blog.csdnimg.cn/20190305003304870.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzI3ODcwNDIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<blockquote>
<p>如果将“cl.show(cards,“card1”)”语句中的 card1 换成 card2，将显示 p2 面板的内容，此时运行结果如图 所示：<br><img src="https://img-blog.csdnimg.cn/20190305003408605.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzI3ODcwNDIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
</blockquote>
<h4 id="四、网格布局管理器-GridLayout"><a href="#四、网格布局管理器-GridLayout" class="headerlink" title="四、网格布局管理器(GridLayout)"></a><code>四、网格布局管理器(GridLayout)</code></h4><h5 id="4-1-GridLayout"><a href="#4-1-GridLayout" class="headerlink" title="4.1 GridLayout"></a>4.1 GridLayout</h5><blockquote>
<p>GridLayout（网格布局管理器）为组件的放置位置提供了更大的灵活性。<code>它将区域分割成行数（rows）和列数（columns）的网格状布局，组件按照由左至右、由上而下的次序排列填充到各个单元格中。</code><br><code>提示：GridLayout 布局管理器总是忽略组件的最佳大小，而是根据提供的行和列进行平分。该布局管理的所有单元格的宽度和高度都是一样的。</code></p>
</blockquote>
<h5 id="4-2-GridLayout-的构造方法"><a href="#4-2-GridLayout-的构造方法" class="headerlink" title="4.2 GridLayout 的构造方法"></a>4.2 GridLayout 的构造方法</h5><pre><code>GridLayout(int rows,int cols)：创建一个指定行（rows）和列（cols）的网格布局。布局中所有组件的大小一样，组件之间没有间隔。
GridLayout(int rows,int cols,int hgap,int vgap)：创建一个指定行（rows）和列（cols）的网格布局，并且可以指定组件之间横向（hgap）和纵向（vgap）的间隔，单位是像素。
</code></pre>
<h5 id="4-3-示例"><a href="#4-3-示例" class="headerlink" title="4.3 示例"></a>4.3 示例</h5><blockquote>
<p>使用 GridLayout 类的网格布局设计一个简单计算器。代码的实现如下：</p>
</blockquote>
<pre><code>import javax.swing.*;
import java.awt.*;

public class GridLayoutDemo &#123;
    public static void main(String[] args) &#123;
        JFrame frame=new JFrame(&quot;GridLayou布局计算器&quot;);

        JPanel panel=new JPanel();    //创建面板
        //指定面板的布局为GridLayout，4行4列，间隙为5
        panel.setLayout(new GridLayout(4,4,5,5));
        panel.add(new JButton(&quot;7&quot;));    //添加按钮
        panel.add(new JButton(&quot;8&quot;));
        panel.add(new JButton(&quot;9&quot;));
        panel.add(new JButton(&quot;/&quot;));
        panel.add(new JButton(&quot;4&quot;));
        panel.add(new JButton(&quot;5&quot;));
        panel.add(new JButton(&quot;6&quot;));
        panel.add(new JButton(&quot;*&quot;));
        panel.add(new JButton(&quot;1&quot;));
        panel.add(new JButton(&quot;2&quot;));
        panel.add(new JButton(&quot;3&quot;));
        panel.add(new JButton(&quot;-&quot;));
        panel.add(new JButton(&quot;0&quot;));
        panel.add(new JButton(&quot;.&quot;));
        panel.add(new JButton(&quot;=&quot;));
        panel.add(new JButton(&quot;+&quot;));

        frame.add(panel);    //添加面板到容器
        frame.setBounds(300,200,200,150);
        frame.setVisible(true);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    &#125;
&#125;
</code></pre>
<blockquote>
<p>上述程序设置面板为 4 行 4 列、间隙都为 5 像素的网格布局，在该面板上包含 16 个按钮，其横向和纵向的间隙都为 5。该程序的运行结果如图所示：</p>
</blockquote>
<p><img src="https://img-blog.csdnimg.cn/20190305004128646.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzI3ODcwNDIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
<h4 id="五、网格包布局管理器-GridBagLayout"><a href="#五、网格包布局管理器-GridBagLayout" class="headerlink" title="五、网格包布局管理器(GridBagLayout)"></a><code>五、网格包布局管理器(GridBagLayout)</code></h4><h5 id="5-1-GridBagLayout"><a href="#5-1-GridBagLayout" class="headerlink" title="5.1 GridBagLayout"></a>5.1 GridBagLayout</h5><blockquote>
<p>GridBagLayout（网格包布局管理器）是在网格基础上提供复杂的布局，是最灵活、 最复杂的布局管理器。<code>GridBagLayout 不需要组件的尺寸一致，允许组件扩展到多行多列。</code>每个 GridBagLayout 对象都维护了一组动态的矩形网格单元，每个组件占一个或多个单元，所占有的网格单元称为组件的显示区域。<br>GridBagLayout 所管理的每个组件都与一个 GridBagConstraints 约束类的对象相关。这个约束类对象指定了组件的显示区域在网格中的位置，以及在其显示区域中应该如何摆放组件。除了组件的约束对象，GridBagLayout 还要考虑每个组件的最小和首选尺寸，以确定组件的大小。</p>
</blockquote>
<h5 id="5-2-GridBagLayout属性设置"><a href="#5-2-GridBagLayout属性设置" class="headerlink" title="5.2 GridBagLayout属性设置"></a>5.2 GridBagLayout属性设置</h5><blockquote>
<p>为了有效地利用网格包布局管理器，在向容器中添加组件时，必须定制某些组件的相关约束对象。GridBagConstraints 对象的定制是通过下列变量实现的。</p>
</blockquote>
<ul>
<li>1.gridx 和 gridy</li>
</ul>
<blockquote>
<p>用来指定组件左上角在网格中的行和列。<code>容器中最左边列的 gridx 为 0，最上边行的 gridy 为 0。</code>这两个变量的默认值是GridBagConstraints.RELATIVE，表示对应的组件将放在前一个组件的右边或下面。</p>
</blockquote>
<ul>
<li>2.gridwidth 和 gridheight</li>
</ul>
<blockquote>
<p><code>用来指定组件显示区域所占的列数和行数，以网格单元而不是像素为单位，默认值为 1。</code></p>
</blockquote>
<ul>
<li>3.fill</li>
</ul>
<blockquote>
<p><code>指定组件填充网格的方式</code>，可以是如下值：GridBagConstraints.NONE（默认值）、GridBagConstraints.HORIZONTAL（组件横向充满显示区域，但是不改变组件高度）、GridBagConstraints.VERTICAL（组件纵向充满显示区域，但是不改变组件宽度）以及 GridBagConstraints.BOTH（组件横向、纵向充满其显示区域）。</p>
</blockquote>
<ul>
<li>4.ipadx 和 ipady</li>
</ul>
<blockquote>
<p><code>指定组件显示区域的内部填充，</code>即在组件最小尺寸之外需要附加的像素数，<code>默认值为 0。</code></p>
</blockquote>
<ul>
<li>5.insets</li>
</ul>
<blockquote>
<p><code>指定组件显示区域的外部填充，</code>即组件与其显示区域边缘之间的空间， <code>默认组件没有外部填充。</code></p>
</blockquote>
<ul>
<li>6.anchor</li>
</ul>
<blockquote>
<p><code>指定组件在显示区域中的摆放位置。</code>可选值有 GridBagConstraints.CENTER（默认值）、GridBagConstraints.NORTH、GridBagConstraints.NORTHEAST、GridBagConstraints.EAST、GridBagConstraints.SOUTH、GridBagConstraints.SOUTHEAST、GridBagConstraints.WEST、GridBagConstraints.SOUTHWEST 以及 GridBagConstraints.NORTHWEST。</p>
</blockquote>
<ul>
<li>7.weightx 和 weighty</li>
</ul>
<blockquote>
<p><code>用来指定在容器大小改变时，增加或减少的空间如何在组件间分配，默认值为 0，</code>即所有的组件将聚拢在容器的中心，多余的空间将放在容器边缘与网格单元之间。weightx 和 weighty 的取值一般在 0.0 与 1.0 之间，数值大表明组件所在的行或者列将获得更多的空间。</p>
</blockquote>
<h5 id="5-3-示例"><a href="#5-3-示例" class="headerlink" title="5.3 示例"></a>5.3 示例</h5><blockquote>
<p>创建一个窗口，使用 GridBagLayout 进行布局，实现一个简易的手机拨号盘。这里要注意如何控制行内组件的显示方式以及使用 GridBagConstraints.REMAINDER 来控制一行的结束。代码的实现如下：</p>
</blockquote>
<pre><code>import javax.swing.*;
import java.awt.*;

public class GridBagLayoutDemo &#123;

    //向JFrame中添加JButton按钮
    public static void makeButton(String title,JFrame frame,GridBagLayout gridBagLayout,GridBagConstraints constraints) &#123;
        JButton button=new JButton(title);    //创建Button对象
        gridBagLayout.setConstraints(button,constraints);
        frame.add(button);
    &#125;
    public static void main(String[] agrs) &#123;
        JFrame frame=new JFrame(&quot;拨号盘&quot;);
        GridBagLayout gbaglayout=new GridBagLayout();    //创建GridBagLayout布局管理器
        GridBagConstraints constraints=new GridBagConstraints();
        frame.setLayout(gbaglayout);    //使用GridBagLayout布局管理器
        constraints.fill = GridBagConstraints.BOTH;    //组件填充显示区域
        constraints.weightx=0.0;    //恢复默认值
        constraints.gridwidth = GridBagConstraints.REMAINDER;    //结束行
        JTextField tf=new JTextField(&quot;13612345678&quot;);
        gbaglayout.setConstraints(tf, constraints);
        frame.add(tf);
        constraints.weightx=0.5;    // 指定组件的分配区域
        constraints.weighty=0.2;
        constraints.gridwidth=1;
        makeButton(&quot;7&quot;,frame,gbaglayout,constraints);    //调用方法，添加按钮组件
        makeButton(&quot;8&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=GridBagConstraints.REMAINDER;    //结束行
        makeButton(&quot;9&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=1;    //重新设置gridwidth的值

        makeButton(&quot;4&quot;,frame,gbaglayout,constraints);
        makeButton(&quot;5&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=GridBagConstraints.REMAINDER;
        makeButton(&quot;6&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=1;

        makeButton(&quot;1&quot;,frame,gbaglayout,constraints);
        makeButton(&quot;2&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=GridBagConstraints.REMAINDER;
        makeButton(&quot;3&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=1;

        makeButton(&quot;返回&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=GridBagConstraints.REMAINDER;
        makeButton(&quot;拨号&quot;,frame,gbaglayout,constraints);
        constraints.gridwidth=1;
        frame.setBounds(400,400,400,400);    //设置容器大小
        frame.setVisible(true);
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    &#125;
&#125;
</code></pre>
<blockquote>
<p>在上述程序中创建了一个 makeButton() 方法，用来将 JButton 组件添加到 JFrame 窗口中。在 main() 方法中分别创建了 GridBagLayout 对象和 GridBagConstraints 对象，然后设置 JFrame 窗口的布局为 GridBagLayout，并设置了 GridBagConstraints 的一些属性。接着将 JTextField 组件添加至窗口中，并通知布局管理器的 GridBagConstraints 信息。<br>在接下来的代码中，调用 makeButton() 方法向 JFrame 窗口填充按钮，并使用 GridBagConstraints. REMAINDER 来控制结束行。当一行结束后，重新设置 GridBagConstraints 对象的 gridwidth 为 1。最后设置 JFrame 窗口为可见状态，程序运行后的拨号盘效果如图所示：<br><img src="https://img-blog.csdnimg.cn/20190305005713575.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzI3ODcwNDIx,size_16,color_FFFFFF,t_70" alt="在这里插入图片描述"></p>
</blockquote>
<h4 id="事件处理机制"><a href="#事件处理机制" class="headerlink" title="事件处理机制"></a>事件处理机制</h4><p>在Java中，事件处理机制是一种常用的编程模式，它允许程序响应用户操作、系统事件等各种不同类型的事件。以下是Java中的事件处理机制：</p>
<ol>
<li>事件源</li>
</ol>
<p>事件源是指能够产生事件的对象，例如按钮、文本框、菜单等。</p>
<ol start="2">
<li>事件监听器</li>
</ol>
<p>事件监听器是一个接口，定义了用于监听特定事件的方法。通常情况下，每个事件都有自己对应的事件监听器。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface ActionListener &#123;</span><br><span class="line">    public void actionPerformed(ActionEvent e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>注册事件监听器</li>
</ol>
<p>将事件监听器注册到事件源上，以便在事件发生时能够被及时捕捉和处理。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JButton button = new JButton(&quot;Click me&quot;);</span><br><span class="line">button.addActionListener(new ActionListener() &#123; // 注册事件监听器</span><br><span class="line">    public void actionPerformed(ActionEvent e) &#123;</span><br><span class="line">        // 处理按钮单击事件</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>事件分发机制</li>
</ol>
<p>当事件发生时，事件源会创建一个相应的事件对象，并将其传递给已注册的所有事件监听器。事件监听器根据具体的事件类型来响应事件。</p>
<ol start="5">
<li>处理事件</li>
</ol>
<p>在事件监听器中实现事件响应的具体逻辑，例如弹出对话框、更新界面等。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">JButton button = new JButton(&quot;Click me&quot;);</span><br><span class="line">button.addActionListener(new ActionListener() &#123; // 注册事件监听器</span><br><span class="line">    public void actionPerformed(ActionEvent e) &#123;</span><br><span class="line">        JOptionPane.showMessageDialog(null, &quot;Button clicked&quot;); // 弹出对话框</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用事件处理机制时应该选择合适的事件源和事件监听器，并遵循事件驱动的编程模式来开发程序，以便实现良好的交互效果和用户体验。</p>
<h3 id="Java多线程"><a href="#Java多线程" class="headerlink" title="Java多线程"></a>Java多线程</h3><h4 id="进程和线程的区别（了解）"><a href="#进程和线程的区别（了解）" class="headerlink" title="进程和线程的区别（了解）"></a>进程和线程的区别（了解）</h4><p>进程和线程都是操作系统中用于实现多任务的概念，但它们有以下区别：</p>
<ol>
<li>调度对象</li>
</ol>
<p>进程是操作系统中资源分配的基本单位，一个进程可以包含多个线程，每个进程拥有独立的内存空间和系统资源。线程是进程的一个执行单元，线程共享进程的内存空间和系统资源。</p>
<ol start="2">
<li>系统开销</li>
</ol>
<p>创建和撤销进程需要较大的系统开销，包括内存空间、系统资源等的分配和回收。而创建和撤销线程的系统开销很小，因为它们共享进程的资源。</p>
<ol start="3">
<li>并发性能</li>
</ol>
<p>由于进程拥有独立的内存空间和系统资源，因此在多核CPU上能够实现真正的并发执行，但进程切换的代价较高。而线程共享进程的资源，因此在线程之间的切换代价较小，但在多核CPU上并不能实现真正的并发执行。</p>
<ol start="4">
<li>通信方式</li>
</ol>
<p>由于进程之间拥有独立的内存空间，因此它们必须通过IPC（进程间通信）机制进行通信。而线程之间共享进程的内存空间，因此它们可以直接访问共享的数据结构来进行通信。</p>
<p>需要注意的是，在使用多线程编程时应该遵循线程安全的原则，并合理地控制线程的数量和优先级，以便实现有效的并发性能和良好的用户体验。</p>
<h4 id="创建线程的方法"><a href="#创建线程的方法" class="headerlink" title="创建线程的方法"></a>创建线程的方法</h4><ol>
<li>继承Thread类</li>
</ol>
<p>继承Thread类并重写run()方法来定义线程的执行逻辑。使用start()方法启动线程。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyThread extends Thread &#123;</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        // 线程执行逻辑</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MyThread thread = new MyThread();</span><br><span class="line">thread.start(); // 启动线程</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>实现Runnable接口</li>
</ol>
<p>实现Runnable接口并重写run()方法来定义线程的执行逻辑。将Runnable对象作为参数传递给Thread类的构造方法以创建线程，并使用start()方法启动线程。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyRunnable implements Runnable &#123;</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        // 线程执行逻辑</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">MyRunnable runnable = new MyRunnable();</span><br><span class="line">Thread thread = new Thread(runnable);</span><br><span class="line">thread.start(); // 启动线程</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用多线程编程时应该遵循线程安全的原则，并合理地控制线程的数量和优先级，以便实现有效的并发性能和良好的用户体验。</p>
<p>3.在Java中，有两种创建多线程的方式：继承 <code>Thread</code> 类和实现 <code>Runnable</code> 接口。但是，这两种方式都只能返回 <code>void</code> 类型的结果，如果要求线程执行完毕后返回一个结果，可以使用 <code>Callable</code> 接口。</p>
<p><code>Callable</code> 是类似于 <code>Runnable</code> 的接口，它们都是为那些其实例可能被另一个线程执行的类设计的。一个关键的区别是 <code>Callable</code> 支持泛型的返回值，而在 <code>Runnable</code> 中的 <code>run()</code> 方法则不能有任何返回值。</p>
<p>下面是使用 <code>Callable</code> 接口创建多线程的示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">import java.util.concurrent.*;</span><br><span class="line"></span><br><span class="line">public class MyCallable implements Callable&lt;Integer&gt; &#123;</span><br><span class="line">    public Integer call() throws Exception &#123;</span><br><span class="line">        int sum = 0;</span><br><span class="line">        for (int i = 1; i &lt;= 100; i++) &#123;</span><br><span class="line">            sum += i;</span><br><span class="line">        &#125;</span><br><span class="line">        return sum;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws Exception &#123;</span><br><span class="line">        ExecutorService executor = Executors.newSingleThreadExecutor();</span><br><span class="line">        Future&lt;Integer&gt; future = executor.submit(new MyCallable());</span><br><span class="line">        System.out.println(future.get());</span><br><span class="line">        executor.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>在上面的示例代码中，我们定义了一个实现了 <code>Callable</code> 接口的类 <code>MyCallable</code>，其中 <code>call()</code> 方法返回一个 <code>Integer</code> 类型的值，表示对 1 到 100 求和的结果。然后，在 <code>main()</code> 方法中，我们通过 <code>ExecutorService</code> 接口创建了一个单一线程池，并通过 <code>submit()</code> 方法提交了一个 <code>MyCallable</code> 类的实例。最后，通过 <code>Future</code> 接口的 <code>get()</code> 方法获取线程执行的结果。</p>
<p>需要注意的是，在使用 <code>Callable</code> 创建多线程时，我们需要使用 <code>ExecutorService</code> 接口来管理线程池。并且，<code>Future</code> 接口的 <code>get()</code> 方法会阻塞当前线程，直到任务完成并返回结果。 </p>
<h4 id="线程的生命周期及其状态转换"><a href="#线程的生命周期及其状态转换" class="headerlink" title="线程的生命周期及其状态转换"></a>线程的生命周期及其状态转换</h4><p>在Java中，线程的生命周期可以描述为以下五个状态：</p>
<ol>
<li>新建状态（New）</li>
</ol>
<p>当线程对象被创建时，它处于新建状态。此时还没有开始执行线程的run()方法。</p>
<ol start="2">
<li>就绪状态（Runnable）</li>
</ol>
<p>当线程调用start()方法后，它进入就绪状态。此时线程已经获得了除CPU以外的所有资源，只等待系统为其分配CPU资源。</p>
<ol start="3">
<li>运行状态（Running）</li>
</ol>
<p>当线程获得CPU资源后，它进入运行状态。此时线程开始执行run()方法中的逻辑代码。</p>
<ol start="4">
<li>阻塞状态（Blocked）</li>
</ol>
<p>当线程因为某些原因无法继续执行时，例如线程调用sleep()方法、等待I&#x2F;O操作完成等，它进入阻塞状态。此时线程不会占用CPU时间，也不会参与竞争CPU资源。</p>
<ol start="5">
<li>终止状态（Terminated）</li>
</ol>
<p>当线程执行完run()方法或者发生异常而导致线程中断时，它进入终止状态。此时线程已经完成了它的使命，不再具有可执行性。</p>
<p>线程之间可以相互转换状态，主要包括以下三种状态转换：</p>
<ol>
<li><p>就绪状态 -&gt; 运行状态：当线程获得CPU资源时，它从就绪状态转换到运行状态。</p>
</li>
<li><p>运行状态 -&gt; 阻塞状态：当线程调用sleep()方法、wait()方法等导致它无法继续执行时，它从运行状态转换到阻塞状态。</p>
</li>
<li><p>阻塞状态 -&gt; 就绪状态：当线程调用sleep()方法、wait()方法等过程结束后，它从阻塞状态转换回就绪状态。</p>
</li>
</ol>
<p>需要注意的是，在使用多线程编程时应该遵循线程安全的原则，并合理地控制线程的数量和优先级，以便实现有效的并发性能和良好的用户体验。</p>
<p><img src="file:///C:/Users/26927/Pictures/Typedown/3385e99f-6fa8-42ec-a24b-a48134c68fa9.png" alt="3385e99f-6fa8-42ec-a24b-a48134c68fa9"></p>
<p><img src="https://www.runoob.com/wp-content/uploads/2014/01/java-thread.jpg" alt="java-thread"></p>
<h4 id="多线程的同步"><a href="#多线程的同步" class="headerlink" title="多线程的同步"></a>多线程的同步</h4><p>在多线程编程中，由于多个线程可能会同时访问和修改共享的资源，因此需要采取措施来保证线程之间的安全性和正确性。常用的同步方法有以下几种：</p>
<ol>
<li>synchronized关键字</li>
</ol>
<p>synchronized关键字可以修饰方法或代码块，它可以保证同一时间只有一个线程访问被synchronized修饰的代码区域。当线程进入synchronized代码块时，会自动获取相应对象的锁，其他线程必须等待该线程执行完毕后才能继续执行。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public synchronized void increment() &#123;</span><br><span class="line">    // 对共享资源进行操作</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Lock接口</li>
</ol>
<p>Lock接口提供了比synchronized更加灵活和高级的锁机制，它支持更多的锁类型、多个条件变量等功能。使用Lock接口时需要手动获取和释放锁，以保证同一时间只有一个线程访问关键代码区域。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Lock lock = new ReentrantLock();</span><br><span class="line">lock.lock(); // 获取锁</span><br><span class="line">try &#123;</span><br><span class="line">    // 对共享资源进行操作</span><br><span class="line">&#125; finally &#123;</span><br><span class="line">    lock.unlock(); // 释放锁</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>volatile关键字</li>
</ol>
<p>volatile关键字用于修饰共享变量，它可以保证所有线程都能够看到最新的变量值，从而避免出现重排序等问题。但是volatile并不能保证多个线程之间对共享变量进行复合操作的原子性，因此在这种情况下还需要采用其他同步方式。</p>
<p>在Java中，volatile是一种轻量级的同步机制，它可以保证多个线程访问共享变量时能够看到最新的值。具体来说，使用volatile关键字修饰的变量具有以下特性：</p>
<ol>
<li>可见性</li>
</ol>
<p>当一个线程修改了volatile变量的值时，其他线程都能够立即看到最新的值，而不会出现数据脏读等问题。</p>
<ol start="2">
<li>原子性</li>
</ol>
<p>对于单次读&#x2F;写操作的变量，volatile关键字保证了其原子性（即操作不可被拆分），但是对于复合操作（例如i++）依然无法保证其原子性。</p>
<ol start="3">
<li>禁止指令重排</li>
</ol>
<p>编译器和处理器在执行程序时为了提高效率，可能会对指令进行重排序，但这种重排序可能会导致程序出错。使用volatile关键字可以禁止指令重排，从而确保程序的正确性和安全性。</p>
<p>需要注意的是，虽然volatile可以保证可见性和禁止指令重排，但是它并不能替代锁机制，因此在需要保证原子性的操作中仍然需要采用锁或其他同步机制。</p>
<p>需要注意的是，在使用多线程编程时应该遵循线程安全的原则，并合理地控制线程的数量和优先级，以便实现有效的并发性能和良好的用户体验。</p>
<h4 id="多线程之间的通信"><a href="#多线程之间的通信" class="headerlink" title="多线程之间的通信"></a>多线程之间的通信</h4><p>在线程之间进行通信是多线程编程中非常重要的一部分，这可以帮助不同的线程协调工作并共享数据。常用的多线程通信方式有以下几种：</p>
<ol>
<li>wait()和notify()&#x2F;notifyAll()</li>
</ol>
<p>wait()和notify()&#x2F;notifyAll()方法是Object类中定义的方法，它们用于实现线程之间的等待和唤醒。当一个线程调用wait()方法时，它会释放对象的锁并进入等待状态，直到其他线程调用notify()&#x2F;notifyAll()方法来唤醒它；而当一个线程调用notify()&#x2F;notifyAll()方法时，它会唤醒一个或所有正在等待该对象的线程。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyRunnable implements Runnable &#123;</span><br><span class="line">    private Object lock = new Object();</span><br><span class="line"></span><br><span class="line">    public void run() &#123;</span><br><span class="line">        synchronized(lock) &#123;</span><br><span class="line">            try &#123;</span><br><span class="line">                lock.wait(); // 等待被唤醒</span><br><span class="line">            &#125; catch (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            // 执行相应操作</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void notifyThread() &#123;</span><br><span class="line">        synchronized(lock) &#123;</span><br><span class="line">            lock.notify(); // 唤醒等待线程</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>CountDownLatch类</li>
</ol>
<p>CountDownLatch类是Java提供的一种多线程同步工具，它可以让一个或多个线程等待其他线程完成操作后再继续执行。在初始化CountDownLatch时需要指定计数器的初始值，每当一个线程完成相应操作时，计数器的值就会减1；当计数器的值减为0时，等待线程就会被唤醒。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyRunnable implements Runnable &#123;</span><br><span class="line">    private CountDownLatch latch;</span><br><span class="line"></span><br><span class="line">    public MyRunnable(CountDownLatch latch) &#123;</span><br><span class="line">        this.latch = latch;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void run() &#123;</span><br><span class="line">        // 执行相应操作</span><br><span class="line">        latch.countDown(); // 计数器减1</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">CountDownLatch latch = new CountDownLatch(10);</span><br><span class="line">for(int i = 0; i &lt; 10; i++) &#123;</span><br><span class="line">    new Thread(new MyRunnable(latch)).start();</span><br><span class="line">&#125;</span><br><span class="line">latch.await(); // 等待所有线程完成操作</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>CyclicBarrier类</li>
</ol>
<p>CyclicBarrier类也是Java提供的一种多线程同步工具，它可以让多个线程在某个屏障处等待，并在所有线程都到达屏障时继续执行。在初始化CyclicBarrier时需要指定参与线程的数量，每当一个线程到达屏障时，它就会调用await()方法等待其他线程；当所有线程都到达屏障时，它们就可以继续执行。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyRunnable implements Runnable &#123;</span><br><span class="line">    private CyclicBarrier barrier;</span><br><span class="line"></span><br><span class="line">    public MyRunnable(CyclicBarrier barrier) &#123;</span><br><span class="line">        this.barrier = barrier;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void run() &#123;</span><br><span class="line">        // 执行相应操作</span><br><span class="line">        barrier.await(); // 等待其他线程到达屏障</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">CyclicBarrier barrier = new CyclicBarrier(10);</span><br><span class="line">for(int i = 0; i &lt; 10; i++) &#123;</span><br><span class="line">    new Thread(new MyRunnable(barrier)).start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用多线程编程时应该遵循线程安全的原则，并合理地控制线程的数量和优先级，以便实现有效的并发性能和良好的用户体验。</p>
<h4 id="线程池的使用（了解）"><a href="#线程池的使用（了解）" class="headerlink" title="线程池的使用（了解）"></a>线程池的使用（了解）</h4><p>线程池可以有效地管理和复用线程，从而提高程序的运行效率和性能。Java中提供了ThreadPoolExecutor类来实现线程池的功能，其使用方法如下：</p>
<ol>
<li>创建线程池</li>
</ol>
<p>可以通过ThreadPoolExecutor类的构造函数来创建一个线程池，其中需要指定核心线程数、最大线程数、线程空闲时间等参数。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ThreadPoolExecutor executor = new ThreadPoolExecutor(</span><br><span class="line">    5, // 核心线程数</span><br><span class="line">    10, // 最大线程数</span><br><span class="line">    60, // 线程空闲时间（秒）</span><br><span class="line">    TimeUnit.SECONDS,</span><br><span class="line">    new ArrayBlockingQueue&lt;Runnable&gt;(100)); // 任务队列</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>提交任务</li>
</ol>
<p>可以通过executor.submit()或executor.execute()方法来向线程池提交任务，其中submit()方法返回一个Future对象，可以用于获取任务执行结果。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">executor.submit(new Runnable() &#123;</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        // 执行相应任务</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>关闭线程池</li>
</ol>
<p>可以通过executor.shutdown()或executor.shutdownNow()方法来关闭线程池，其中shutdown()方法是平滑关闭线程池，会等待所有任务执行完毕后再关闭；而shutdownNow()方法是强制关闭线程池，会立即终止所有正在执行的任务。</p>
<p>示例代码：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">executor.shutdown(); // 平滑关闭线程池</span><br><span class="line">// 或者</span><br><span class="line">executor.shutdownNow(); // 强制关闭线程池</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用线程池时应该合理地设置核心线程数、最大线程数和任务队列大小，以避免因线程过多或任务过多导致系统资源不足或死锁等问题。同时，也应该遵循线程安全的原则来编写并发程序，以保证程序的正确性和稳定性。</p>
<h3 id=""><a href="#" class="headerlink" title=""></a></h3><h3 id="Java网络编程"><a href="#Java网络编程" class="headerlink" title="Java网络编程"></a>Java网络编程</h3><h4 id="网络通信协议-了解"><a href="#网络通信协议-了解" class="headerlink" title="网络通信协议(了解)"></a>网络通信协议(了解)</h4><p>网络通信协议是计算机网络中用于规定数据传输格式和传输方式的约定，是保证网络通信正确、高效、安全的基础。常见的网络通信协议有以下几种：</p>
<ol>
<li>TCP&#x2F;IP协议</li>
</ol>
<p>TCP&#x2F;IP协议是Internet网络使用的最基本的协议，它包括两个部分：传输控制协议（TCP）和互联网协议（IP）。TCP负责可靠地传输数据，确保数据的正确性和完整性；而IP负责将数据包从源主机传输到目标主机。</p>
<ol start="2">
<li>HTTP协议</li>
</ol>
<p>HTTP协议是超文本传输协议，它是一种基于请求-响应模式的协议，常用于Web应用中进行客户端和服务器之间的通信。HTTP使用TCP作为其传输层协议，支持三种请求方法：GET、POST和HEAD。</p>
<ol start="3">
<li>FTP协议</li>
</ol>
<p>FTP协议是文件传输协议，用于在客户端和服务器之间传输文件。FTP使用TCP作为其传输层协议，支持文件上传、下载、重命名等操作。</p>
<ol start="4">
<li>SMTP协议</li>
</ol>
<p>SMTP协议是简单邮件传输协议，用于在不同的邮件系统之间传送邮件。SMTP使用TCP作为其传输层协议，支持邮件发送、接收、转发等操作。</p>
<ol start="5">
<li>DNS协议</li>
</ol>
<p>DNS协议是域名系统协议，用于将域名转换成IP地址或反向解析。DNS使用UDP作为其传输层协议，支持域名解析、缓存查询等操作。</p>
<ol start="6">
<li>SSL&#x2F;TLS协议</li>
</ol>
<p>SSL&#x2F;TLS协议是安全套接字层&#x2F;传输层安全协议，用于保护网络通信的安全性。SSL&#x2F;TLS使用TCP作为其传输层协议，在数据传输过程中使用加密技术和数字证书来确保数据的保密性和完整性。</p>
<p>需要注意的是，不同的网络通信协议具有不同的特点和适用范围，应根据具体需求选择合适的协议，并遵守相应的协议规范来编写程序。同时，也应该关注网络安全问题，采取相应措施保障数据的安全性和隐私性。</p>
<h6 id="TCP-x2F-IP参考模型"><a href="#TCP-x2F-IP参考模型" class="headerlink" title="TCP&#x2F;IP参考模型"></a>TCP&#x2F;IP参考模型</h6><p><img src="file:///C:/Users/26927/Pictures/Typedown/207be562-0a32-4f5c-8b85-8563a8c56db7.png" alt="207be562-0a32-4f5c-8b85-8563a8c56db7"></p>
<p>TCP&#x2F;IP参考模型分为四层，从下往上依次是：网络接口层、网络层、传输层和应用层。每个层次都有其特定功能，并且使用不同的协议来实现这些功能。具体地：</p>
<ol>
<li><p><strong>网络接口层</strong>：该层负责处理与物理媒介（如网线、光纤等）相关的所有问题，包括数据在媒介上传输的方式、流控制、错误检测和物理寻址等。在TCP&#x2F;IP模型中，这一层对应OSI模型中的物理层和数据链路层。常见的协议有Ethernet、Wi-Fi、PPP等。</p>
</li>
<li><p><strong>网络层</strong>：该层主要是解决数据在网络中的传输问题，包括数据包的路由选择和转发、IP地址的管理和分配等。在TCP&#x2F;IP模型中，这一层对应OSI模型中的网络层。常见的协议有IP、ICMP、ARP等。</p>
</li>
<li><p><strong>传输层</strong>：该层负责数据的可靠传输，包括建立连接、流量控制、数据重传等功能。在TCP&#x2F;IP模型中，这一层对应OSI模型中的传输层和会话层。常见的协议有TCP、UDP等。</p>
</li>
<li><p><strong>应用层</strong>：该层提供网络应用程序与用户之间的接口，负责实现具体的应用功能，如电子邮件、文件传输、网页浏览等。在TCP&#x2F;IP模型中，这一层对应OSI模型中的表示层和应用层。常见的协议有HTTP、FTP、SMTP等。</p>
</li>
</ol>
<h4 id="UDP通信-了解"><a href="#UDP通信-了解" class="headerlink" title="UDP通信(了解)"></a>UDP通信(了解)</h4><p>UDP通信是一种基于用户数据报协议（User Datagram Protocol，简称UDP）的无连接网络通信方式，它不需要像TCP一样先建立连接再进行数据传输，因此在网络上的实时性和效率比较高。</p>
<p>UDP通信的特点如下：</p>
<ol>
<li>无连接</li>
</ol>
<p>UDP通信不需要像TCP一样先建立连接，可以直接发送数据包。因此，UDP的开销比TCP要小，适用于一些实时性要求高的场合。</p>
<ol start="2">
<li>不可靠</li>
</ol>
<p>由于UDP没有连接的概念，所以不会保证数据传输的可靠性，可能会出现数据丢失、重复、乱序等问题。</p>
<ol start="3">
<li>简单</li>
</ol>
<p>相对于TCP而言，UDP的实现比较简单，代码量也比较少，因此在实现一些简单的网络应用时，可以考虑使用UDP。</p>
<p>UDP通信的使用步骤如下：</p>
<ol>
<li>创建DatagramSocket对象，并指定客户端的IP地址和端口号。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DatagramSocket socket = new DatagramSocket();</span><br><span class="line">InetAddress address = InetAddress.getByName(&quot;127.0.0.1&quot;);</span><br><span class="line">int port = 8888;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><p>创建DatagramPacket对象，用于封装数据包。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">byte[] data = &quot;Hello, UDP!&quot;.getBytes();</span><br><span class="line">DatagramPacket packet = new DatagramPacket(data, data.length, address, port);</span><br></pre></td></tr></table></figure>
</li>
<li><p>发送数据包。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.send(packet);</span><br></pre></td></tr></table></figure>
</li>
<li><p>接收数据包。</p>
</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">DatagramPacket receivePacket = new DatagramPacket(buffer, buffer.length);</span><br><span class="line">socket.receive(receivePacket);</span><br><span class="line">String message = new String(receivePacket.getData(), 0, receivePacket.getLength());</span><br></pre></td></tr></table></figure>

<ol start="5">
<li><p>关闭DatagramSocket对象。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.close();</span><br></pre></td></tr></table></figure></li>
</ol>
<p>需要注意的是，在使用UDP通信时需要处理好数据传输的可靠性和安全性问题。例如，可以采用分组确认、校验码等技术来提高数据传输的可靠性；同时，也可以采用加密技术和数字签名等手段来保证数据的安全性和完整性</p>
<h4 id="TCP通信（了解）"><a href="#TCP通信（了解）" class="headerlink" title="TCP通信（了解）"></a>TCP通信（了解）</h4><p>TCP通信是一种基于传输控制协议（Transmission Control Protocol，简称TCP）的面向连接的网络通信方式，它可以保证数据传输的可靠性和完整性，被广泛应用于互联网上的各种应用中。</p>
<p>TCP通信的特点如下：</p>
<ol>
<li>面向连接</li>
</ol>
<p>TCP通信需要先建立连接，然后才能进行数据传输。在连接建立过程中会进行一系列的握手过程，以确保连接的可靠性和正确性。</p>
<ol start="2">
<li>可靠性高</li>
</ol>
<p>由于TCP采用了一系列的流量控制、拥塞控制以及确认机制等技术，因此可以保证数据传输的可靠性和完整性，不容易出现数据丢失、重复、乱序等问题。</p>
<ol start="3">
<li>稳定性好</li>
</ol>
<p>TCP通信具有较好的稳定性，即使在网络状况较差的情况下也可以保持较高的传输效率和速度。</p>
<p>TCP通信的使用步骤如下：</p>
<ol>
<li>创建Socket对象，并指定服务器的IP地址和端口号。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Socket socket = new Socket(&quot;127.0.0.1&quot;, 8888);</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>获取输入输出流，进行数据的读写操作。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">InputStream inputStream = socket.getInputStream();</span><br><span class="line">OutputStream outputStream = socket.getOutputStream();</span><br><span class="line"></span><br><span class="line">// 读取数据</span><br><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">int length = inputStream.read(buffer);</span><br><span class="line">String message = new String(buffer, 0, length);</span><br><span class="line"></span><br><span class="line">// 发送数据</span><br><span class="line">String data = &quot;Hello, TCP!&quot;;</span><br><span class="line">outputStream.write(data.getBytes());</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>关闭Socket对象。</li>
</ol>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">socket.close();</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在使用TCP通信时需要处理好连接建立和断开的过程，以及数据传输的可靠性和安全性问题。例如，可以采用连接池和连接复用技术来提高连接的利用率；同时，也可以采用加密技术和数字签名等手段来保证数据的安全性和完整性。</p>
<h4 id="网络程序的开发"><a href="#网络程序的开发" class="headerlink" title="网络程序的开发"></a>网络程序的开发</h4><p>网络程序的开发一般需要遵循以下步骤：</p>
<ol>
<li>设计网络协议</li>
</ol>
<p>在进行网络程序开发之前，需要首先设计好通信协议，即规定数据传输格式和传输方式的约定。这是保证网络通信正确、高效、安全的基础。常见的网络通信协议有TCP&#x2F;IP、HTTP、FTP、SMTP等，应根据具体需求选择合适的协议，并遵守相应的协议规范来编写程序。</p>
<ol start="2">
<li>编写服务器端程序</li>
</ol>
<p>服务器端程序一般用于监听客户端的请求，并进行相应的处理。在编写服务器端程序时，需要使用相应的网络库或框架，例如Java中的Socket、ServerSocket、Netty、Spring Boot等，根据设计好的协议进行数据处理和响应。</p>
<ol start="3">
<li>编写客户端程序</li>
</ol>
<p>客户端程序一般用于向服务器端发送请求，并接收服务器端的响应。在编写客户端程序时，也需要使用相应的网络库或框架，例如Java中的Socket、URLConnection、HttpClient、OkHttp等，根据设计好的协议进行数据的读写。</p>
<ol start="4">
<li>测试和调试</li>
</ol>
<p>完成程序的编写后，需要进行测试和调试，以确保程序的正确性和稳定性。可以采用各种工具和技术，例如Junit、Mockito、Postman、Wireshark等，对程序进行单元测试、集成测试、接口测试、网络流量分析等，及时发现并修复问题。</p>
<ol start="5">
<li>部署和运维</li>
</ol>
<p>当程序测试通过后，就可以进行部署和运维了。需要将程序发布到相应的服务器上，配置好环境和参数，并设置相应的监控和日志系统，以便及时发现和解决问题。同时，也需要对程序进行维护和更新，以满足不断变化的业务需求和安全风险。</p>
<p>在Java中，可以使用多种方式来开发网络程序，常用的包括Socket、ServerSocket、URLConnection、HttpClient、Netty等。</p>
<p>使用Socket和ServerSocket：</p>
<p>Socket和ServerSocket是Java中最基本的网络编程类，它们提供了TCP&#x2F;IP协议的支持，可以实现客户端和服务器之间的通信。其中，Socket用于建立连接并进行数据传输，而ServerSocket用于监听客户端的请求和接受连接。以下是一个简单的例子：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 服务端代码</span><br><span class="line">ServerSocket serverSocket = new ServerSocket(8888);</span><br><span class="line">while (true) &#123;</span><br><span class="line">    Socket socket = serverSocket.accept();</span><br><span class="line">    InputStream inputStream = socket.getInputStream();</span><br><span class="line">    OutputStream outputStream = socket.getOutputStream();</span><br><span class="line"></span><br><span class="line">    // 读取客户端发送的数据</span><br><span class="line">    byte[] buffer = new byte[1024];</span><br><span class="line">    int length = inputStream.read(buffer);</span><br><span class="line">    String message = new String(buffer, 0, length);</span><br><span class="line"></span><br><span class="line">    // 处理数据</span><br><span class="line">    String response = &quot;Hello, &quot; + message;</span><br><span class="line"></span><br><span class="line">    // 发送响应数据</span><br><span class="line">    outputStream.write(response.getBytes());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 客户端代码</span><br><span class="line">Socket socket = new Socket(&quot;127.0.0.1&quot;, 8888);</span><br><span class="line">InputStream inputStream = socket.getInputStream();</span><br><span class="line">OutputStream outputStream = socket.getOutputStream();</span><br><span class="line"></span><br><span class="line">// 发送数据</span><br><span class="line">String data = &quot;World!&quot;;</span><br><span class="line">outputStream.write(data.getBytes());</span><br><span class="line"></span><br><span class="line">// 接收响应数据</span><br><span class="line">byte[] buffer = new byte[1024];</span><br><span class="line">int length = inputStream.read(buffer);</span><br><span class="line">String response = new String(buffer, 0, length);</span><br></pre></td></tr></table></figure>

<p> 使用URLConnection和HttpClient：</p>
<p>除了使用Socket和ServerSocket外，还可以使用Java内置的URLConnection和第三方库HttpClient来进行网络编程。URLConnection支持HTTP协议，可以实现简单的HTTP请求和响应。而HttpClient则是一个功能强大的HTTP客户端库，可以实现各种HTTP请求和响应。以下是一个示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 使用URLConnection发送GET请求</span><br><span class="line">URL url = new URL(&quot;http://example.com&quot;);</span><br><span class="line">HttpURLConnection connection = (HttpURLConnection)url.openConnection();</span><br><span class="line">connection.setRequestMethod(&quot;GET&quot;);</span><br><span class="line"></span><br><span class="line">InputStream inputStream = connection.getInputStream();</span><br><span class="line">BufferedReader reader = new BufferedReader(new InputStreamReader(inputStream));</span><br><span class="line">StringBuilder response = new StringBuilder();</span><br><span class="line">String line;</span><br><span class="line">while ((line = reader.readLine()) != null) &#123;</span><br><span class="line">    response.append(line);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 使用HttpClient发送POST请求</span><br><span class="line">CloseableHttpClient httpClient = HttpClients.createDefault();</span><br><span class="line">HttpPost httpPost = new HttpPost(&quot;http://example.com&quot;);</span><br><span class="line">httpPost.setEntity(new StringEntity(&quot;&#123;\&quot;key\&quot;:\&quot;value\&quot;&#125;&quot;));</span><br><span class="line"></span><br><span class="line">CloseableHttpResponse httpResponse = httpClient.execute(httpPost);</span><br><span class="line">HttpEntity entity = httpResponse.getEntity();</span><br><span class="line">String response = EntityUtils.toString(entity);</span><br></pre></td></tr></table></figure>

<p>使用Netty：</p>
<p>Netty是一个基于NIO的高性能网络编程框架，可以快速开发复杂的网络应用。它提供了丰富、灵活的API和事件模型，支持各种协议和编解码器。以下是一个简单的Netty案例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 服务端代码</span><br><span class="line">EventLoopGroup bossGroup = new NioEventLoopGroup(1);</span><br><span class="line">EventLoopGroup workerGroup = new NioEventLoopGroup();</span><br><span class="line">try &#123;</span><br><span class="line">    ServerBootstrap bootstrap = new ServerBootstrap();</span><br><span class="line">    bootstrap.group(bossGroup, workerGroup)</span><br><span class="line">             .channel(NioServerSocketChannel.class)</span><br><span class="line">             .childHandler(new ChannelInitializer&lt;SocketChannel&gt;() &#123;</span><br><span class="line">                 @Override</span><br><span class="line">                 public void initChannel(SocketChannel ch) &#123;</span><br><span class="line">                     ChannelPipeline pipeline = ch.pipeline();</span><br><span class="line">                     pipeline.addLast(new StringDecoder());</span><br><span class="line">                     pipeline.addLast(new StringEncoder());</span><br><span class="line">                     pipeline.addLast(new SimpleChannelInboundHandler&lt;String&gt;() &#123;</span><br><span class="line">                         @Override</span><br><span class="line">                         protected void channelRead0(ChannelHandlerContext ctx, String msg) &#123;</span><br><span class="line">                             String response = &quot;Hello, &quot; + msg;</span><br><span class="line">                             ctx.channel().writeAndFlush(response);</span><br><span class="line">                         &#125;</span><br><span class="line">                     &#125;);</span><br><span class="line">                 &#125;</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">    ChannelFuture future = bootstrap.bind(8888).sync();</span><br><span class="line">    future.channel().closeFuture().sync();</span><br><span class="line">&#125; finally &#123;</span><br><span class="line">    bossGroup.shutdownGracefully();</span><br><span class="line">    workerGroup.shutdownGracefully();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 客户端代码</span><br><span class="line">EventLoopGroup group = new NioEventLoopGroup();</span><br><span class="line">try &#123;</span><br><span class="line">    Bootstrap bootstrap = new Bootstrap();</span><br><span class="line">    bootstrap.group(group)</span><br><span class="line">             .channel(NioSocketChannel.class)</span><br><span class="line">             .handler(new ChannelInitializer&lt;SocketChannel&gt;() &#123;</span><br><span class="line">                 @Override</span><br><span class="line">                 public void initChannel(SocketChannel ch) &#123;</span><br><span class="line">                     ChannelPipeline pipeline = ch.pipeline();</span><br><span class="line">                     pipeline.addLast(new StringDecoder());</span><br><span class="line">                     pipeline.addLast(new StringEncoder());</span><br><span class="line">                     pipeline.addLast(new SimpleChannelInboundHandler&lt;String&gt;() &#123;</span><br><span class="line">                         @Override</span><br><span class="line">                         protected void channelRead0(ChannelHandlerContext ctx, String msg) &#123;</span><br><span class="line">                             System.out.println(msg);</span><br><span class="line">                             ctx.channel().close();</span><br><span class="line">                         &#125;</span><br><span class="line">                     &#125;);</span><br><span class="line">                 &#125;</span><br><span class="line">             &#125;);</span><br><span class="line"></span><br><span class="line">    ChannelFuture future = bootstrap.connect(&quot;127.0.0.1&quot;, 8888).sync();</span><br><span class="line">    future.channel().writeAndFlush(&quot;World!&quot;).sync();</span><br><span class="line">    future.channel().closeFuture().sync();</span><br><span class="line">&#125; finally &#123;</span><br><span class="line">    group.shutdownGracefully();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>需要注意的是，在进行网络程序开发时，需要处理好数据传输的可靠性和安全性问题。例如，可以采用分组确认、校验码、加密技术和数字签名等技术来提高数据传输的可靠性和安全性；同时，也应该关注网络攻击和安全漏洞等问题，采取相应措施保障网络的安全性。 </p>
<h3 id="JDBC编程"><a href="#JDBC编程" class="headerlink" title="JDBC编程"></a>JDBC编程</h3><h4 id="JDBC原理（了解）"><a href="#JDBC原理（了解）" class="headerlink" title="JDBC原理（了解）"></a>JDBC原理（了解）</h4><p><img src="file:///C:/Users/26927/Pictures/Typedown/53456b23-3d52-44f5-95db-3e65db0e04dc.png" alt="53456b23-3d52-44f5-95db-3e65db0e04dc"></p>
<p>JDBC是Java数据库连接的标准接口，它定义了一组用于访问和操作数据库的API。在使用JDBC时，可以通过DriverManager类来获取数据库连接，并使用Connection、Statement、PreparedStatement、ResultSet等接口来进行数据库操作。</p>
<p>JDBC的工作原理如下：</p>
<p><img src="file:///C:/Users/26927/Pictures/Typedown/56ad66ce-a912-4aaa-a2c7-577052c7e9f9.png" alt="56ad66ce-a912-4aaa-a2c7-577052c7e9f9"></p>
<ol>
<li>加载并注册数据库驱动</li>
</ol>
<p>要想连接数据库，首先需要加载并注册数据库驱动。可以通过Class.forName()方法或DriverManager.registerDriver()方法来完成这一步骤。</p>
<ol start="2">
<li>建立数据库连接</li>
</ol>
<p>在注册完数据库驱动后，就可以使用DriverManager.getConnection()方法来获取数据库连接。该方法需要传入数据库URL、用户名和密码等参数。如果连接成功，则返回一个Connection对象，否则抛出SQLException异常。</p>
<ol start="3">
<li>创建和执行SQL语句</li>
</ol>
<p>在获取到Connection对象后，就可以使用Statement、PreparedStatement等接口来创建和执行SQL语句。其中，Statement适用于静态SQL语句，而PreparedStatement适用于动态SQL语句。调用executeUpdate()方法可以执行更新操作，调用executeQuery()方法可以执行查询操作，并返回一个ResultSet对象。</p>
<ol start="4">
<li>处理查询结果</li>
</ol>
<p>对于执行查询操作返回的ResultSet对象，可以使用其提供的方法来遍历结果集、获取数据、关闭资源等操作。例如，可以使用next()方法来将游标移动到下一行，并返回一个布尔值表示是否还有更多的行；也可以使用getString()、getInt()等方法来获取指定列的数据。</p>
<ol start="5">
<li>关闭连接和资源</li>
</ol>
<p>当最终完成数据库操作后，应该及时关闭数据库连接和相关资源，以释放资源并避免内存泄漏。可以分别调用Connection、Statement和ResultSet对象的close()方法来实现关闭。</p>
<p>需要注意的是，在使用JDBC进行数据库编程时，应该遵循事务管理、连接池复用、预编译SQL语句等最佳实践，以提高程序的性能和可靠性。同时，也应该注意处理SQL注入等安全问题，采取相应措施保障数据的安全性。</p>
<h4 id="Connection接口、Statement接口、ResultSet接口、PreparedStatement接口的使用；"><a href="#Connection接口、Statement接口、ResultSet接口、PreparedStatement接口的使用；" class="headerlink" title="Connection接口、Statement接口、ResultSet接口、PreparedStatement接口的使用；"></a>Connection接口、Statement接口、ResultSet接口、PreparedStatement接口的使用；</h4><p>在JDBC中，Connection、Statement、ResultSet和PreparedStatement是常用的接口，用于进行数据库操作。</p>
<ol>
<li>Connection接口<br><img src="file:///C:/Users/26927/Pictures/Typedown/e456d783-f2f2-41c8-b426-2b0d1f6988d6.png" alt="e456d783-f2f2-41c8-b426-2b0d1f6988d6"><br><img src="file:///C:/Users/26927/Pictures/Typedown/82810560-a2f7-468d-a0bf-cc0cd2a366f5.png" alt="82810560-a2f7-468d-a0bf-cc0cd2a366f5"></li>
</ol>
<p>Connection接口代表一个与数据库的物理连接。使用该接口可以创建Statement、PreparedStatement等对象，也可以控制事务提交、回滚等操作。以下是使用Connection接口建立与MySQL数据库的连接的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String url = &quot;jdbc:mysql://localhost:3306/mydatabase&quot;;</span><br><span class="line">String user = &quot;root&quot;;</span><br><span class="line">String password = &quot;123456&quot;;</span><br><span class="line">Connection conn = DriverManager.getConnection(url, user, password);</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>Statement接口<br><img src="file:///C:/Users/26927/Pictures/Typedown/49f5e2f0-9b02-4080-93b2-24803483ef17.png" alt="49f5e2f0-9b02-4080-93b2-24803483ef17"></li>
</ol>
<p>Statement接口用于执行静态SQL语句（即在编译期就已经确定的SQL语句）。它提供了executeQuery()、executeUpdate()、execute()等方法来执行SQL语句，并返回ResultSet或int类型的结果。以下是使用Statement执行查询操作的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Statement stmt = conn.createStatement();</span><br><span class="line">ResultSet rs = stmt.executeQuery(&quot;SELECT * FROM users&quot;);</span><br><span class="line">while (rs.next()) &#123;</span><br><span class="line">    int id = rs.getInt(&quot;id&quot;);</span><br><span class="line">    String name = rs.getString(&quot;name&quot;);</span><br><span class="line">    int age = rs.getInt(&quot;age&quot;);</span><br><span class="line">    System.out.println(id + &quot;\t&quot; + name + &quot;\t&quot; + age);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>ResultSet接口</li>
<li><img src="file:///C:/Users/26927/Pictures/Typedown/256c026c-3def-47f9-8410-35f67b6bd683.png" alt="256c026c-3def-47f9-8410-35f67b6bd683"></li>
</ol>
<p>ResultSet接口代表一个结果集，其中包含查询到的数据。它提供了各种获取数据的方法，例如getInt()、getString()、getDouble()等方法。以下是使用ResultSet获取数据的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ResultSet rs = stmt.executeQuery(&quot;SELECT * FROM users&quot;);</span><br><span class="line">while (rs.next()) &#123;</span><br><span class="line">    int id = rs.getInt(&quot;id&quot;);</span><br><span class="line">    String name = rs.getString(&quot;name&quot;);</span><br><span class="line">    int age = rs.getInt(&quot;age&quot;);</span><br><span class="line">    System.out.println(id + &quot;\t&quot; + name + &quot;\t&quot; + age);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="4">
<li><p>PreparedStatement接口<br><img src="file:///C:/Users/26927/Pictures/Typedown/01bd44fd-44ca-4101-a9a6-edd48f5f3bfc.png" alt="01bd44fd-44ca-4101-a9a6-edd48f5f3bfc"></p>
</li>
<li><p><img src="file:///C:/Users/26927/Pictures/Typedown/92a78951-33e1-4e67-b96b-0a9c00075090.png" alt="92a78951-33e1-4e67-b96b-0a9c00075090"></p>
</li>
</ol>
<p>PreparedStatement接口用于执行动态SQL语句（即在运行期通过变量拼接而成的SQL语句）。相比Statement接口，PreparedStatement可以预编译SQL语句，在多次执行同一条SQL语句时可以提高性能，并且可以避免SQL注入攻击。以下是使用PreparedStatement执行插入操作的示例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PreparedStatement pstmt = conn.prepareStatement(&quot;INSERT INTO users(name, age) VALUES (?, ?)&quot;);</span><br><span class="line">pstmt.setString(1, &quot;Tom&quot;);</span><br><span class="line">pstmt.setInt(2, 18);</span><br><span class="line">int rowsAffected = pstmt.executeUpdate();</span><br></pre></td></tr></table></figure>

<p>以上是这四个接口的简单使用示例，实际开发中需要根据具体情况使用不同的接口来完成不同的数据库操作，同时注意处理异常、释放资源等问题</p>
<h4 id="使用JDBC操作数据库"><a href="#使用JDBC操作数据库" class="headerlink" title="使用JDBC操作数据库"></a>使用JDBC操作数据库</h4><p>使用JDBC操作数据库通常需要以下步骤：</p>
<ol>
<li>加载驱动程序</li>
</ol>
<p>在JDBC中，每个数据库驱动都由一个类实现。因此，首先需要通过Class.forName()方法加载相应的驱动程序类。例如，在MySQL中，可以使用以下代码来加载MySQL驱动程序：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Class.forName(&quot;com.mysql.cj.jdbc.Driver&quot;);</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>建立连接</li>
</ol>
<p>在加载完驱动程序后，就可以使用DriverManager.getConnection()方法建立与数据库的连接。该方法需要传入数据库URL、用户名和密码等参数。例如，在MySQL中，可以使用以下代码来建立数据库连接：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">String url = &quot;jdbc:mysql://localhost:3306/mydatabase&quot;;</span><br><span class="line">String user = &quot;root&quot;;</span><br><span class="line">String password = &quot;123456&quot;;</span><br><span class="line">Connection conn = DriverManager.getConnection(url, user, password);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>创建Statement或PreparedStatement对象</li>
</ol>
<p>在建立连接之后，就可以创建Statement或PreparedStatement对象，用于执行SQL语句。其中，Statement适用于静态SQL语句，而PreparedStatement适用于动态SQL语句。例如，在MySQL中，可以使用以下代码来创建Statement对象：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Statement stmt = conn.createStatement();</span><br></pre></td></tr></table></figure>

<p>或者创建PreparedStatement对象：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PreparedStatement pstmt = conn.prepareStatement(&quot;INSERT INTO users(name, age) VALUES (?, ?)&quot;);</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>执行SQL语句并处理结果</li>
</ol>
<p>在创建好Statement或PreparedStatement对象后，就可以调用其executeQuery()、executeUpdate()等方法执行SQL语句，并获取执行结果。例如，在MySQL中，可以使用以下代码来执行查询操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">ResultSet rs = stmt.executeQuery(&quot;SELECT * FROM users&quot;);</span><br><span class="line">while (rs.next()) &#123;</span><br><span class="line">    int id = rs.getInt(&quot;id&quot;);</span><br><span class="line">    String name = rs.getString(&quot;name&quot;);</span><br><span class="line">    int age = rs.getInt(&quot;age&quot;);</span><br><span class="line">    System.out.println(id + &quot;\t&quot; + name + &quot;\t&quot; + age);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>或者执行插入操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pstmt.setString(1, &quot;Tom&quot;);</span><br><span class="line">pstmt.setInt(2, 18);</span><br><span class="line">int rowsAffected = pstmt.executeUpdate();</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>关闭连接和相关资源</li>
</ol>
<p>在完成数据库操作后，应该及时关闭数据库连接和相关资源，以释放资源并避免内存泄漏。可以分别调用Connection、Statement和ResultSet对象的close()方法来实现关闭。</p>
<p>以上是使用JDBC操作数据库的基本流程，需要注意的是，实际开发中还需要考虑事务管理、异常处理、数据类型转换等问题，以保障程序的性能和可靠性。</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
  </entry>
  <entry>
    <title>解决此错误:无法将apis(com.google.common.base.Predicate)应用于(java.util.function.Predicate)</title>
    <url>/2024/04/06/swagger%E6%8A%A5%E9%94%99/</url>
    <content><![CDATA[<h1 id="解决此错误-无法将apis-com-google-common-base-Predicate-应用于-java-util-function-Predicate"><a href="#解决此错误-无法将apis-com-google-common-base-Predicate-应用于-java-util-function-Predicate" class="headerlink" title="解决此错误:无法将apis(com.google.common.base.Predicate)应用于(java.util.function.Predicate)"></a>解决此错误:无法将apis(com.google.common.base.Predicate)应用于(java.util.function.Predicate)</h1><p>在本篇博客中，我将分享我在完成黑马项目苍穹外卖导入knife4j依赖过程中遇到的报错。</p>
<p>通过这篇博客，希望能帮助到其他遇到相似问题的，并加深自对问题的理解解决能力</p>
<p>报错描述</p>
<p>apis(com.google.common.base.Predicate&lt;springfox.documentation.RequestHandler&gt;)’ in ‘springfox.documentation.spring.web.plugins.ApiSelectorBuilder’ cannot be applied to ‘(java.util.function.Predicate&lt;springfox.documentation.RequestHandler&gt;)</p>
<p>苍穹外卖提供的knife4j依赖如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">   &lt;groupId&gt;com.github.xiaoymin&lt;/groupId&gt;</span><br><span class="line">   &lt;artifactId&gt;knife4j-spring-boot-starter&lt;/artifactId&gt;</span><br><span class="line">   &lt;version&gt;3.0.2&lt;/version&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<p>配置类如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Bean</span><br><span class="line">  public Docket docket() &#123;</span><br><span class="line">      ApiInfo apiInfo = new ApiInfoBuilder()</span><br><span class="line">              .title(&quot;苍穹外卖项目接口文档&quot;)</span><br><span class="line">              .version(&quot;2.0&quot;)</span><br><span class="line">              .description(&quot;苍穹外卖项目接口文档&quot;)</span><br><span class="line">              .build();</span><br><span class="line">      Docket docket = new Docket(DocumentationType.SWAGGER_2)</span><br><span class="line">              .apiInfo(apiInfo)</span><br><span class="line">              .select()</span><br><span class="line">              .apis(RequestHandlerSelectors.basePackage(&quot;com.sky.controller&quot;))</span><br><span class="line">              .paths(PathSelectors.any())</span><br><span class="line">              .build();</span><br><span class="line">      return docket;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>在进行调试时发现这个依赖并未生效，访问doc.hmtl为404，想着可能是版本依赖问题，所以将版本改为了3.0.3，进行编译时</p>
<p>apis(RequestHandlerSelectors.basePackage(“com.sky.controller”))出现报错，apis(com.google.common.base.Predicate&lt;springfox.documentation.RequestHandler&gt;)’ in ‘springfox.documentation.spring.web.plugins.ApiSelectorBuilder’ cannot be applied to ‘(java.util.function.Predicate&lt;springfox.documentation.RequestHandler&gt;)传入参数与需要类型不一致导致，于是进行类型强转发现也没有用，于是查看pom.xml文件，发现导入了swagger2依赖，经过查询发现不能混合Springfox 2.9.2和knife4j依赖 -&gt;删除springfox启动程序，reload后成功启动。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">        &lt;groupId&gt;io.springfox&lt;/groupId&gt;</span><br><span class="line">        &lt;artifactId&gt;springfox-swagger2&lt;/artifactId&gt;</span><br><span class="line">        &lt;version&gt;2.9.2&lt;/version&gt;</span><br><span class="line">    &lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<p>由于Knife4j 使用了 Java 8 函数库，而 springfox-swagger2 使用了 Guava 类库。在旧版本的 springfox-swagger2（例如2.9.2版本）中，<code>RequestHandlerSelectors.basePackage</code>方法期望传入<code>com.google.common.base.Predicate&lt;springfox.documentation.RequestHandler&gt;</code>而不是<code>java.util.function.Predicate&lt;springfox.documentation.RequestHandler&gt;</code>。</p>
<p>通过以上方法，成功解决了这个报错，并顺进行了接口调试。希望这个经验可以对你有所帮助，如果有任何疑问或其他解决方法欢迎在评论中！</p>
]]></content>
      <categories>
        <category>java</category>
      </categories>
  </entry>
</search>
